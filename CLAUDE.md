# Megawide EPC Procurement â€” WPM Dashboard

## Project Overview
Work Package Management (WPM) Dashboard for Megawide Construction Corporation EPC projects. Tracks procurement work packages, award status, budgets, and contractors across multiple projects.

**Live URL:** https://pmodepartment.github.io/prc-app (login: `/login.html`)
**Staging URL:** https://fmlozano-pmo.github.io/prc-app-dev (login: `/login.html`)
**Stack:** Vanilla HTML/CSS/JS (no build step) + Supabase (PostgreSQL + Auth) + GitHub Pages hosting
**GitHub (prod):** https://github.com/PMODepartment/prc-app
**GitHub (staging):** https://github.com/fmlozano-pmo/prc-app-dev â€” branch `staging`, push via `git push dev staging:main`
**Supabase (prod):** `https://cayjeqeleenizbdzrums.supabase.co`
**Supabase (staging):** `https://duivwgmjcbxtfagkiqyj.supabase.co`

---

## Architecture

No build step â€” edit files directly, push to GitHub, GitHub Pages auto-deploys (~1â€“2 min).

### Key Files
| File | Purpose |
|---|---|
| `assets/js/auth.js` | Supabase auth wrapper â€” `AppAuth.requireLogin()`, `AppAuth.requireAdmin()`, `getSB()`, profile cache |
| `assets/js/db.js` | All DB operations via `WPDb.*` â€” also `computeStats()`, `Fmt.*`, `renderUserBar()` |
| `assets/js/ui.js` | Shared UI helpers â€” sidebar init, modals, toast, hamburger menu, iOS pinch-zoom prevention |
| `assets/css/dashboard.css` | Global styles, CSS variables, responsive breakpoints, view-tabs, mobile fixes |
| `supabase-schema.sql` | Full DB schema for reference |
| `assets/js/onboarding.js` | Role-aware guided-tour modal (`window.Onboarding`) â€” auto-opens on first login, re-openable via topbar **?** button. Self-contained (injects own CSS, follows dark mode via CSS vars). |
| `onboarding.html` | Standalone 13-slide onboarding deck for training (keyboard nav, F=fullscreen, P=print). Not wired into the app. |
| `WPM_Dashboard_Onboarding.pptx` | Editable PowerPoint version of the onboarding deck (same content). |

> `assets/js/` + `assets/css/` files are canonical. The stale **root-level duplicate** `auth.js`/`db.js`/`ui.js`/`charts.js`/`dashboard.css` copies (and `firebase-config.js`, `auth-guard.js`, etc.) were **deleted** 2026-07 (they were unreferenced dead code — see Known Issues #9). Don't recreate them.

### Pages
| File | Auth | Purpose |
|---|---|---|
| `login.html` | public | Sign-in + Step 2 project picker. Shows Portfolio Overview card (admins), project list, Add New Project (admin/super_admin). On create â†’ `project.html?id=<newId>`. |
| `register.html` | public | Self-registration (creates `pending` user) |
| `pending.html` | public | Shown to unapproved users |
| `forgot-password.html` | public | Password reset request â€” `redirectTo` points to `/prc-app/reset-password.html` (was `login.html`, which had no recovery handling) |
| `reset-password.html` | public | **Set-new-password page** â€” the target of the reset email link. Self-contained (own Supabase client + inline dark mode, like `forgot-password.html`). On load, `detectSessionInUrl` (default) parses the recovery token from the URL and fires `onAuthStateChange('PASSWORD_RECOVERY')`; the page then reveals New/Confirm password fields and calls `sb.auth.updateUser({password})`, signs out, and redirects to `login.html`. Shows an "invalid/expired link" error (with a link back to Forgot Password) when no recovery session settles (~1.8s) or the URL carries `error_description`. **REQUIRES** the redirect URL to be allow-listed in Supabase (see below). |
| `index.html` | user | Portfolio Overview â€” consolidated dashboard, 6 tabs (Dashboard tab merged into Overview) |
| `project.html` | user | Single project dashboard â€” 3 tabs |
| `wp-form.html` | user | Add / edit work package |
| `review.html` | user+ | **All Work Packages** — bulk view/edit surface (Grid + Cards). No approval tabs: every writing role auto-approves. Admin-only legacy-cleanup strip. |
| `admin.html` | admin + manager | User management + project management (admin/super_admin); Performance tab (all three roles) |
| `claim-form.html` | user | Add / edit claim or CO â€” **hidden feature, not yet active** |
| `my-wps.html` | user | Officer's WP list |
| `project-selector.html` | user | Standalone picker â€” **unused, not linked** |

---

## Database (Supabase)

**URL:** `https://cayjeqeleenizbdzrums.supabase.co`

### Tables
- **`projects`** â€” `id` (text PK e.g. 'AVR101'), name, location, status, budget_bcb, start_date, end_date
- **`users`** â€” `id` (UUID FK â†’ auth.users), name, email, role (`super_admin|admin|user|viewer`), status (`pending|approved|rejected`), projects (text[]), last_login
- **`work_packages`** â€” all WP fields. **Generated columns (never INSERT into):** `total_awarded` (= `awarded_cost + additionals`), `awarding_lead_time` (= `actual_awarding_date - awarding_date`), `variance` (= `approved_budget_bcb - total_awarded`). Use `awarded_cost` and `lead_time` instead. `unmap()` in `db.js` strips all three automatically.
- **`claims`** â€” `id`, `project_id`, `claim_no`, `claim_type`, `party` (`Client|Vendor`), `description`, `wp_no`, `contractor`, `date_filed`, `amount_claimed`, `basis`, `status`, `approved_amount`, `date_resolved`, `review_status`, `review_notes`, `remarks`, `submitted_by`

### WPDb API (db.js)
```js
WPDb.getProjects()                         // all projects
WPDb.getProject(id)
WPDb.createProject(data)
WPDb.updateProject(id, data)
WPDb.archiveProject(id)                    // sets status='archived'
WPDb.unarchiveProject(id)
WPDb.deleteProject(id)                     // also deletes all WPs

WPDb.getApprovedWPs(pid)                   // approved WPs for one project
WPDb.getAllApprovedWPs()                   // all approved WPs (single query)
WPDb.getApprovedWPsForProjects(ids)        // approved WPs for array of IDs â€” avoids N+1
WPDb.getAllWPs(pid)                        // all WPs regardless of status
WPDb.getPendingWPs()                       // pending_review WPs (admin)
WPDb.submitWP(data, user)                  // inserts with review_status='pending_review'; throws on error
WPDb.updateWP(id, data)                    // update (resets to pending_review); throws on error
WPDb.updateWPDirect(id, data)             // update without status change; throws on error
WPDb.approveWP(id)
WPDb.rejectWP(id, _, reason)
WPDb.deleteWP(id)                          // permanently deletes one WP; throws on error
WPDb.getAllUsers()
WPDb.updateUser(id, updates)
WPDb.deleteUser(id)                        // COMPLETE remove: purges public.users profile + auth.users login via admin_delete_user() RPC (admin "Remove")
WPDb.updateLastLogin(userId)
```

### Auth Flow
1. `getSB()` â€” returns `window.__sb` (Supabase client, UMD bundle, created synchronously on page load)
2. `AppAuth.requireLogin(cb)` â€” checks session â†’ loads profile from `sessionStorage` cache (`wpm_prof_{userId}`) or fetches from DB â†’ checks `status === 'approved'` â†’ calls cb(user, profile)
3. `AppAuth.requireAdmin(cb)` â€” wraps `requireLogin`, requires role in `['admin', 'super_admin']`
4. `AppAuth.logout()` â€” clears `wpm_prof_*` sessionStorage keys, signs out, redirects to login
5. Role in `window.__wpmRole`, profile in `window.__profile`

### Supabase Settings
- **Email confirmation disabled** â€” users go straight to `pending` for admin approval
- **Free tier cold start:** Pauses after 7 days inactivity â†’ 5â€“30s delay. Use UptimeRobot (ping every 3â€“4 days) to prevent.
- **Email rate limit:** ~3 auth emails/hour on free tier. Use custom SMTP (Resend/Brevo) for reliability.
- **Password-reset flow** (Auth â†’ URL Configuration): the **Redirect URLs allow-list must contain `https://pmodepartment.github.io/prc-app/**`** (wildcard). It was previously **empty**, so Supabase ignored `resetPasswordForEmail`'s `redirectTo` and fell back to the **Site URL** (`/prc-app/` = the portfolio, which needs login and has no reset form) â€” the reset link "went nowhere useful." With the wildcard added, the email link lands on **`reset-password.html`**, which handles the recovery token. If you add other post-auth redirect targets, they must match this allow-list (or add them).
- **Custom SMTP (Resend)**: configured then **reverted** (toggled OFF 2026-07 at user request) â€” back on the built-in email service. The Resend config (domain `megawide.com.ph` pending verification, `supabase-smtp` API key, sender/host/username) remains **saved** in Supabase; re-enabling is just the "Enable custom SMTP" toggle. DNS records to verify the domain live in the handoff Artifact. This is independent of the reset-link fix above.
- **Sign in with Microsoft (2026-08, `login.html`)** â€” a "Sign in with Microsoft" button (`#btnMicrosoft`) calls `sb.auth.signInWithOAuth({provider:'azure', ...})`, redirecting the whole page to Microsoft and back. **Requires manual setup, not yet done (as of this writing):** (1) an **Entra ID (Azure AD) app registration** in the Megawide tenant â€” Single tenant account type (this is what actually restricts sign-in to `@megawide.com.ph`, not app code), redirect URI `https://<supabase-project-ref>.supabase.co/auth/v1/callback`, a client secret, and **admin consent granted** for the `User.Read` delegated permission; (2) in Supabase **Authentication â†’ Providers â†’ Azure**, enable it and paste the Application (client) ID, client secret, and Tenant ID. Until both are done the button will error (provider not enabled). **First-time sign-in bootstrap:** a brand-new Microsoft identity has an `auth.users` row but no `public.users` profile yet â€” `ensureProfile(user)` in `login.html` inserts one (`name`/`email` from the Microsoft identity, `role:'user'`, `status:'pending'`) exactly like `register.html`'s self-registration insert, so they land on `pending.html` and go through the **same admin-approval queue as everyone else** (no auto-approve, by design/user request) â€” an admin still has to approve and assign role/projects/manager. The same `ensureProfile()` runs in the auto-restore-session check at page load, so it also covers a returning Microsoft user whose profile row hasn't been created yet. Relies on Supabase's automatic identity-linking for a verified email matching an existing `auth.users` row (so an existing email/password account with the same `@megawide.com.ph` address doesn't get duplicated) â€” verify this is enabled in the project's Auth settings when configuring the provider.

---

## DB Migrations â€” Run in Supabase SQL Editor (all IF NOT EXISTS, safe to re-run)

```sql
-- WP form fields
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS works text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS type_of_works text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS scope text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS actual_delivery date DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS type_of_service text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS charging_type text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS contract_package_no text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS co_description text DEFAULT NULL;
-- Bond / payment / retention
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS surety_bond text DEFAULT 'No';
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS performance_bond text DEFAULT 'No';
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS warranty_bond text DEFAULT 'No';
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS payment_terms_days integer;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS dp_percent numeric(5,4);
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS dp_terms text;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS dp_release_date date;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS dp_amount numeric(18,2);
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS retention_percent numeric(5,4);
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS retention_amount numeric(18,2);
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS approver_name text;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS approval_date date;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS submittal_document_type text;
-- Columns missing from original live DB
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS approver text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS support_team text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS delivery_status text DEFAULT 'Not Awarded';
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS submittal_type text DEFAULT 'Not Required';
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS dp_notes text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS retention_period text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS awarding_status text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS purchase_request text DEFAULT NULL;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS osm text DEFAULT 'No';   -- OSM flag (Yes/No). Column + WP-form field + importer RETAINED; the OSM FILTER was removed from both dashboard WP Lists (2026-07-23), and the OSM TAG was removed GLOBALLY from the UI (2026-07-24): the WP-form field (`f-osm`), the review-grid column, the importer's OSM rule bullet + KPI tile + preview column, and project.html's `_osmMatch` dead code. The DB column is UNTOUCHED — `wp-form` still saves `osm:'No'` and the importer still writes `osm` from Remarks, so existing rows and the column keep working; only the visible tag is gone.
-- Per-WP audit trail ("last change" log) — see MIGRATION_wp_audit_trail.sql (also recreates wp_view_public)
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS updated_at timestamptz;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS updated_by uuid;
ALTER TABLE work_packages ADD COLUMN IF NOT EXISTS updated_by_name text;
-- Users: activity tracking. WITHOUT this, getAllUsers() returns last_login=undefined for
-- every row and admin.html shows EVERY approved user as "Active" (the activity badge
-- degrades gracefully when the column is missing). auth.js/login.html already write it.
ALTER TABLE users ADD COLUMN IF NOT EXISTS last_login timestamptz;
-- Viewer role
ALTER TABLE users DROP CONSTRAINT IF EXISTS users_role_check;
ALTER TABLE users ADD CONSTRAINT users_role_check CHECK (role IN ('super_admin','admin','user','viewer'));
```

> The live DB was created from an early schema version. Any "column not found" error means a column needs to be added above â€” all statements are idempotent.

---

## BCB Baselines (BCB0 … BCB2)

A project's procurement budget is **re-baselined** over time, so the same work package can show a **saving against BCB0 and a loss against BCB1** (the re-baseline ate the savings). Run **`MIGRATION_bcb_baselines.sql`** once — it adds `budget_bcb0..budget_bcb2 numeric(18,2)` and backfills `budget_bcb0` from `approved_budget_bcb`.

- **`approved_budget_bcb` still holds the CURRENT baseline** (newest non-blank), so all ~110 existing read sites — charts, KPIs, tables, PDF, CSV, importer — keep working untouched. **Do not repoint them at a baseline column.**
- **The switcher re-maps the data, not the readers** (`applyBcbBaseline(wps, level)` in `db.js` returns a copy with `approved_budget_bcb` set to that baseline). `project.html` keeps the loaded rows in **`_rawWPs`** and `index.html` in **`_rawAll`**; `allWPs` / `_allWPs` are the mapped views. When you add a load path, map it the same way or the switcher will silently stop applying.
- **Carry-forward:** `bcbEffective(w, level)` returns that baseline's figure, else the newest EARLIER one — a WP not re-baselined at BCB2 keeps its BCB1 value. `bcbValue()` is the raw per-baseline figure (BCB0 falls back to the legacy single column so pre-migration rows still read).
- **A project may have NO BCB0.** QHL706's sheet heads its budget `BUDGET 1 - BCB1 (VAT EX)`, so all 170 WPs import into `budget_bcb1` and `budget_bcb0` is empty. `bcbLevelsPresent()` therefore reports **only populated levels** (it no longer hardcodes `bcb0:true`) and **`BcbBaseline.resolve(wps)`** renders the user's stored pick only if that baseline has data here, else the lowest level that does. Without this, QHL706 would show a **BCB0 tab displaying BCB1's own figures** (the `approved_budget_bcb` fallback). Rows imported before baselines existed have no level populated at all — BCB0 is then offered and stands for the single stored budget. **Both dashboards call `resolve()`, not `get()`.**
- **Default is BCB0**
- **UI:** per-project switcher `#ov-bcb-switch` on the Overview (beside Charging Scope, with a note counting re-baselined WPs); portfolio switcher `#idx-bcb-switch` in the filter bar. Both re-render everything — KPIs, batteries, period charts, backlog, WP List.
- **WP form:** Budget & Contract now has **Budget BCB0 (Net) \*** (the original, required) plus optional **Revised BCB1/BCB2**; blank means "unchanged at that baseline". Each field shows a live delta vs the previous baseline and, once an Awarded Cost is entered, whether that baseline turns the award into a **saving** or **OVER**. Save writes all four columns and sets `approved_budget_bcb` to the newest non-blank.
- **Review grid:** the Budget & Contract section shows **Budget BCB0 (required) / BCB1 / BCB2** — the SAME three fields as the WP form. There is deliberately **no "current" column**: `approved_budget_bcb` is DERIVED (the newest non-blank baseline), so showing it beside BCB0 duplicated the same figure and let the two drift. Editing ANY baseline recomputes and stages `approved_budget_bcb` (bcb2 ?? bcb1 ?? bcb0) in `_xlSet`, because every dashboard/chart/export reads that field — without it the KPIs wouldn't move. `xlVal` still falls back to `approved_budget_bcb` when reading BCB0 on rows imported before baselines existed.

---

## Role-Based Access Control

| Role | Projects Visible | Edit WPs | Auto-Approve | Admin Rights | Cost Data |
|---|---|---|---|---|---|
| `super_admin` | All | All | âœ… | Full | âœ… |
| `admin` | All | All | âœ… | Users + Projects | âœ… |
| `specialist` | All (read) | Assigned only | âœ… | None | âœ… |
| `manager` | Assigned | Assigned | âœ… | None | âœ… |
| `user` | Assigned | Assigned | ✅ | None | ✅ |
| `viewer` | Assigned | None | â€” | None | âŒ |
| `viewer_budget` | Assigned | None | â€”  | None | âœ… |

**`viewer_budget` (added 2026-08-03)** â€” identical to `viewer` in every read-only respect (no edit/add/delete anywhere, redirected off `wp-form.html`/`review.html`/`claim-form.html`, assigned-projects-only, same single-page stacked layout, Tools/Status-Tracker hidden) but CAN see budget/cost data. The one flag that used to conflate "read-only" and "hide cost" is now two flags: **`window.__isViewer`** (`true` for both `viewer` and `viewer_budget` â€” gates every read-only/layout behavior below, unchanged) and **`window.__hideBudget`** (`true` ONLY for `viewer` â€” gates every cost-hiding behavior). A parallel CSS class **`body.budget-hidden`** (toggled from `__hideBudget`) carries all the cost-hiding selectors that used to live under `body.viewer-mode`; `body.viewer-mode` itself keeps only the read-only/layout selectors (view-tabs hidden, single-page layout, Tools/Status-Tracker hidden, etc.) â€” those still apply to `viewer_budget` too. **Data path:** `db.js` `_wpRel()` now gates on `window.__wpmRole==='viewer'` specifically (not `__isViewer`), so `viewer_budget` reads `work_packages` directly (full cost columns) via RLS instead of the cost-free `wp_view_public` view â€” `viewer` is the only role still routed through that view. **Server-side:** `MIGRATION_viewer_budget_role.sql` (run once) widens `users_role_check` to allow the value and updates the `wp_insert`/`wp_update`/`wp_delete`/`claims_insert`/`claims_update` RLS policies to deny `viewer_budget` too (`not in ('viewer','viewer_budget')`, was `<> 'viewer'`); `wp_select` is untouched (viewer_budget was never excluded from it, so it already reads the base table once the client stops routing it to the view). Wired into: `login.html` (skip-picker redirect, role-label map), `wp-form.html`/`review.html`/`claim-form.html` (redirect guards â€” claim-form.html had no viewer redirect at all before this and now has one), `admin.html` (role `<select>`s + `roleLabel()`/`roleStyle()`), `onboarding.js` `tierOf()` (mapped into the `viewer` tier). **When adding a new cost-bearing UI element, gate it on `window.__hideBudget` (or `body.budget-hidden`), not `window.__isViewer` (or `body.viewer-mode`)** â€” the latter now also matches `viewer_budget`, which must keep seeing cost data.


**Auto-approve roles** (`AppAuth.isAutoApprove(profile)`): `super_admin`, `admin`, `specialist`, `manager`, `user` (added 2026-07 — regular users add/edit/delete WPs freely, no manager/WP-approver sign-off; only `viewer` is read-only). **ALL write paths must gate auto-approve on `AppAuth.isAutoApprove({role})`, never on a hardcoded `['admin','super_admin']` list** — the project.html quick-add modal (`saveQuickWP`) had that bug (fixed 2026-07): a `user`/`manager`/`specialist` quick-adding a WP left it stuck `pending_review` (invisible on dashboards, which query `review_status='approved'`) with no self-service approval. `wp-form.html`, the Status Tracker, and CSV import were already correct. **Archived projects are READ-ONLY for everyone** (incl. admins — they can only unarchive): `project.html` sets `window.__archived` + `body.archived-readonly` (CSS hides `#add-wp-link`/`#sidebar-tools`/`#status-tracker`; edit/delete links gated by `window.__archived`), and `openAddWPModal`/`saveQuickWP`/`updateWpStatus`/`deleteWPFromDetail`/`importWPsFromCSV` all early-return when archived; `wp-form.html` `submitForm()`/`deleteCurrentWP()` block if the selected project's status is `archived`. In the **Portfolio Overview** archived projects start **UNSELECTED** (`_activeIds` excludes `status==='archived'`) so they're out of the charts by default (their pill can still be toggled on). The **project-filter label** (`updateFilterLabel`) counts against **active (non-archived) projects only** — reads `"N of M active projects selected"` (e.g. `6 of 6 active projects selected`, not `6 of 7 projects selected`), appending ` (+K archived)` only when archived pills are toggled on. â€” WPs save directly as `approved`; no `pending_review` step.

**Specialist** sees all projects in login picker and `index.html` (same as admin) but `canAccessProject()` still limits editing to `profile.projects`. Uses `getAllApprovedWPs()` (single query, no N+1).

**Viewer restrictions** (`body.viewer-mode` + `window.__isViewer`): cost KPIs hidden, Budget tab hidden, Financial WP List tab hidden, cost columns excluded via `_getActiveCols()`/`getActiveCols()`, Add WP + Edit buttons hidden, Tools section hidden, `wp-form.html` redirects immediately. **Viewers must not see ANY budget / cost / award-amount in charts, graphs or tables** — enforced by `body.viewer-mode` CSS rules in BOTH pages plus a few JS guards. Hidden for viewers: the **Top 5 panels** (`.top5-row` — High Value / Savings / Overbudget), the **% Awarded by Value** battery (`#ov-battery-val` / `#idx-ov-battery-val` — the **% by WP** battery + count stats table stay), the **Procurement Budget (BCB) & Awarded by Period** chart panels (`#dash-period-panel`/`#idx-dash-period-panel`) and the **backlog period** chart panels (`#backlog-period-panel`/`#idx-bl-period-panel`), the **WP-List Filtered Analysis** (project `#pwa-analysis` via JS, portfolio `#wp-mon-analysis` via CSS), the **Status Tracker** (`#status-tracker`, JS), the project **Budget** + **Works** tabs (`#tab-works`/`#tab-works-btn`, plus a `switchTab` redirect to overview), the Schedule **Budget** data-mode toggle (`#sch-btn-budget` — chart stays in #WPs count mode), the backlog-table budget column (project `#backlog-table` col 6, portfolio `#bl-table` col 7), the portfolio **project-table Budget column** (`#proj-data-table` col 7) and the **project-card Budget/Awarded ₱ line** (guarded by `window.__isViewer` in `renderProjectCard`). When adding any new money-bearing chart/panel/column, add a matching `body.viewer-mode` hide rule.

**Viewer UI simplification (2026-07-30, `project.html`)** — because viewers are read-only with most panels stripped, the leftover UI was made lighter: the WP-List **"Set Procurement & Delivery status inline" hint** (`#wplist-edit-hint`) is hidden (viewers can't edit); the **Overview top row** (`.ov-top-row`) is centered with `max-width:860px` so the lone WP-status summary reads as a deliberate summary instead of a stranded half-width block (cost KPIs, period chart and Top 5 are all hidden for viewers). All `body.viewer-mode` CSS.

**Viewer single-page layout (2026-07-30, `project.html`)** — viewers get **no main tab bar** (`#view-tabs` hidden). Instead of switching between Overview / Backlog / WP List, **all three views are shown stacked on one scrolling page**, each under a red `.viewer-page-head` section header (Overview / Backlog / Work Packages). `switchView(v)` short-circuits to **`_viewerSinglePage()`** for viewers: it shows all three `#view-*` containers, injects the section header once each (`el.dataset.vhead` guard), sets `currentView='table'` (so the WP-List filter/search handlers that gate on it stay live), and renders all three (`renderOverview`/`renderBacklog`/`buildTable`). Non-viewers keep the normal tabbed behavior untouched.

Within the viewer single-page layout the **Work Packages section keeps the normal tabbed WP List** — the Overview/Schedule/Submittals sub-tab switcher (`#proj-view-tabs`) stays clickable and the single paged table renders as usual (an interim "stacked read-only sub-sections" experiment — `renderViewerWpSections`/`#viewer-wp-sections` — was **reverted** at user request). `renderCell` remains extracted to module scope as **`_wpRenderCell(key,wp,_stickyLeft)`** (+ helper `_fmtPct`) — a harmless refactor still used by `buildTable`. The Submittal-status donut is visible to viewers (not hidden). Award/All sub-tabs stay hidden for viewers (cost).

**Viewer single-page layout also applied to the Portfolio Overview (2026-07-30, `index.html`)** — same treatment, one level up: viewers get **no main tab bar** (`#view-tabs` hidden) and see **Overview → Backlog → Schedule → Work Packages stacked on one scrolling page** under red `.viewer-page-head` section headers (`_VIEWER_TABS`/`_VIEWER_TAB_HEADS`). **Budget and Works are excluded** — they're money-only tabs already fully hidden for viewers (`#tab-budget`/`#tab-budget-btn`/`#tab-works`/`#tab-works-btn`), same as before. `window.switchTab(tab)` short-circuits to **`_viewerSinglePage()`** for viewers (ignores the requested tab, ignores/ never sets `_activeTab`); `renderAll()`'s final "render the active tab" step also branches to `_viewerSinglePage()` for viewers so a project-filter change re-renders all four stacked sections instead of only `_activeTab`. Each section's `_renderTab(t,...)` call is unchanged — this only changes which tabs render and how they're laid out. The WP List section here keeps its own `wp-vtab` Overview/Award/Schedule/Submittals/All sub-tab switcher untouched (Award/All already hidden for viewers), matching the project-page decision not to further sub-divide it. `#idx-ov-battery-val`/Top 5/period-chart/etc. viewer cost-hides are unaffected.

**Viewer single-page layout, round 2 (2026-07-30) — project picker first, collapsible sections, sticky nav, WP % chart mode:**
- **Project selector relocated into the topbar (`index.html`).** `#proj-filter-wrap` (the "All projects ▾" dropdown) now lives in `.topbar-left` next to the READ-ONLY badge on desktop, via **`_viewerRelocateProjFilter()`** (called at the top of `_viewerSinglePage()`) — `window.matchMedia('(min-width:768px)')` decides: desktop moves it into `#topbar-proj-filter-slot` (a new empty slot in the topbar markup); narrow viewports move it back to `#proj-filter-anchor`, a stable marker left in its original spot inside `#filter-tabs-bar` (so it's never lost off-screen; re-evaluated on every `_viewerSinglePage()` call, i.e. every filter change / resize-triggered rerender). Compact chip styling via `body.viewer-mode .topbar-left #proj-filter-wrap` (smaller padding, no shadow, `--surface-2` fill, label ellipsis-clamped to 160px). `#idx-bcb-switch` (BCB baseline switch, when applicable) stays in `#filter-tabs-bar` untouched — it isn't cost data, so it was never gated for viewers.
- **Project cards/table moved to the very top of the page, ahead of Overview (`index.html`).** The cards+table markup (Cards/Table toggle, `#project-cards`, `#project-table-wrap`) is wrapped in **`#idx-viewer-projects`**; `_viewerSinglePage()` does `mainContent.insertBefore(projSec, mainContent.firstElementChild)` so it becomes the first thing on the page, given its own collapsible **"Projects"** section head. Non-viewers never call `_viewerSinglePage()`, so for them this markup stays exactly where it was authored (end of the Overview tab, after Top 5) — no DOM move, no behavior change. Rendering (`renderProjectCard`/`filterProjectTable`/etc.) is untouched since it targets the same element ids regardless of their position in the DOM.
- **Section dividers are now collapsible (both pages).** `.viewer-page-head` gained a caret (`.vph-caret`, rotates -90° when `.collapsed`), `cursor:pointer`, hover/`:focus-visible` states, and `role="button" tabindex="0"` + Enter/Space handling. Implementation: **`_mkCollapsibleHead(container,label,icon)`** — defined once per page (`index.html` and `project.html` each have their own copy, consistent with this codebase's no-shared-JS-module convention) — moves ALL of a section container's existing children into a new `.viewer-section-body` wrapper, then appends a clickable head + that body. Click toggles `body.style.display`. Idempotent via `container.dataset.vhead`. **Default state is expanded** on every page load (per user's explicit choice — collapse is a manual declutter action, not a default). Applied to: `#idx-viewer-projects` (Projects), `#tab-overview`/`#tab-backlog`/`#tab-schedule`/`#tab-wplist` (index.html), and `#view-overview`/`#view-backlog`/`#view-table` (project.html).
- **Backlog / WP List / Schedule-summary group tables start collapsed for viewers, on `index.html` only** (project.html wasn't part of this ask — its Backlog/WP List trade-groups still default expanded for everyone, unchanged). Three separate collapse mechanisms, one seed each:
  - **Backlog table** (two-level Project→Trade): `_blCollapseState` Map, key `'__P__'+pid`. Seeded `true` **once per key** (`if(window.__isViewer && !_blCollapseState.has(pKey)) _blCollapseState.set(pKey,true)`) right where the render loop first computes `pKey` — the `Map.has` guard means a viewer who manually expands a project stays expanded across re-renders (filter changes), it's only ever collapsed automatically on first encounter.
  - **WP List table** (two-level Project→Trade): same pattern on `_projCollapseState`, key = bare `pid`.
  - **Schedule "summary by project" table**: no persisted Map (this table is fully string-rebuilt every render, same for all roles) — instead the initial HTML itself renders the trade-header rows with `display:none` and the project indicator glyph as `+` (not `−`) whenever `window.__isViewer` (`const pStartCollapsed = !!window.__isViewer`). `toggleSchProj`'s existing `isHidden` detection (reads `tradeHdrs[0].style.display`) picks this up correctly with no changes to the toggle function itself.
  - In all three cases only the OUTER (project) grouping level starts collapsed — trade-level sub-groups, once a project is opened, render in their normal (expanded) state.
- **Sticky section-jump nav rail, both pages** — mirrors `wp-form.html`'s `#form-nav` pattern exactly (same CSS shape, same IntersectionObserver scroll-spy approach), adapted to a new class name (`.viewer-side-nav`) since it lives on a different set of pages. Markup: `<nav class="viewer-side-nav" id="viewer-side-nav">` inserted as the first child of `#main-content-region`, hidden (`display:none`) until built. `body.viewer-mode #main-content-region{display:flex;gap:20px;align-items:flex-start}` turns it into a flex row with `#main-content`/`#loading` (`flex:1;min-width:0`) — inert for non-viewers since the region stays `display:block` for them. **Desktop**: sticky **40px** left rail (narrowed 2026-07-30, was 200px — see follow-up note below), `top:76px` (topbar 60px + gap), `max-height:calc(100vh - 92px)`, own scrollbar. **Mobile (≤900px)**: becomes a sticky horizontal scroll-spy strip under the topbar (`top:52px`), pill-style active tab (bottom border instead of left border) — same breakpoint/values as wp-form. Section containers get `scroll-margin-top:76px` (`112px` on mobile) so `scrollIntoView` lands below the sticky chrome. **`window._viewerBuildNav()`** (called at the end of every `_viewerSinglePage()`) filters a candidate list down to sections currently `style.display !== 'none'` (so Budget/Works never appear in the nav) and rebuilds the link list + rewires `_viewerWireNavSpy(nav)`, which: (a) wires click → `scrollIntoView({behavior:'smooth'})`, auto-**expanding** the target section first if its head has `.collapsed` (`head.click()` before scrolling, so a nav click never lands on a blank collapsed section); (b) runs an `IntersectionObserver` (`rootMargin:'-80px 0px -55% 0px'`) that highlights whichever observed section is nearest the top, suppressed for 700ms after a click (`_viewerNavClickLock`) to avoid spy flicker during the scroll animation — identical timing to wp-form's `clickLock`. index.html's candidate list: Projects/Overview/Backlog/Schedule/Work Packages; project.html's: Overview/Backlog/Work Packages (no Projects/Schedule sections there).

**Sticky nav rail narrowed to 40px with rotated labels (2026-07-30, both pages)** — the original 200px labeled rail (icon + horizontal text, one per line) ate too much horizontal content width per user feedback. Desktop now uses a **40px rail with each label rotated 90°** (`writing-mode:vertical-rl; transform:rotate(180deg)` on the `<a>`, `display:block; text-align:center` — deliberately NOT `display:flex`, since mixing flexbox alignment with `writing-mode` on the same element is unreliable across engines) and **no icon** (`.viewer-side-nav a i{display:none}` — icons don't rotate sensibly under vertical writing-mode without a separate counter-rotation, and the option chosen was "rotated text, no icons"). `white-space:nowrap` is load-bearing here: without it a long label like "Work Packages" could wrap into a second vertical line and widen the rail past 40px. The **mobile (≤900px) horizontal strip is unchanged in spirit** but now explicitly resets `writing-mode:horizontal-tb; transform:none` and restores `display:flex; align-items:center; gap:9px` (icon shown again) — a rotated rail only saves space in a vertical sidebar layout, not a horizontal top-strip one, so mobile keeps the original icon+upright-label pill design. `.viewer-side-nav-title` ("Sections" heading) is now `display:none` unconditionally (both breakpoints) — it didn't fit a 40px column and was dropped rather than rotated.

**Project-header pill style (2026-07-30, `index.html` only) — Backlog table + WP List table project headers now match the Schedule Summary by Project look.** Per user feedback that Schedule's row style ("a light `#f0f0f0` row + a small red project-code pill + plain-text project name + grey WP-count/rate + a grey `+`/`−` toggle badge") reads better than a full-width solid-red bar. Both the **Backlog table's two-level Project→Trade header** (`_idxBlRenderTable`'s `twoLevel` branch, project-level `pKey` row) and the **WP List's `projheader` item row** (`renderWPMonTable`) were restyled from `background:#EE3124` (solid red, white text, colspan-wide) to the pill style: `background:#f0f0f0`, a `<span>` pill (`background:#EE3124;color:#fff;padding:2px 7px;border-radius:4px`) holding just the project ID, plain-text project name (`permitted.find(...).name`, NOT the old combined "ID — Name" `_blProjLabel`/`_projLabel` strings — those two helpers are unchanged and still used by the (dead, mode-locked) single-level Backlog branch and the mobile card view respectively), and a grey `+`/`−` indicator (`background:#e0e0e0;color:#555`, was `rgba(255,255,255,.25)` on red). **Backlog's WP-count+budget text is now a single inline span** (`N WPs · ₱X.XM`, hidden budget suffix for viewers) rather than two separately-floated spans, to fit naturally after the pill+name on one line. **The WP List's project header keeps its two-`<td>` sticky/non-sticky split** (`tdSticky` colspan = frozen-column count, `tdRest` = the remainder) — this is load-bearing per Known Issues #12 (contiguous sticky-column offsets; a single full-colspan cell has nothing to stick against horizontally) and was preserved exactly, only the colors/content changed. The trade-level sub-header rows (light grey `#fafafa`/`#f8f8f8`, already close to this style) and the mobile card view's `.wpc-projgroup` chip (already light-bg/red-accent) were untouched — only the solid-red PROJECT-level desktop-table headers changed. **`project.html`'s own Backlog/WP-List project headers still use the solid-red style** — this pass was scoped to `index.html` per the request; ask if you want the per-project dashboard's tables restyled to match too.
- **"WP %" toggle added to the Schedule tab's "Work Package by Period (Planned vs Actual)" chart (`index.html`).** This chart (`#c-idx-sch-period`) previously only had **# WPs / Budget** modes (`_idxSchDataMode`, `setIdxSchDataMode`); Budget is money and already viewer-hidden (`body.viewer-mode #sch-btn-budget{display:none}`). Added a third button `#sch-btn-wppct` → mode `'wppct'`, **visible to viewers** (it's counts, not money — no new hide rule needed, it simply isn't matched by the existing Budget-only selector). No `charts.js` changes were needed: `wpCountByPeriodMonthly`/`wpCountByPeriod` already accepted an `opts.countPct` flag (added for the Overview tab's existing "# WPs %" mode, `_periodCountPct` — Planned/Actual WP counts as a % of the portfolio's total WP count, with cumulative Planned %/Actual %/Forecast % lines, same S-curve treatment as Budget %). `_renderIdxSchPeriodChart()` just gained a `_idxSchDataMode==='wppct'` branch calling the same chart functions with `{countPct:true}` instead of no-opts; `setIdxSchDataMode` now also toggles `#sch-btn-wppct`'s active state via `_segBtn`.

**Admin role restriction**: admins cannot assign `super_admin` or `specialist` roles to other users (only `super_admin` can).

**User â†’ Manager assignment**: `assigned_admin` DB column stores the manager's UUID. `admin.html` uses `WPDb.getManagerUsers()` to populate the dropdown (role=`manager` only). The "Assign to Manager" section is visible to `super_admin` only. Column header in user table shows "Manager".

**DB constraint** (run in Supabase SQL Editor):
```sql
ALTER TABLE users DROP CONSTRAINT IF EXISTS users_role_check;
ALTER TABLE users ADD CONSTRAINT users_role_check CHECK (role IN ('super_admin','admin','specialist','manager','user','viewer'));
```

Both approval modal (`modalRoleSelect`) and Change Role modal (`crm-role-select`) in `admin.html` include all 6 roles.

---

## Navigation & Sidebar

**Collapsible sidebar + topbar logo** (`initMobileMenu` in `ui.js`, central â€” applies to every page with a `.topbar`): the `.btn-menu` hamburger is now visible on **all** sizes. On desktop/tablet (â‰¥768px) it toggles `body.sidebar-collapsed` (CSS slides `.sidebar` out via `translateX(-100%)` and sets `.main { margin-left:0 }` to reclaim the space); state persisted in `localStorage.wpm_sidebar_collapsed` and restored on load with a `.no-transition` guard to avoid a slide animation on first paint. **Default is COLLAPSED (2026-08-03)** â€” `initMobileMenu` collapses the sidebar on first load unless the user has explicitly expanded it before (`localStorage.getItem('wpm_sidebar_collapsed') === '0'`); previously the default was expanded, so every new session required a hamburger click just to get the sidebar out of the way. On mobile (<768px) the hamburger still opens the slide-in drawer + overlay. `initMobileMenu` also injects the **Megawide mark** (`assets/img/megawide-mark.png`, `.topbar-logo`, ~26px) into `.topbar-left` right after the menu button (idempotent) â€” mirrors the Planning App topbar (hamburger + mark + title).

**Sidebar lists ACTIVE projects only:** both `project.html` and (since 2026-07-23) `index.html` filter `status!=='archived'` out of the sidebar project list — an archived project (TES101) is not something you jump into to manage WPs. They stay reachable from the login picker, a direct URL, and the portfolio project cards/table (where their pill starts unselected). The Overview search box's >5-project visibility test counts active projects too.

**Sidebar project search:** both `project.html` (`#sidebar-proj-search` → `renderSidebarProjects()`) and `index.html` (`#nav-proj-search` → `window.__sidebarRefresh`) filter the sidebar project list by id or name. On the Overview it appears only when the user has **more than 5 projects**, is `position:sticky` at the top of the section, and shows a "No matching projects" empty state. Pinned/unpinned ordering (`SidebarPrefs.sortProjects`) is applied to the FILTERED list.

**Sidebar scrolling — ONE scroller only:** `.sidebar` is the single scroll container (`overflow-y:auto`, thin custom scrollbar in `dashboard.css`). The project list (`#sidebar-proj-list`) previously had its own `max-height:240px;overflow-y:auto`, which rendered **two scrollbars side by side**; it now grows naturally and the search box is `position:sticky` at the top of the section so it stays reachable. Don't reintroduce an inner scroll region inside the sidebar.

**Nav context** (`sessionStorage` key `wpm_nav_ctx`): stores project ID or `'consolidated'`. Set by `project.html` and `index.html`; read by `admin.html` and `review.html` for context-aware sidebar.

### Sidebar per Page

**`project.html`**
```
Current Project â†’ [project name]
Work Packages â†’ Add Work Package | Review WPs (admin, pending badge)
Claims & Change Order Register â†’ HIDDEN (display:none)
Tools â†’ Download Template
Admin (admin only) â†’ Portfolio Overview | User Management | Pinned/Recent Projects
```

**`wp-form.html`**
```
Current Project â†’ Back to [project]
Work Packages â†’ WP Form | Review WPs (admin)
Navigation â†’ Portfolio Overview
Tools â†’ Download Template
Admin (admin only) â†’ User Management
```

**`review.html`**
```
Overview â†’ Portfolio Overview
Current Project â†’ Back to [project] (project context only)
Projects â†’ [list]
Work Packages â†’ Add Work Package | Review WPs (active)
Admin â†’ User Management
```

**`admin.html`** â€” consolidated context
```
Projects â†’ [searchable list] | New Project
Admin â†’ User Management (active) | Portfolio Overview
```

**`admin.html`** â€” project context
```
Current Project â†’ Back to [project]
Admin â†’ User Management (active) | Portfolio Overview
```

**Key rules:**
- "Add Work Package" must NOT appear in `admin.html`
- `admin.html` uses plain project links (no pin/star)
- `project.html` and `review.html` use `SidebarPrefs.projectLink()`
- `SidebarPrefs`: pins in `localStorage` key `wpm_sidebar_{userId}`; `window.__sidebarRefresh` callback re-renders after pin toggle

---

## All Work Packages Page (`review.html`) — approval workflow retired

**The approval workflow is gone (2026-07-23).** Every writing role is auto-approve (`AppAuth.isAutoApprove` = `super_admin, admin, specialist, manager, user`; only `viewer` is excluded, and viewers are redirected off this page), so **no write path can create a `pending_review` WP any more** — `wp-form.html`, the quick-add modal, CSV/Excel import and the Status Tracker all save straight to `approved`. The Pending / Approved / Rejected tabs and the Approve/Reject buttons were therefore dead controls and were **removed**, along with the reject-reason modal, `switchTab`, `currentTab` and `rejectingId`.

The page is now the **bulk view-and-edit surface for all work packages**:
- Retitled **"Work Packages"** (sub: "View and bulk-edit all work packages"); the sidebar link on `review.html`, `project.html`, `wp-form.html` and `claim-form.html` reads **"All Work Packages"** (`ti-table` icon, was "Review WPs" / `ti-clipboard-check`).
- **Grid (Excel) is the default view** (`_reviewView` starts `'grid'`; the Grid button leads the switcher, Cards is the alternate). One card list (`#panel-cards`) shows every filtered WP — no status tabs — sorted by project then WP No., with an **Edit** button that degrades to a disabled **"View only"** when `canEditProject` is false.
- **Legacy cleanup strip** (`#legacy-strip`, admins only, dismissible per session via `sessionStorage.wpm_legacy_dismissed`): if any rows still carry a non-`approved` `review_status` — only possible for WPs predating the auto-approve change — it reports the count and offers **Approve all N** (`approveLegacy()` loops `WPDb.approveWP`, busts the WP cache, reloads). Those rows also show a `PENDING (legacy)` / `REJECTED (legacy)` chip and a per-card Approve button. Once cleared, the strip never appears again.
- The grid's status dropdown labels the states honestly: **All work packages / Approved (live) / Pending review (legacy) / Rejected (legacy)**.
- `WPDb.getPendingWPs()` and the sidebar pending badges (`project.html`, `wp-form.html`) are retained — they now surface exactly these legacy leftovers and read 0 once cleaned up. `updateWP` (which resets `review_status='pending_review'`) is still called on the non-auto-approve branch of the Status Tracker and the grid save; that branch is currently unreachable but is kept so re-introducing a review-required role is a one-line change to `isAutoApprove`.

## Review Page — Excel-like Grid View (`review.html`)

`review.html` has a **Cards | Grid (Excel)** switcher (`setReviewView('cards'|'grid')`, state `_reviewView`) in the filter bar. Cards = the original review-card list (with the Pending/Approved/Rejected tab row); **Grid** = a full spreadsheet over the same `allWPs`, with its own status dropdown (`#xl-status-filter`: All / Pending / Approved / Rejected) and the tab row hidden. The Trade + Search filters drive BOTH views (`applyFilters` is wrapped to re-render the grid).

**Engine (all inline in `review.html`, no libraries):**
- **Columns are grouped by the 8 WP-form sections** (`XL_GROUPS`: Identity & Classification, Approval Matrix, Insurance Bonds, Material/Subcon Submittals, Procurement Schedule, Budget & Contract, Payment Terms, Procurement Status). Every column carries `g:'<group>'` and `XL_COLS` is ordered by section, so the grid mirrors the form top-to-bottom. The thead is **two sticky rows** — a clickable group row (`tr.xl-grp`, `top:0`) above the column row (`th.xl-colh`, `top:26px`).
  - **Collapsible sections:** clicking a group header toggles it (`toggleXlGroup(g)`); **Expand all / Collapse all** buttons sit in the toolbar. State lives in `_xlCollapsed` and persists per user in `localStorage['wpm_grid_groups_<uid>']` (`_xlLoadGroups`/`_xlSaveGroups`, loaded in `requireLogin`). `XL_GROUPS_DEFAULT_COLLAPSED = ['approval','bonds','payment']` on a first visit so the grid opens at a workable width.
  - A collapsed group keeps its `keep:true` columns (Project + WP No. in Identity — the frozen keys) or, if it has none, a **narrow read-only placeholder column** (`ph:true`, hatched) so its header stays on screen and clickable — without it a collapsed section could never be re-opened. The header shows `+N` for how many columns are hidden.
  - **`VC` (visible columns) is the coordinate space.** `_xlBuildVC()` runs at the top of every render, and **every r/c coordinate — selection, nav bounds, copy/paste, fill-down, editors, dirty repaint — indexes into `VC`, never `XL_COLS`**. Only the required-field tally (`_xlMissingCells`) and the save-time `_xlRowMissing` scan `XL_COLS`, so a collapsed section can't hide a missing required field from the count or from save gating. If you add a coordinate-based lookup, use `VC`.
- **`XL_COLS`** — 58 column defs `{g,k,label,w,type,opts,ro,req,reqIfAwarded,keep,dynOpts,users}` covering every WP-form field (identity, approval matrix, bonds, submittals, schedule, budget, payment terms, status). Two extra editor types beyond the grid's original set: **`pct`** (stored as a decimal, shown and entered as whole percent — `dp_percent`, `retention_percent`, matching the form) and **`bool`** (`requires_approval`, a Yes/No select over a real boolean).
  - **Cascading Trade → Works → Type (2026-07):** `works` and `type_of_works` are `type:'select'` with **`dynOpts:'works'`/`'type'`** — their dropdown options are computed **per row** from that row's Trade via `XL_TRADE_WORKS` (the same ladder as the WP form — keep in sync) in `_xlWorksOpts(w)`/`_xlTypeOpts(w)`, exactly like `onTradeChange`/`_setTypeOptions`. Off-list/legacy stored values are preserved (unshifted). Because dynOpts options vary per row, the header **▾ "set all"** menu falls back to a free-text input for these two (can't list one shared option set).
  - **User-directory fields (2026-07):** `responsible_team`/`approver`/`support_team` carry **`users:true`** — their editor is a text `<input>` wired to a shared `<datalist id="xl-users-dl">` of names from `WPDb.getAllUsers()` (loaded once via `_xlLoadUsers()` in `requireLogin`, stored in `_xlUsers`). Autocomplete from the real user list while still allowing multiple comma-separated names (matches the WP form's multi-select intent).
  - **Column order — Description before Cost Code (2026-07):** in the Identity group the grid lists Project, WP No., **Description, then Cost Code** (Cost Code moved right of Description). A contiguous left-freeze captures everything to the pin's left, so ordering Description first lets "freeze up to Description" stop before the often-empty Cost Code column.
- **Readability (2026-07-23):** even rows are zebra-striped (`--surface-2`, excluding frozen + missing + dirty cells so those keep their own colour), hovering a row tints it `--mw-red-light` and reddens its gutter number (`.xl-rownum`) so you can track a row across the wide horizontal scroll. The dense keyboard-shortcuts paragraph is collapsed into a **`<details class="xl-help">`** disclosure ("How to use this grid — works like Excel", `<kbd>` chips), closed by default. The required-empty tally is amber (`#B45309`), not alarm-red — it's a worklist, not a failure (save gating already blocks bad saves). **Toolbar legend polish (2026-07):** the two colour swatches (`required, empty` / `unsaved`) are grouped in a rounded `.xl-legend-box` chip with a single explanatory `title` tooltip, and a thin `.xl-sep` divider separates the legend from the Sections controls. **The `.xl-help` disclosure arrow was a corrupted glyph** — `summary::before { content:'B8' }` (a stray control char + "B8" from a bad edit) literally rendered "B8" beside "How to use this grid"; fixed to `content:'▸'` (rotates when open). **Required-column `*` placement:** the red required marker was `th.xl-req::after`, which put it AFTER the pin/caret/resize icons and wrapped it to a second line under the header; it's now an inline `<span class="xl-req-star">*</span>` rendered right after the label text inside a `.xl-hlbl` span. **Save/Discard buttons:** Save carries a `ti-device-floppy` icon + a `#xl-save-lbl` span (so `_xlUpdateBar`/`gridSave` update the LABEL, not the whole button — preserving the icon) and gets a red shadow only when there are pending edits; Discard is a quiet `.ghost` button with a `ti-arrow-back-up` icon; both sit in a divider-led `.xl-actions` group.
- **Resizable columns:** every column header carries a 7px drag grip (`.xl-rz`) at its right edge — **drag to resize, double-click to restore that column's default**, plus a **Reset widths** button in the toolbar. Overrides live in `_xlWidths{k:px}` (clamped 44–900) and persist per user in `localStorage['wpm_grid_widths_<uid>']`. **All width reads go through `_xlW(col)`** (default `col.w` unless overridden) — the colgroup, `_xlLeft()` frozen offsets and `_xlTotalW()`. The drag live-updates only the `<col>` + table width (no re-render), then re-renders on release so the frozen columns' sticky `left` offsets follow the new widths.
- **LAYOUT GOTCHA (was a live rendering bug):** `table.xl` had `table-layout:fixed` **together with `width:max-content`**. A max-content width resolves through the *auto* table algorithm, which **discards `table-layout:fixed` and every `<col>` width** — so one long `description` (the QHL706 plumbing WPs are ~600 chars) stretched its column to ~4700px, blew the table to ~10000px and pushed every other column off-screen (the user's screenshot: only Project + WP No. visible, text bleeding across the grid). **`renderGrid()` now sets an explicit pixel `width` on the table** (`_xlTotalW()` = gutter + the visible columns) and the CSS no longer sets `max-content`. Never reintroduce a content-based width on this table — with `table-layout:fixed` the width must be definite.
- **Grid prefs load lazily** via `_xlLoadPrefs()` at the top of `renderGrid` (guarded by `_xlPrefsLoaded`), NOT from the `requireLogin` callback — loading there depends on the callback firing *after* the module's `let` declarations initialise, which is true only because Supabase auth is async. The lazy load removes the ordering hazard entirely.
- **Only the ACTIVE cell gets a real `<input>`/`<select>`** (`_xlBeginEdit`, absolutely positioned inside `#xl-wrap`), so thousands of cells stay fast. Cells are plain `<td>` text otherwise.
- **Excel keyboard model:** click to select, **type any character** or **Enter/F2** to edit, **Tab/arrows** to move, **Shift+click / Shift+arrows** to range-select, **Ctrl+C / Ctrl+V** (TSV — round-trips with real Excel, multi-cell block paste from the anchor), **Ctrl+D** fill down, **Del** clear range, **Ctrl+Z** undo (batch stack `_xlUndo`), **Esc** cancel.
- **Excel mouse model (2026-07-23):** selection is driven by a document **`mousedown`** handler (replaced the old `click` handler — a click handler fires AFTER mouseup and would clobber a drag range). **Drag to select a range** (`_xlDragMode==='select'`, anchor set lazily on first move so a plain click stays a single cell); **Shift+click** still extends. **Fill handle** — a floating `#xl-fill-handle` square pinned to the bottom-right of the selection (`_xlSyncFill()`, called from `_xlSelect`/`_xlPaintRange`, repositioned on wrap scroll, hidden while editing); **drag it up/down to fill** (`_xlDragMode==='fill'`, live `.xl-fillprev` preview, `_xlFillValue` repeats the source block like Excel, commit via `_xlCommitFill` into the undo stack). **Per-column bulk set** — a header **▾** (`.xl-colset`, shown for editable non-key columns) opens `#xl-colmenu` (`_xlColMenu`): select/bool columns list their options as buttons, other types get a value input; `_xlColApply`/`_xlColApplyInput` → `_xlSetColumn(k,val)` stages the value on **every visible row** (respecting edit-scope; confirm first; undoable). Great for the dropdowns (Charging, Procurement/Submittal/Delivery Status). CSS: `#xl-fill-handle`, `.xl-fillprev`, `.xl-colset`, `#xl-colmenu`.
- **"Last Change" audit columns + "Review" column removed (2026-07-23):** the grid now has a **`Last Change`** group (`g:'audit'`) with read-only **Last Edited By** (`updated_by_name`) and **Last Edited On** (`updated_at`, `type:'datetime'` — new format in `xlFmt`) so officers can see who last touched every WP without opening the detail panel (the grid has no detail panel). The vestigial read-only **`review_status` "Review" column was removed** — the approval workflow is retired so it always read "approved"; the SHOW dropdown still filters on the underlying field for legacy cleanup.
- **BCB0 shows the CURRENT baseline:** for rows imported before per-baseline columns existed (their single budget lives in `approved_budget_bcb`, `budget_bcb0` null), the grid's **BCB0** cell falls back to `approved_budget_bcb` in `xlVal` (mirrors the dashboard's `bcbValue()` carry-forward) instead of showing blank; and **editing BCB0 also moves `approved_budget_bcb`** when no higher baseline (BCB1/BCB2) is set (in `_xlSet`), so the "current" column and all dashboard KPIs stay in step.
- **Add a WP from the grid** (`xlAddRow()`): an **Add WP** button in the toolbar creates a **draft row** (`_newRows`, id `new_N`, `__new:true`) pinned to the TOP of the grid and styled green, with a `×` in the row gutter to discard it (`xlRemoveNewRow`). Drafts ignore the Trade/Search/status/"Only incomplete" filters so a row you just added can never be filtered out from under you. The cursor lands on Description ready to type. Sensible defaults are pre-set (OSM No, Charging Main Contract, Procurement Not Started, Award Not Yet Awarded, Submittal Not Required, bonds No) so only the genuinely required fields highlight red; **WP No. auto-numbers** to `max whole number + 1` for that project counting saved AND draft rows, and stays editable. Project comes from the page context; in the **consolidated grid a project picker** (`#xl-new-proj`) appears next to the button listing only projects the user may edit (archived + DEMO excluded).
  - **Insert happens on Save, through the same path as the WP form / quick-add**: `WPDb.submitWP(payload, profile)` then `WPDb.approveWP` for auto-approve roles. `_xlDraftPayload(w)` builds the payload from `XL_COLS`, skipping `ro`/placeholder/`_`-prefixed keys so **no generated column is ever written**. Drafts must pass the FULL required-field check (unlike untouched existing rows, which only highlight), and a **duplicate WP No. guard** runs first against both saved rows and other drafts — the DB has a `(project_id, wp_no)` unique constraint. A draft that fails to insert stays staged (its edits are preserved) while successful ones are cleared.
- **Staged, not live:** every edit goes into `_gridEdits = {wpId:{field:value}}` and paints the cell amber (`.xl-dirty`); nothing hits the DB until **Save**. `gridSave()` batches one update per WP — `WPDb.updateWPDirect` for auto-approve roles, `WPDb.updateWP` (back to `pending_review`) otherwise, mirroring the WP form. `gridDiscard()` drops all staged edits; a `beforeunload` guard warns on unsaved changes.
- **Read-only cells** (`.xl-ro`): `project_id`, `review_status`, the generated `total_awarded`, the computed `_variance` (recomputed live from the staged budget/awarded), and **`award_status`** — which stays **derived from Procurement Status** (Known Issues #18): editing Procurement Status also stages `award_status`. Generated columns are stripped from the patch before the write.
- **Row-level edit scope:** cells are locked (and paste skips them, with a toast) unless `AppAuth.canEditProject(profile, w.project_id)` — so a specialist/manager/user editing the consolidated grid can only touch their assigned projects. RLS enforces the same server-side. Viewers never reach this page (redirected to `index.html`).
- **Save-time safeguard:** an Awarded WP missing an Awarded Cost or Awarded Vendor blocks the whole save with a message naming the WP Nos. — same rule as `wp-form.html`.
- **Required-field highlighting:** columns carry `req:true` (WP No., Description, Trade, Charging, Planned Award, Target Delivery, Target Completion, Budget (BCB)) or `reqIfAwarded:true` (Awarded Cost, Awarded Vendor — required only while `award_status==='Awarded'`), mirroring `wp-form.html` `save()`. Their headers get a red `*` (`.xl-req`), and any required-but-empty cell is tinted red with a red left bar (`.xl-missing`, dark-mode aware). `_xlMissing(w,col)` / `_xlRowMissing(w)` / `_xlMissingCells()` drive it; `_xlRefreshRow` re-evaluates on every edit so the Awarded-only requirements appear and disappear as Procurement Status changes. The toolbar shows a live **"N required fields empty in M WPs"** tally plus a colour legend, and an **"Only incomplete"** checkbox filters the grid to rows with gaps (the count reads `3 work packages (of 6)`). **Save gating is edit-scoped:** a row you EDITED must have all its required fields or the save is blocked with a per-WP list of the missing labels; rows you didn't touch are highlighted but never block, since many arrived incomplete from the monitoring-sheet imports. This replaced the narrower Awarded-cost/vendor-only check.
- **Derived-award gotcha (fixed):** the `procurement_status` → `award_status` derivation in `_xlSet` must compare against **`xlVal(w,'award_status')` (the current effective value), not the stored `w.award_status`** — comparing to the stored value left a stale staged `award_status` when a user flipped a WP to Awarded and back, so it stayed "Awarded" (and kept demanding a cost/vendor) despite Procurement Status reading Sourced. When the derived value returns to the stored one the override is deleted rather than re-staged.
- Sticky header + **user-configurable frozen columns** (default Project + WP No.) after a row-number gutter; offsets computed by `_xlLeft(i)` (contiguous chain — Known Issues #11/#12). Each header shows a **📌 pin** (`.xl-pin`) — click it to **freeze columns up to and including that one** (click the current boundary again to reset to default). `XL_FREEZE` is now a `let`, recomputed each render by `_xlComputeFreeze()` from **`_xlFreezeKey`** (the KEY of the last frozen column, so the freeze survives group collapse/expand — falls back to the default `XL_FREEZE_DEFAULT=2` if that key is hidden). Persisted per user in `localStorage['wpm_grid_freeze_<uid>']` (`_xlLoadFreeze`/`_xlSaveFreeze`, loaded in `_xlLoadPrefs`). `window.xlSetFreeze(key)` toggles + re-renders. Styles live in the page's `<style>` under "Excel-like grid view" (`.xl-*`, dark-mode aware).

---

## Consolidated Dashboard (index.html)

Single Supabase query: `getAllApprovedWPs()` (admin) or `getApprovedWPsForProjects(ids)` (user). **Never revert to per-project N+1 calls.**

Lazy rendering: `_rendered` flags per tab â€” charts render on first open, reset on filter change.

**Tabs** (Claims tab hidden):

| Tab | Key content |
|---|---|
| Overview | Mirrors the per-project Overview layout (**Dashboard tab removed** â€” merged here). Top row: Cost Overview **4 KPIs (BCB, Actual Award, Balance to Award [amount + WP qty remaining], Savings / Loss [â‚± + %]) â€” matches the per-project Overview** (`.ov-cost-grid` is 2-col; `card()` takes `sub`/`subColor` for the qty/% sub-line; `balanceAmt=ctc`, `savings=hm.budgetAwarded-awardedCost`) left + two WP Status cards (donut % by WP/Value toggle + stats table) right. Then **Procurement Budget (BCB) and Awarded by Period** chart (`c-idx-dash-period`, Monthly/Quarterly + Budget/# WPs toggles, `_idxDashRenderPeriod`) + **Top 5 panels** (High Value / Savings / Overbudget). **Portfolio-specific:** clickable **project cards/table** (`setProjectView`) come **last, after the Top 5**. Rendered by the `tab==='overview'` branch of `_renderTab` (the former Dashboard render body was merged in; the old status-bars/summary/dashboard-backlog statements remain but self-no-op since their DOM was removed). |
| Backlog | **Charts first** (aging chart + status donut + period chart + submittal donut), then the Backlog — Not Awarded WPs table **below** (so charts are visible on tab open). Table filter bar: Trade, Sort, **BCB min/max (`#bl-bmin`/`#bl-bmax`, ₱M → ₱, applied in `renderBlTable`)** — mirrors the per-project backlog budget filter — and Search. **The table is ALWAYS grouped two-level Project → Trade** (mirrors the WP List) — `_idxBlGroupMode` is a fixed `'project-trade'` const and the old **Flat / By Trade / By Project toggle (`#idx-bl-group-toggle` / `toggleIdxBlGroupMode`) was removed** (2026-07-30). The render draws a prominent **red Project header** (collapse key `__P__<pid>`), then **Trade subheaders** under it (key `<pid>||<trade>`, independent per project), both collapsible via `toggleBlGroup` (`_blCollapseState`), with the Project AND Trade columns hidden from rows (`showProj`/`showTrade` flags rebuild the thead + `nCols`). Project groups order by the `permitted` list; trade groups by `TRADE_ORDER`. (`renderBlTable` still contains the now-dead single-level flat/grouped branches — harmless, never reached while the mode is fixed.) The BCB inputs are hidden for viewers (`body.viewer-mode #bl-bmin/#bl-bmax`). |
| Budget | Cost KPIs + budget-by-period + budget-by-trade HBar + Budget vs Awarded by Project grouped bar + budget table by trade. **KPI cards use `helpingMetrics(wps)` — the SAME authoritative basis as the Overview tab — so BCB / Awarded Cost / CTC / EAC / SavY-Loss reconcile across tabs.** The old naive `Σ total_awarded` over ALL rows over-counted provisional costs on Not-Yet-Awarded WPs (see #17), making the Budget-tab figures disagree with the Overview. The 5th/6th cards are **Savings / Loss** (`budgetAwarded − awardedCost` = `BCB − EAC`) + **% Savings / Loss** (`savings ÷ budgetAwarded`) — renamed from "Variance"/"% Variance". The budget-by-trade table's Awarded column + TOTAL also use the strict `award_status==='Awarded' && total_awarded>0` predicate so the table reconciles with the cards (the per-trade **Variance / % Var** column headers are unchanged). |
| Schedule | Period chart (# WPs Planned vs Actual default; Budget toggle available via `_idxSchDataMode`) + WP by Trade + WP by Status + collapsible schedule summary table |
| Works | Stacked period chart + donuts by trade + collapsible BCB by Period per Scope table + BCB & Awarded by Period per Scope table |
| WP List | **Project-grouped â†’ Trade-grouped** (two-level: a prominent red Project group header above each project's Trade subheaders), 5 view tabs (Overview/Award/Schedule/Submittals/All), sortable headers, virtual pagination, slide-in detail panel |

**IDX_TRADE_ORDER** (used for Budget, Schedule, Works tables):
```js
['General Requirements','Site Works','Structural Works','Architectural Works',
 'Mechanical Works','Electrical Works','Auxiliary Works','Plumbing Works',
 'Fire Protection Works','Allied Services','Site Development Works']
```

**WP List** (`index.html`): `_WPC` column defs + `_WP_VIEWS` + `_getActiveCols()`. `renderWPMonTable()` rebuilds colgroup/thead per view and also populates `#wp-mon-cards` (mobile card view). `setWPListView(view)` switches + resets sort. `openWPDetail(w)` / `closeWPDetailModal()` â€” slide-in panel. All tab hidden for viewers.
  - An **OSM filter** (`wp-mon-osm`: All/Yes/No, filters `w.osm==='Yes'`) sits in the filter bar too. **Cascading multiselect filters (Trade â†’ Works â†’ Vendor)**: Trade is a single native `<select>` (`wp-mon-trade`, `onWpMonTradeChange()`); **Works and Vendor are custom checkbox-dropdown multiselects** (`.msf`/`.msf-btn`/`.msf-pop`, state in `_msfSel={works:Set,vendor:Set}`). `_recomputeFilterOptions()` cascades: Works options = distinct works of the trade-filtered set; Vendor options = distinct vendors (`_wpVendors(w)` splits `contractor||proposed_vendors` on `/[\r\n;|]+/` â€” newline/semicolon/pipe only, NEVER comma or space so multi-word names and "Company, Inc." stay intact; source data must use one of these delimiters) of the trade+works-filtered set. Selecting a Trade clears Works+Vendor and repopulates; selecting Works narrows the Vendor list and drops now-invalid vendor selections. `msfToggle/msfOnToggle/msfSelectAll/msfFilterOpts` drive the popovers; outside-click closes them. `filterWPTable()` reads the two Sets (empty Set = all). The custom buttons are fixed-width (`min/max-width` 150â€“190px) so the filter bar stays a **single row** regardless of project selection (replaced the old wide native selects that wrapped to 3 rows when all projects were selected).
  - **Filtered Analysis KPIs**: 4 cards â€” Work Packages, Total Budget (BCB), Total Awarded, **Total to be Awarded** (`wpmon-sum-tba`, amber â€” sum of `approved_budget_bcb` for `award_status!=='Awarded'` rows). The period chart uses `{tidyLabels:true}` (see charts.js `_dlTidy`) to declutter dense month/quarter bars.
  - **Budget / # WPs toggle** (`setWpMonDataMode('budget'|'count')`, state `_wpMonDataMode`, buttons `wpmon-btn-budget`/`wpmon-btn-count`): mirrors the Overview-tab chart's toggle. `budget` (default) → `budgetAwardedByPeriod(Monthly)` (BCB + Awarded cost, distinct-colour `_dlTidy` labels); `count` → `wpCountByPeriod(Monthly)` (Planned vs Actual WP counts). In **table** view the count mode renders a Planned/Actual-WPs count table (Planned by `awarding_date`, Actual by `actual_awarding_date` clamped to today) instead of the Budget/Awarded ₱ table. Applies to BOTH chart and table view. Same toggle added to the per-project WP List Analysis (`setPwaDataMode`, `_pwaDataMode`, `pwa-btn-budget`/`pwa-btn-count`).
  - **Overview period chart — 3-way toggle + default # WPs (2026-07):** the Overview "Procurement Budget (BCB) & Awarded by Period" chart (`c-dash-period` project / `c-idx-dash-period` portfolio) now has **# WPs / Budget / Budget %** and **defaults to # WPs** (`_dashDataMode`/`_idxDashDataMode` = `'count'`; buttons reordered). **Budget %** passes `{sharePct:true}` to `budgetAwardedByPeriodMonthly`/`budgetAwardedByPeriod`, which early-return into **`_periodSharePct(id,labels,budgetData,awardedData)`** — a plain bar chart of each period's Budget (BCB) and Awarded as a **share of the project's total budget** (bars, no cumulative lines, `%` axis/labels). `_periodSharePct` reuses the already-÷1e6 period arrays so ₱M cancels in the ratio. `sCurve(id,wps,mode)` is likewise `mode`-aware (`'wp'`|`'budget'`|`'budgetpct'` → cumulative % complete) for when the S-curve is re-added to a page. **Stable toggle buttons (2026-07):** the period-chart header title changes length per mode, which shifted the toggle buttons; the title `<span>` is now `flex:1 1 auto;min-width:0` with `text-overflow:ellipsis;white-space:nowrap` and the button group `flex-shrink:0`, so the buttons stay pinned at the panel's right edge regardless of title text (project + portfolio). All three modes' legends already share `font.size` 10 (desktop) — verified uniform.

**Top 5 rank tables (2026-07):** several ideas were tried and dropped — frozen WP-name column (covered the values), name truncation (unreadable), inner ◂▸ content-slide (user wanted the whole card to move, not the content). **Final = card-expand accordion** (`initTop5Accordion` in ui.js): clicking a card's title widens that whole `.panel` to `flex:1 1 100%` (full row width) so its table reads at a glance; the other two shrink to slim 128px title-only cards (`rank-list` hidden); click again to return to 3-up. **Row-detail vs card-expand are SEPARATE interactions (2026-07-30):** clicking a WP **row** (`.rank-list`) only opens that WP's detail panel (`openWPDetail`) — it NEVER expands/collapses the card; expand/collapse fires only from the card title/caret or empty chrome. Previously a row click in the default 3-up state both opened the panel AND enlarged the card (the guard only skipped the toggle when already expanded); the click handler now returns early for ANY `e.target.closest('.rank-list')`. **Layout:** collapsed cards group **on top** (`order:1`), GROW to share the full top row (`flex:1 1 0;min-width:0`) so the two chips stretch across the whole width with no dead space (each ~half the row), one-line title (`white-space:nowrap`, ellipsis safety net), 46px chip height and the expanded card takes its **own full-width row below** them (`order:2`, `flex:1 1 100%` — basis 100% forces the wrap so it spans the full width). (An interim `flex:1 1 0%` "master-detail" put the expanded card beside the collapsed strip; reverted per user — they wanted collapsed-on-top, expanded-below.) **Collapsed titles stay HORIZONTAL** — the first accordion rotated them vertically and a long title ("TOP 5 HIGH VALUE PACKAGES") then forced a ~530px row = a giant empty box; horizontal keeps collapsed cards ~75–90px tall. Widths set **inline `!important`** (saved/restored from `dataset.t5Flex/t5MinW`) so they beat the cards' own inline `flex:`; **no `transition:flex`** (non-interpolatable %↔px basis breaks it). Desktop only (`matchMedia('(min-width:768px)')` + `@media` guard; mobile already stacks full-width). `buildRankTable` (db.js) itself is back to plain full-name rows with `overflow-x:auto`. Called after `initExpandableCharts()` on index + project. Verified: expanded card 338px (270px table, no empty box), collapsed 75–90px, restores to 3-up.

**Budget % period chart (2026-07):** `_periodSharePct(id,labels,budgetData,awardedData,cumB,cumADisp,forecast)` renders per-period share **bars PLUS the cumulative Budget %, Cumulative Awarded % and Forecast % lines** — the same S-curve treatment as the # WPs and Budget modes, just ÷total×100. The callers (`budgetAwardedByPeriodMonthly`/`budgetAwardedByPeriod`) now compute `cumB`/`cumADisp`/`forecast` first and pass them in (the sharePct early-return moved below that). Cumulative Budget % ends at 100%; Forecast % ends at projected spend (below 100% by savings, like the ₱ mode). Labels via **`_dlTidy`** (bars <6% of the series max unlabelled) — kills the 0.0%/tiny-% clutter. The earlier trailing-empty-period trim was removed so the forecast line can extend (bars there are 0-height/invisible; _dlTidy handles labels).

**Chart panels — real fullscreen (2026-07):** `initExpandableCharts` (ui.js) now toggles the **Fullscreen API** on the `.panel` (was a 2.2× taller in-page panel, which didn't fit large displays / smart TVs). `fullscreenchange` sets the wrap to `calc(100vh - 96px)`, calls **`Charts.resize(id)`** (new — `c.resize()`+`update('none')`) to re-fit the canvas, and toggles data labels; `.panel:fullscreen` CSS paints `--surface` over the API's black backdrop. Button icon `ti-arrows-maximize`↔`ti-arrows-minimize`.
  - **Chart / Table toggle** (`setWpMonViewMode('chart'|'table')`, state `_wpMonViewMode`): Chart|Table switch beside Monthly|Quarterly. Table view (`renderWpMonPeriodTable`) shows the same per-period buckets as the chart â€” Month/Quarter, Budget (BCB), Awarded (red), Cum. Budget, Cum. Awarded + Total row â€” respecting Monthly/Quarterly mode and active filters. Bucketing mirrors charts.js (budget by planned `awarding_date`; awarded by `actual_awarding_date||awarding_date` fallback, `total_awarded>0`). `renderWPMonAnalysis()` renders chart OR table per `_wpMonViewMode`; `#wpmon-chart-wrap`/`#wpmon-table-wrap` toggled in `setWpMonViewMode`.
  - **Month-to-month (From/To) range scope + bidirectional cascade** (`wpmon-from` + `wpmon-to`, state `_wpMonFrom`/`_wpMonTo`): two dropdowns in the Filtered Analysis header restrict the **KPIs + chart + table** to WPs whose **planned award month/quarter (`awarding_date`)** falls within the inclusive `[From, To]` range (`_wpMonPeriodMatch` on `_wpMonKey(d)` — `YYYY-MM` monthly / `YYYY-Qn` quarterly, string-comparable). Either end "all" = unbounded; both "all" keeps null-date rows. **Bidirectional faceting:** `populateWpMonAnalysisFilters()` (called at the top of `renderWPMonAnalysis`) rebuilds the From/To option lists from the **Trade/Works/Vendor/Status-filtered** set (`_wpMonFiltered`) so the periods map to those filters; and `_recomputeFilterOptions()` scopes the **Trade + Works + Vendor** option lists to the selected period range (`periodOk`/`inRange`) so picking a range narrows them too (Trade value is preserved even if out-of-range so the main list isn't silently cleared; Works/Vendor invalid selections are dropped). `onWpMonAnalysisFilterChange` → `_recomputeFilterOptions()` then `filterWPTable()` (which re-renders + remaps From/To). `setWpMonPeriodMode` resets the range to "all" (keys change format) and re-renders. Scope applies ONLY to the analysis panel, not the main WP list below.
  - **Two-level grouping (Project â†’ Trade)**: rows group by `project_id` first (ordered by the `permitted` list), then by trade (ordered by `TRADE_ORDER`) within each project. The virtual `items[]` interleaves `projheader` (greater, red `#f0f0f0`/`#EE3124` header â€” `toggleProjectGroup(pid)`, state in `_projCollapseState`) â†’ `header` (trade subheader, indented, `toggleTradeGroup(key)` where **key = `${project_id}||${trade}`** so the same trade collapses independently per project, state in `_collapseState`) â†’ `row`. `collapseAllTrades`/`expandAllTrades` operate on the composite keys; expand-all also clears `_projCollapseState`. Mobile cards get a `.wpc-projgroup` header (dark-mode rule in dashboard.css "Mobile WP List cards"). The per-project dashboard (`project.html`) stays single-level trade grouping (one project only).
  - **WP No. display normalization**: `_normWpNo(v)` (index.html) / `normWpNo(v)` (project.html) strip a leading **`WP-`/`WP ` prefix** AND a trailing `.0`/`.00` from purely-numeric WP numbers at data-load — the WP No. is the **digit only** (agreed format). The Excel importer (`import-monitoring.html`) now stores just the number (was `'WP-<n>'`); existing rows are cleaned by `UPDATE_wpno_strip_prefix.sql`; the CSV-import de-dup `_wpKey` also strips the prefix so legacy `WP-5` and new `5` match (e.g. SLN101 stores `"2.0"` â†’ shown as `"2"`); real decimals like `2.1` are preserved. Applied in the `.map()` right after `normTrade` on every load/reload path. The DB still stores the original; `UPDATE_wpno_normalize_SLN101.sql` (one-off) normalizes the stored values, and the award-date UPDATE scripts match `wp_no` via `regexp_replace(wp_no,'\.0+$','')` so they work regardless.

**Mobile WP List (both pages)**: On â‰¤767px the `.wp-table-wrap` is hidden and `.wp-card-list` shown instead. Cards render from the same paged `items[]` â€” trade group header + one card per WP. Each card: WP No (red, tappable â†’ detail panel) + status badge, description, trade â€º works, budget/awarded/variance (non-viewer), target award + delivery dates, vendors, View Details + Edit. CSS classes: `.wp-card-list`, `.wp-card`, `.wp-card-*`, `.wp-table-wrap` â€” defined in each page's inline `<style>` block.

**Collapse pattern**: items[] built from trade groups (header always + rows only if expanded). `toggleTradeGroup` / `collapseAllTrades` / `expandAllTrades`. Works tab uses DOM-only toggle (`toggleWkTrade`, `toggleWkBudget`). Schedule tab uses `toggleSchRow`.

**Project filter**: `_activeIds` Set; `toggleProjectPill` calls `renderAll()` immediately. Empty Set = "No projects selected" state (red label, empty charts).

**READ-ONLY badge**: never add `display:inline-flex` as inline style â€” media query sets `display:none` and inline overrides it.

---

## Per-Project Dashboard (project.html)

Tabs: Overview → Backlog → WP List (**Dashboard tab removed** — its period chart + Top 5 panels moved into Overview)

**Header project switcher:** the topbar page title (`#page-title`) is a clickable searchable dropdown (`toggleHdrSwitcher`/`renderHdrSwitcher`, popover `#hdr-switcher` + search `#hdr-sw-search` + list `#hdr-sw-list`) that filters the user's permitted projects by id/name and links to `project.html?id=`. Reuses `window._sidebarProjects` (permitted, non-archived, non-DEMO). The title text lives in `#page-title-text` (a span) so the runtime `.textContent` update doesn't wipe the caret `#hdr-sw-caret`; the caret + click affordance are hidden when the user has ≤1 project. Mirrors the sidebar Projects search. **Styled as a visible pill button, not plain text (2026-08-03)** â€” the switcher used to be inert-looking heading text with a tiny low-opacity chevron, so users didn't realize it opened a project picker. `.proj-switcher`/`.proj-switcher-caret` (dashboard.css) give it a bordered `--surface-2` pill background and a circular caret badge that highlights red on hover/focus; markup also gained `role="button" tabindex="0"` + Enter/Space handling for keyboard access.

**Popover row redesign (2026-08-03, same day):** the dropdown list itself was a plain single-line-ellipsis row per project (generic building icon + `"ID — Name"` string truncated to one line), so on a full project name the browser's native `title` tooltip popped up on hover and visually overlapped the highlighted (active) row — read as a rendering glitch. Redesigned via new `dashboard.css` classes (`.proj-switcher-pop`, `.proj-switcher-pop-search`, `.proj-switcher-pop-list`, `.proj-switcher-item`, `.psi-pid`, `.psi-name`, `.psi-check`, `.proj-switcher-empty`) replacing the old giant inline-style blob: each row is now a small red **ID pill** (`.psi-pid`, inverts to white-bg/red-text on the active row) + the project name in a `-webkit-line-clamp:2` block (wraps instead of truncating to one line, so the native `title` tooltip is no longer needed and was dropped) + a trailing check icon (`.psi-check`) on the active row. Popover widened 300px→340px. Hover uses `--mw-red-light` (same tint used for row-hover elsewhere in the app, e.g. the Excel grid). `renderHdrSwitcher()` now escapes `p.id`/`p.name` through `esc()` before interpolating into `innerHTML` (the old code only escaped the `title` attribute, not the visible text — a real gap versus the app's Known Issues #23 "escape every `${...}` sink" rule; `renderSidebarProjects()`'s identical sidebar-list interpolation was fixed the same way). **Don't reuse `.nav-item` for this popover** — that class is tuned for the dark sidebar (near-white text/hover) and only looked right here because the row markup was overriding it with an inline `color:inherit`; the new dedicated classes are correct for a light popover surface in both themes via the standard `--surface`/`--border`/`--text-primary` var set.

**`--line` was a phantom CSS variable — never fix by reusing it (found 2026-08-03):** the original `.proj-switcher` pill (added earlier the same day) referenced `border: 1.5px solid var(--line)`, but `--line` is never defined anywhere in `dashboard.css` — only `--border` (`rgba(0,0,0,.08)`/dark `rgba(255,255,255,.09)`) and `--border-md` (`.14`/`.16`) exist. An undefined custom property with no fallback makes the property invalid at computed-value time, so `border-color` silently resolved to `currentcolor` instead of a visible gray — the pill's border was effectively invisible/wrong-tinted in both themes. Fixed by switching to `var(--border-md)` (and `var(--border)` for the popover's lighter internal dividers). **When adding a border/divider, use `--border`/`--border-md` — `--line` doesn't exist**, despite reading like a plausible token name.

- **Overview — Charging Scope switcher** (`#ov-charging-switch`, Overview tab ONLY): a 3-way segmented toggle **Combined / Main Contract / Change Order** (`setOvCharging('all'|'main'|'co')`, state `_ovCharging`, filter `ovWPs()`, `_isCO(w)` = `/change\s*order/i` on `charging_type`). A **blank `charging_type` counts as Main Contract** (the Excel importer defaults every WP to Main Contract). Everything the Overview renders re-computes from the scoped set — the 4 Cost KPIs, both battery cards, the WP-status stats table, the period chart (`renderDashPeriod`) and the Top 5 panels — because they all flow through `buildMetrics(wps)`. **Backlog / WP List / Status Tracker are NOT affected.** `_syncOvCharging()` replaces the old `buildMetrics(allWPs)` call sites (initial load + `__wpmReloadFn`): it re-applies the current scope and **hides the whole switcher when the project has no Change Orders** (resetting to Combined). Button CSS `.ov-ch-btn`/`.ov-ch-btn.active` in the page's inline `<style>`. A count hint (`#ov-ch-count`) shows `N WPs of M` when scoped.
- **Overview**: Left/right split. Left: Cost Overview 6 KPIs (BCB, Actual Award, Cost to Complete, Estimate at Completion, Variance, %Variance) via `buildMetrics(wps)`. Right: two WP Status cards â€” battery chart card (`#ov-battery-charts`, renders via `renderOvBattery(wps)` â€” two vertical CSS battery bars for % by WP and % by Value, segments: green=Awarded, red=Due Not Awarded, gray=Not Due Yet; no toggle, no Canvas) + stats table card (`#metrics-wp`). CSS: `.ov-top-row`, `.ov-cost-grid` (2-col), `.ov-wp-split`, `.ov-donut-card`, `.ov-stats-card`. Below the top row: **Procurement S-Curve** (`#c-ov-scurve`, `Charts.sCurve(id, wps)`, project view only) â€” cumulative WP count by month, Planned line from `awarding_date` (Planned Award) vs Actual line from `actual_awarding_date` of `award_status==='Awarded'` WPs; the actual line stops at the current month (no future projection). Mirrors the Excel "S-Curve" sheet (Cumulative Planned WP vs Cumulative Actual WP) and updates automatically as WPs are awarded. Rendered from `buildMetrics()`. **Date parsing (`sCurve` in charts.js) must be format-agnostic**: the internal `pd()` helper extracts the `YYYY-MM-DD` prefix via regex from ANY value (date-only string, full ISO timestamp `â€¦T00:00:00+00:00` / `â€¦Z`, space-separated timestamp, or a `Date` object) and builds a **local-midnight** Date. This fixes two bugs at once â€” (1) `new Date('2026-06-30')` is UTC midnight = June 29 evening in UTC+8, which pushed month-end comparisons into the wrong bucket; (2) a naive `split('-')` parser returned `null` for timestamp-typed columns (the live DB predates the current schema and may return timestamps), which made `valid` empty and hid the curve for EVERY project behind the `.scurve-nodata` placeholder. When there are genuinely no parseable award dates, a `.scurve-nodata` message is shown and the canvas hidden. **Call-site guard gotcha (was the real "no S-curve on any project" bug):** `buildMetrics()` in `project.html` must guard the call with `if (typeof Charts !== 'undefined' && Charts.sCurve)` â€” NOT `if (window.Charts && â€¦)`. `Charts` is a top-level `const` in charts.js, which does **not** create a `window.Charts` property, so the old `window.Charts` guard was always falsy and the S-curve was never even called (blank panel, no chart, no placeholder). **Actual-award-date fallback:** both the S-curve "Actual" line and the period charts' "Awarded"/"Cumulative Awarded" series fall back to the **planned `awarding_date`** when `actual_awarding_date` is null â€” the WP-Monitoring imports populate `award_status` + `total_awarded` (award flag from Remarks col P) but never an actual award *date*, so without the fallback every awarded series was flat at â‚±0 / had zero points. Awarded series filter on `total_awarded>0`; the month/quarter key set includes awarded WPs' fallback dates so their buckets exist. **No future awards (clamp-to-today):** an awarded WP can never be plotted in the future â€” the effective award date is **clamped to today** (`_awDate`/`_actAwDate` in charts.js; inline `awd()` in `sCurve`; clamped `awDate` in index.html `renderWpMonPeriodTable`). So when the actual date is missing and the planned `awarding_date` is in the **future**, the award is pulled back to the current day (it lands in the current period instead of a future month). Budget (BCB) bars still use the raw planned `awarding_date`, so future **budget** is shown but future **awarded** never is. Applies to `budgetAwardedByPeriod(Monthly)`, `wpCountByPeriod(Monthly)`, `wpByPeriodQuarterly`, `sCurve`, and the WP-List period table. **Cumulative Awarded line stops at today:** in `budgetAwardedByPeriod(Monthly)` the `Cumulative Awarded` series is nulled for periods after the current month/quarter (`cumADisp`, `spanGaps:false`) so the red cumulative line **ends at today** rather than running flat across the future budget months to the last planned quarter (the awarded bars are already 0 there, but the line previously extended to e.g. Q3 2028). Cumulative Budget still extends (future planned budget is legitimate).
- **Dashboard**: Period chart + **WP Status split** (donut card `c-dash-status` with `setDashStatusMode('wp'|'val')` toggle + stats table `#dash-status-table`) + WP by Trade + **Top 5 panels (now ABOVE the backlog)** + **Work Package Backlog â€” Not Awarded** table. `renderDashBacklog()` now also applies a **Trade filter** (`#dash-bl-trade`, populated once from the not-awarded set) and **Min/Max â‚±M budget filter** (`#dash-bl-bmin`/`#dash-bl-bmax`) in addition to the existing sort dropdown (single project â†’ no project grouping)
- **Backlog**: Filter bar (Trade, Sort, Search, Budget min/max) + backlog table + aging/status/period/submittal charts. `renderBacklog()` applies filters. Collapsible trade groups.
- **WP List**: filter bar = Status / **OSM (`f-osm`: All/Yes/No)** / Trade / **Works (multi)** / **Vendor (multi)** / Search. (**Zone filter removed** — to match the Portfolio WP List, which has none. The `f-zone` `<select>` was earlier dropped from the markup but JS references lingered; `document.getElementById('f-zone').innerHTML=…` in the data-load path then threw a TypeError that **aborted the rest of `loadData` → project views appeared to not load / load slowly**. All `f-zone` reads removed from `getFilteredSorted`, `_pwaFilteredWPs`, the filter-populate block, and the change-listener array.) Works + Vendor are the same `.msf` checkbox-dropdown multiselects as the portfolio (CSS now in `dashboard.css`, shared), cascading **Trade â†’ Works â†’ Vendor** via `_recomputeFilterOptions()` (state `_msfSel={works:Set,vendor:Set}`; `applyWpListFilters()` re-renders table + Filtered Analysis). Vendor options from `_projVendors(w)` (`contractor||proposed_vendors` split on `/[\r\n;|]+/` â€” newline/semicolon/pipe only, never comma/space). Applied in `getFiltered`, `getFilteredSorted`, and `_pwaFilteredWPs` (empty Set = all). **Source data must delimit multiple vendors with a newline (Alt+Enter) or `;`** â€” AVR101's legacy data is space-joined with no delimiter (can't be auto-split reliably), so re-import with a delimiter is required to get per-vendor options. 5 view tabs (Overview/Award/Schedule/Submittals/All). `WP_TABLE_VIEWS` + `getActiveCols()`. `setWPTableView(view)`. `buildTable()` renders colgroup + thead + tbody via `innerHTML` exactly like `renderWPMonTable()` in `index.html` â€” uses `_stickyLeft` dict (never mutates col objects), `renderCell` switch, `white-space:normal` on `<th>`. Trade group header splits into sticky + non-sticky cells. Also populates `#proj-wp-cards` (mobile card view â€” same paged items, CSS toggles visibility). `openWPDetail(w)` slide-in panel.
  - **WP List — user-configurable frozen columns (2026-07-31):** mirrors `review.html`'s Excel-grid pin (see its entry above). Each header in `buildTable()`'s thead carries a hover-reveal **pin** (`.wp-pin`, Tabler `ti-pin`/`ti-pinned-filled`) — click it to freeze columns up to and including that one (contiguous from the left, matching Known Issue #12); click the current boundary again to reset to the view's default (Overview: cost_code+wp_no+works; other views: cost_code+wp_no+description). State is **`_wpFreezeKey`** (the KEY of the last frozen column, `null`=default), applied as an override pass at the end of `getActiveCols()` (`cols.map((c,idx)=>({...c, sticky: idx<=i}))`) — falls back to the view's default sticky set if the pinned key isn't present in the currently active view's columns (e.g. pinned in Overview then switched to Schedule). Persisted per user in `localStorage['wpm_wplist_freeze_<uid>']` (`_wpLoadFreeze`/`_wpSaveFreeze`, loaded in `requireLogin` right after `userProfile` is set). `window.wpSetFreeze(key)` toggles + calls `buildTable()`. Scoped to `project.html` only — `index.html`'s WP List (`renderWPMonTable`) still uses its hardcoded sticky set; ask if you want it added there too.
    - **Header/body desync fix (2026-07-31, same day):** the first cut of this feature hardcoded sticky positioning inline into only 4 of ~35 `_wpRenderCell` case branches (`cost_code`/`wp_no`/`works`/`description` — the only columns the OLD fixed sticky set ever used). Freezing any OTHER column marked it `.sticky=true` in `getActiveCols()`'s override (correctly, for the thead + `_stickyLeft` offset math) but that column's body `<td>` still fell through to a case with no sticky styling at all — the header floated in place while the body cell scrolled normally underneath, which is what read as "table contents are clashing". Fixed by making `_wpRenderCell` apply stickiness **generically** via a `td(inner, extraStyle, extraAttrs)` helper wrapping every case (same principle as `review.html`'s per-index `fz` check — sticky-ness is a property of the COLUMN POSITION, never opted into per case), and moving `position`/`background`/`z-index` out of inline styles into a **`.wp-freeze`** CSS class (`background:var(--surface)`/`var(--surface-2)`, dark-mode-safe by construction — the class replaces the old hardcoded `background:#fff` inline, one instance of the "reserialised inline style" dark-mode trap called out elsewhere in Known Issues). Only the per-column `left:Xpx` offset stays inline (genuinely per-render data). The thead `<th>` sticky styling was updated the same way. **When adding a new WP-List column, don't hand-roll sticky support for it — it's automatic now; just make sure any case you write returns via the `td(...)` helper, not a raw `` `<td>...</td>` `` string.**
    - **Freeze semantics settled on "pin = the ONE frozen column" (2026-07-31, iteration 2 of 3):** the first cut copied `review.html`'s literal "freeze everything up to index i", which blanket-froze most of the table for a distant pin (that grid rarely shows more than 1-2 columns ahead of a pin, since most WP-form-section groups start collapsed — the WP List has no such collapsing). A second cut tried "default sticky columns + reorder the pin next to them," but explicit user feedback ruled that out too: pinning **Work Package Description** still left Cost Code No. and WP No. frozen, which wasn't wanted. That iteration made the pinned column the ONLY sticky column, moved to index 0.
    - **Extended to MULTIPLE simultaneous pins (2026-07-31, iteration 3, per user request):** the single-column model above couldn't do what the user actually wanted next — Description **and** Procurement Status frozen at the same time; pinning the second column just replaced the first. `_wpFreezeKey` (one key or `null`) became **`_wpFreezeKeys`** (a `Set`), persisted comma-joined in the same `localStorage['wpm_wplist_freeze_<uid>']` key. **`window.wpToggleFreeze(key)`** replaces `wpSetFreeze` — it adds/removes that one key from the set (doesn't replace the whole set), then `buildTable()`. `getActiveCols()`'s override now pulls out every column whose key is in `_wpFreezeKeys` and moves ALL of them to the front — **in their original relative column order, not click order** (so pinning Procurement Status then Description still renders Description first, since it comes first in `COLS`) — a sticky column still has to be part of the single leftmost contiguous run to render correctly (Known Issue #12), which is why scattered pins can't stay in their natural positions. Empty set ⇒ view's default sticky set (unchanged); non-empty set ⇒ ONLY the pinned columns are frozen, defaults dropped (same "no unrelated columns stay frozen" rule as iteration 2). **The pin's "on"/filled state now reflects an EXPLICIT entry in `_wpFreezeKeys`, not mere `c.sticky`** — a column that's sticky only because it's part of the default set shows the outline (unpinned) icon, so a default-sticky column doesn't look "already pinned" when it isn't yet a deliberate choice. Verified the multi-key reorder with a standalone simulation (pinning in either click order produces the same column order) before shipping.
  - **WP List restructure (2026-07-23):** the standalone **Filtered Analysis** panel and the separate **Status Tracker** tile were REMOVED from the project WP List — status is now edited **inline in the table's Overview view** (Procurement Status and Delivery Status are `_stSelect` dropdowns, Award Status is the derived `_stAwardBadge` refreshed in place via `_stRefreshAwardBadge`; `delivery_status` was added to `COLS` + the overview/all view keys). `updateWpStatus`→`_stSaveFields` preserves the `.wp-table-wrap` scroll position across the inline-edit re-render. `renderPwaAnalysis`/`renderStatusTracker` are now guarded no-ops (their panels are gone). The Filtered Analysis stays on the **Portfolio** WP List (`index.html`), but was **moved BELOW the table**. The `#st-award-modal` (cost+vendor prompt on →Awarded) is unaffected.
  - **`#wplist-edit-hint` wording clarified (2026-07-31):** was *"Set Procurement & Delivery status inline in the Overview columns — Award Status follows Procurement."* — "Overview columns" was ambiguous (it means the WP List's own **Overview** sub-tab, one of the 5 view tabs: Overview/Award/Schedule/Submittals/All — NOT the Overview dashboard tab), and "Award Status follows Procurement" didn't say *which* Procurement value triggers it. Now: *"Switch to the **Overview** view below to set Procurement & Delivery status inline — Award Status updates automatically once Procurement Status is set to Awarded."* The hint (`project.html` line ~400) sits ABOVE `#proj-view-tabs`, so "view below" is positionally accurate.

**Work Package Status Tracker** (`#status-tracker`, between `#pwa-analysis` and the WP table): a scrollable inline-edit table letting user/manager/specialist/admin update **Award Status / Procurement Status / Delivery Status** per WP without opening the form. `renderStatusTracker()` builds it from `getFilteredSorted()` (respects the WP-List filters, trade-grouped). Each cell is a `<select>` (`_stSelect`/`_stOptsHtml`; option sets `ST_AWARD_OPTS`/`ST_PROC_OPTS`/`ST_DELIV_OPTS`; a stored value outside the set is prepended so legacy data shows pre-migration). `updateWpStatus(id,field,value,sel)` **respects the approval model** (`_stAutoApprove()` = `AppAuth.isAutoApprove({role})`): auto-approve roles (admin/super_admin/specialist/manager) → `WPDb.updateWPDirect` (live, keeps approved status, toast "Saved"); non-auto-approve `user` → `WPDb.updateWP` (sets `review_status='pending_review'`, toast "Submitted for approval") so the change goes to the manager/WP approver — same as editing via the form. Either way it updates the local `allWPs` object, busts the `wpm_wps_idx*` cache, then re-renders `renderPwaAnalysis()`+`buildTable()` (NOT the tracker, to avoid scroll jump). The header hint text (`#status-tracker-hint`) switches to reflect whether edits are live or sent for approval. Wired into `switchView('table')` and `applyWpListFilters()`. **Hidden for viewers** (display:none in the `__isViewer` block). **Award column is a READ-ONLY badge** (`_stAwardBadge`, id `st-aw-<id>`) — Award Status is derived from Procurement Status, not edited directly. Changing **Procurement Status** drives it: →`Awarded` sets award=Awarded and, if the WP lacks a cost/vendor, opens the `#st-award-modal` popup (`_stOpenAwardModal`/`stAwardConfirm`/`stAwardCancel`) to collect **Awarded Cost (Net)** + **Awarded Vendor** and commits `{procurement_status, award_status, awarded_cost, contractor}` together via `_stSaveFields`; any earlier stage sets award=Not Yet Awarded. `_stRefreshAwardBadge(id)` updates the badge after save (tracker isn't fully re-rendered, to avoid scroll jump); Cancel reverts the Procurement dropdown. **"Partially Awarded" was removed everywhere** (tracker `ST_AWARD_OPTS=['Not Yet Awarded','Awarded']`, `awardDonut`, `statusByZone`, CSV import `pAward` maps "partial"→Not Yet Awarded; existing rows migrated by `UPDATE_remove_partially_awarded.sql` — **ALL** Partially Awarded → Not Yet Awarded with the partial `awarded_cost` **cleared** so it can't be read as fully awarded or inflate savings). **Final straggler cleanup (2026-07-21):** the last inert `award_status==='Partially Awarded'` color-branches (detail-panel `sc`, WP-list cell colors, `_sc`/`_bc` status-donut helpers, the `admin.html` status color, the `project.html` `{...}` color map) were collapsed to the 2-state `Awarded ? green : red`, and the dead `partial` count was dropped from `computeStats()` (it was returned but never read). `pAward()` in `db.js` (maps legacy "partial" text → Not Yet Awarded on import) and the unrelated **Claims** `Partially Approved` status are intentionally retained.
  - **Filtered Analysis â€” Budget & Awarded by Period** (`#pwa-analysis`, ported from the index.html WP-List panel): sits above the WP table, scoped to the project's `f-status`/`f-trade`/`f-zone`/`f-search`-filtered set (`_pwaFilteredWPs()`). Same controls as the portfolio version â€” **From/To month-to-month range** (`pwa-from`/`pwa-to`, state `_pwaFrom`/`_pwaTo`, `_pwaPeriodMatch`), Monthly/Quarterly (`setPwaPeriodMode`), Chart/Table (`setPwaViewMode`) â€” plus the 4 KPI cards (Work Packages / Total Budget / Total Awarded / Total to be Awarded) and the period chart (`c-pwa-period`, `{tidyLabels:true}`) or `renderPwaPeriodTable`. From/To options come from the filtered set (`_populatePwaFilters`). `renderPwaAnalysis()` is called from `switchView('table')` and the WP-List filter/search listeners (NOT pagination). **Hidden for viewers** (`#pwa-analysis` display:none in the `__isViewer` block). All ids/functions use a `pwa`/`_pwa` prefix to avoid collision with the per-project chart code.
- Claims tab exists in HTML but hidden (`display:none`)

`_rendered = { overview, dashboard, backlog, table }` â€” reset on filter change or data reload.

---

## WP Form (wp-form.html)

### Sticky Section Navigator
The form is a single scroll (NOT tabs — tabs would hide fields from `save()`'s cross-section validation + `showErr` "scroll-to-offending-field"). A **sticky section navigator** (`#form-nav`, `.form-layout` flex wrapper around the nav + `.panel`) gives fast jumping without hiding anything. Each of the 8 `.form-section`s has an id (`sec-identity`/`sec-approval`/`sec-bonds`/`sec-submittals`/`sec-schedule`/`sec-budget`/`sec-payment`/`sec-status`) with `scroll-margin-top` for the sticky-topbar offset. Desktop: a 210px left rail sticky at `top:72px`. Mobile (≤900px): a horizontal sticky scroll-spy strip under the topbar (`top:52px`). A vanilla IntersectionObserver (`rootMargin:'-80px 0px -55% 0px'`) highlights the section nearest the top; clicking a link smooth-scrolls (a 700ms `clickLock` suppresses spy flicker mid-animation). All inline in wp-form.html — no new deps. When you ADD or REORDER a form section, add its `id` + a matching `#form-nav` link.

### Sections & Key Fields
1. **Identity & Classification**: Cost Code, Trade â†’ Works â†’ Type (cascade), WP No., Description, Scope, Project, Zone, **OSM (Yes/No, `f-osm` → `osm` column, filterable in the WP List)**, Detailed Description, Type of Service, Type of Procurement, Type of Contract, Proposed Vendors, No. of PO/JO, PO/JO Numbers
2. **Approval Matrix**: Responsible (multi-select), Approver (single select, admin/super_admin only), Support (multi-select). All from `WPDb.getAllUsers()`. Uses `u.name` for display. Values stored comma-separated in `responsible_team` / `approver` / `support_team`.
3. **Insurance Bonds**: Surety Bond, Performance Bond, Warranty Bond
4. **Material/Subcon Submittals**: Requires Approval, Type of Submittal, Name of Approver, Date of Approval. **When Requires Approval = "No" the other three fields are Not Applicable** — `onRequiresApprovalChange()` (wired to the `f-requires-approval` `onchange`) sets Type of Submittal + Name of Approver to **`N/A`** and disables them, and clears + disables Date of Approval (a date can't hold "N/A"). Switching to "Yes" re-enables all three and clears the "N/A" placeholders. Called on fresh load (new WP defaults to "No" → fields start N/A) and after edit-fill (`fill('f-requires-approval', …)` then `onRequiresApprovalChange()`) so stored values sync to the flag. Disabled fields still return their `.value` via `getVal`, so save persists `submittal_document_type='N/A'` / `approver_name='N/A'` / `approval_date=null`.
5. **Procurement Schedule**: Lead Time (â†’ `lead_time`), Awarding Date, Actual Awarding Date, Target Delivery, Actual Delivery, Target Installation, Target Completion
6. **Budget & Contract**: **Budget (BCB) (Net)** (label; â†’ `approved_budget_bcb`), **Awarded Cost (Net)** (label; â†’ `awarded_cost`), Award Status, **Awarded Vendor** (label; â†’ `contractor`), **Not to be Awarded** (checkbox â†’ `not_to_be_awarded` â€” see Known Issues #28: waives the Awarded Cost/Vendor requirement and tells the dashboard to book this WP's full budget as savings at an effective â‚±0 cost, for a WP Ops has permanently pulled from the award pipeline)
7. **Payment Terms**: Terms (Days) (**free numeric input** â€” any value, not a fixed dropdown), Down Payment % (free numeric input â€” user enters percentage e.g. 20, stored as 0.20), DP Terms, DP Amount, Date of DP Release, Payment Notes, **Retention %** (**free numeric input** â€” user enters percentage e.g. 7, stored as 0.07; edit mode multiplies by 100; was a fixed 5/10/15% dropdown), Retention Amount, Retention Period
8. **Procurement Status**: Procurement Status, Submittal Status, Delivery Status, Remarks, Charging (required: Main Contract â†’ Contract Package No. | Change Order â†’ CO Description)

### WP No. Auto-Numbering
Both add-WP surfaces pre-fill the next WP No. from the project's existing WPs — **always editable**.
- **`wp-form.html`** (new WP only, never in edit mode): `suggestWpNo()` fetches the target project's WPs via `WPDb.getAllWPs(pid)` (cached per project in `_wpNoCache`) and sets `#f-wp-no` to `floor(max whole number) + 1` (so `2.1` → next is `3`; empty project → `1`). Wired to load **and** to the `#f-project` `change` event, so switching project re-suggests. `_wpNoTouched` (set by `onWpNoInput`) locks the field once the user types their own value; **clearing the field re-enables** auto-suggest. A hint under the input (`#wp-no-hint`) says it's auto-numbered and editable.
- **`project.html` quick-add modal**: `_wqmSuggestWpNo()` computes the next number from the already-loaded `allWPs` (no extra query) on modal open and again after each save so **"Add Another" keeps counting up** — `_wqmExtra[]` holds numbers saved during the session since the `__wpmReloadFn` refresh is async.
- **Duplicate guard (both paths):** the DB has a `(project_id, wp_no)` unique constraint, so each save first checks the typed number against the project's existing WP Nos. (normalized: strip `WP-` prefix + trailing `.0`, lowercase) and shows a plain "WP No. X already exists in <PID>" error instead of a raw Postgres constraint failure. `wp-form.html` skips this in edit mode.

### Trade â†’ Works â†’ Type Cascade
**Trade dropdowns list exactly the 10 canonical trades** (see Known Issues #7) — the WP-form `#f-trade` and the project.html quick-add `#wqm-trade` were updated so **Electrical Works + Auxiliary Works are a single `Electrical and Auxiliary Works` option** (their `TRADE_WORKS` works-lists were merged, duplicates removed) and the quick-add's stale list (Civil Works / MEPF & Allied Services / General Requirement / Site Development) was replaced with the canonical 10. `TRADE_WORKS` object maps Trade â†’ array of `[works, type]`. `onTradeChange()` repopulates Works; `onWorksChange()` sets Type. The Type dropdown (`_setTypeOptions`) offers **`Materials` / `Labor`** for material works (the combined **"Materials & Labor" option was removed** per user request) and **`Service`** for service works; no value is auto-selected for material works. Legacy stored values (incl. old "Materials & Labor" records) are preserved via the `unshift` fallback.

### Approval Matrix Multi-Select
`toggleMs(field)`, `msClearAll(field)`, `getMsValues(field)`, `_setMsFromText(field, text)`, `_msPillRemove(field, uid)`. Mobile: dropdown uses `position:fixed` full-width.

### Budget Input Formatting
`f-budget` uses `type="text" inputmode="numeric"` with comma formatting on blur, stripped on focus. All reads use `.replace(/,/g,'')` before `parseFloat`.

### Unsaved Changes Guard
`formDirty` flag. `markDirty()` on input/change events. `markClean()` called on successful save. `beforeunload` fires only if `formDirty`.

### Save Validation
Required fields (in `save()`): WP No., Work Package Description, Project, Trade, Awarding Date, Target Delivery Date, Target Completion Date, Budget (BCB), Charging type. **Cost Code No. is NOT required** (label has no `*`, no validation check). **Award-status safeguards:** when Award Status = `Awarded`, both **Awarded Cost (Net)** (`f-contract`) and **Awarded Vendor** (`f-contractor`) are required. **PO/JO consistency:** `No. of PO/JO` (`f-po-jo-count`) must equal the number of entries in `PO/JO Numbers` (`f-po-jo-numbers`, split on `/[\s,;]+/`); mismatch aborts the save. **Actual Awarding Date cannot be in the future** — if `f-actual-awarding-date` parses to a date after today (local midnight), `save()` aborts with "Actual Awarding Date cannot be in the future." (a WP can't be awarded on a date that hasn't happened yet). The **CSV import** path enforces the same rules (future-date + the Awarded-cost/vendor + PO/JO safeguards): both `importWPsFromCSV()` (in `project.html` and `wp-form.html`) validate the whole batch up front and `throw` (naming the offending WP No(s)) before any DB write, so any violation aborts the entire import with no partial rows. The **quick-add modal** (`saveQuickWP()` in `project.html` — now has `wqm-contract` Awarded Cost + `wqm-contractor` Awarded Vendor inputs) enforces the Award-status safeguard too (it has no PO/JO fields, so that check is N/A there).

### Cost Overview KPIs â€” abbreviated headline figures
The 6 Cost Overview KPIs (`#metrics-cost`, `.ov-cost-grid`) show **`Fmt.moneyShort`** (auto-scales: `â‚±16.52B` / `â‚±2.40M` / full < 1M; sign dropped, callers prepend +/-) with the exact value in a `title` tooltip (`Fmt.moneyFull` â†’ `â‚±16,521,423,165`). This replaced the earlier "shrink-to-fit" approach for billion-scale portfolios. The `card()` helper in both `index.html` and `project.html` takes a 4th `full` arg for the title. `.ov-cost-grid .metric-value` stays `white-space:nowrap;overflow:hidden` and `fitMetricValues()` still runs as a harmless safety net. Scoped to `.ov-cost-grid` only.

### Forecast Diagnostic (`?diag=1`)
`project.html?id=<PID>&diag=1` runs `runForecastDiag()` after data load (non-viewers only): a dismissible panel (prepended to `#view-overview`) + `console.table` output that explains when the dashed Forecast line sits above the Cumulative Budget. The chart's red lines are **row-derived** (sum of per-WP `total_awarded` vs `approved_budget_bcb`), while the Cost-Overview "Savings" KPI is **Helping-Sheet-derived** (`HELPING_FIGURES`/`helpingMetrics`). The diag shows the row-based forecast end vs budget, the row-vs-Helping-Sheet reconciliation gap, and lists: WPs awarded over their own BCB, cost-bearing rows still flagged "Not Awarded" (`total_awarded>0` but `award_status!=='Awarded'`), and awarded rows with no BCB. Use it to find which WPs push the forecast over budget.

### Delete WP
A WP can be deleted from three places (non-viewers only): the **WP detail slide-in panel** on `index.html` and `project.html` (`deleteWPFromDetail(id, wpNo)` â€” Edit + Delete buttons in the panel footer), and **`wp-form.html` edit mode** (`#btn-delete` â†’ `deleteCurrentWP()`, shown only when `editId` is set). All confirm first, call `WPDb.deleteWP(id)`, bust the `wpm_wps_idx*` sessionStorage cache, then reload (index: `location.reload()`; project: `loadData()`; form: redirect to `project.html?id=`). Requires DELETE granted on `work_packages` (already present â€” `deleteProject` uses it).

---

## Excel Monitoring-Sheet Importer (`import-monitoring.html`)

A **self-contained** tool (open to any **approved** signed-in user — the admin-only gate was removed). **Contributors (specialist/manager/user) can now run a FULL Replace import on their assigned projects** — `MIGRATION_contributor_wp_delete.sql` widened the `wp_delete` RLS policy from super_admin-only to the SAME scope as insert/update (super_admin/admin anywhere; specialist/manager/user on `assigned` projects; viewer denied), so the delete-then-insert Replace succeeds for the projects they own (also lets them delete individual WPs on assigned projects via the detail panel). Admins/super_admins can Replace any project. The delete safety-net still aborts before insert if the delete removed nothing (no access to that project). The "open the Excel importer →" links in `wp-form.html` + the `project.html` CSV modal are labelled *"(imports to projects you're assigned to)"*. **Target-project picker is a searchable combobox** (`#proj-search` text input + `#proj-options` popover + hidden `#project` holding the id; `_projs` list, `renderProjOptions`/`selectProj`/`initProjCombo`) — filters by project id or name as you type; nothing is pre-selected so `parse()`/`doImport()` guard on an empty `#project` and prompt to pick one that reads a raw **Work-Package Monitoring `.xlsx` directly in the browser** (SheetJS via CDN) — no Python extractor, no CSV step. It **replaces** the old `gen_import.py` + generated `avr101-import.html` workflow (both git-ignored — `avr101-import.html` embeds real data and must never be committed). Loads supabase-js + SheetJS via CDN, reuses the existing localStorage session. Stores **digit-only `wp_no`** (no `WP-` prefix). **Delete-permission safety net:** after the "Replace" delete it re-counts the project's WPs and aborts before inserting if any remain (RLS restricts WP delete to `super_admin`, so a blocked delete can't silently create duplicates).

- **Header-name AUTO-DETECTION (no fixed column maps)** — the monitoring sheets differ per project (AVR, GRP, SLN, SLT, UTM, OPW each have different column positions, header rows, and even templates), so the parser locates every field by matching its **header text** instead of hardcoded indices. `detectLayout(rows)` finds the header row (the one containing `WORK PACKAGE NO.`), then `findCol(band, incl, excl)` scans a 4-row header band for each field via regex (e.g. budget = `/budget.*(bcb|vat ?ex|net)/`, awarded = `/awarded cost/`, awarding date = prefer `/^awarding date/` then `/awarding date/` excluding `/actual|baseline|revised/` — some sheets (e.g. **CCM302**) have a decoy **"BASELINE AWARDING DATE"** column (col Q, mostly blank) to the *left* of the real planned **"AWARDING DATE"** (col DR); since `findCol` returns the leftmost match, without the `baseline`/`revised` exclusion the parser picked the blank baseline column and every planned award date came in empty (fixed 2026-07), stage cols `^sourcing$`/`^rfq$`/…). This adapts to all current sheets **and future ones**. Replaced the earlier fixed `LAYOUTS` profiles. **Verified against all 5 real files** (GRP 80 / OPW 104 / SLN 87 / SLT 87 / UTM 84 WPs) — correct budgets, awarding dates, and trades.
  - **Trade is canonicalised at import** — `rowToWP` runs the tracked `curTrade` (after `mepfSplit`) through a local **`normTrade()`** (same map+keyword logic as the dashboard's) so the **stored + previewed** trade is one of the 10 official trades with proper capitalization (e.g. `Structural Materials and Consumables` / `Structural Steel` / `SITE WORKS` → `Structural Works` / `Site Works`; `Landscapes and Amenities` → `Architectural Works`). Previously the importer stored the raw group-header text and relied on the dashboard to normalize on load; now both agree. Keep this `normTrade()` in sync with the two in `index.html`/`project.html`.
  - **Hierarchical trade** (all these templates): the discipline is written on group-header rows (no WP No.) above the WPs it covers — `parse()` tracks `curTrade` when a non-WP row matches `TRADE_RE` (general requirement / site works / structural / architectural / mechanical / electrical / auxiliary / plumbing / fire protection / mepf / allied…), ignoring SUBTOTAL/GRANDTOTAL. **The trade header lives in a DIFFERENT column per template**, so `detectLayout` computes **`tradeCols` — the whole column window between WP No. and the WP-Description column** (`[wp_no+1 .. wp_name]`) and `parse()` scans ALL of them (first TRADE_RE match wins). Most sheets (AVR/GRP/SLN/SLT/UTM/CCM) put the trade in the **Description** column, but **QHL706 writes it in the "WORK PACKAGE NAME" column** (one left of Description) — with the old single-`descCol` scan its trades never updated and every WP fell back to the garbage `type_of_service` value (the reported bug, fixed 2026-07-23; QHL706 now parses 170 WPs with correct trades). Col 0 is **deliberately excluded** from the window because some sheets (e.g. CCM302) use it as a per-row helper that can carry a stray/wrong trade on a header row. The `NON-PO/JO` break scans the same `tradeCols`. `normTrade()` in the app canonicalises the result to the 10 trades. Any residual `MEPF WORKS` header is split into Mechanical / Electrical&Auxiliary / Plumbing / Fire by keyword on the description (`mepfSplit`). Falls back to the type-of-service column if no group header is found.
  - `rowToWP(r, pid, today, C, curTrade)` takes the detected column map `C` with a null-safe getter; award = `awarded_cost>0` (+ the future-date rule), reconciled with proc status.
- **Sheet + project pickers:** the workbook's sheet list (defaults to `/monitoring/i`) and a target-project dropdown (**any project**). A green **"Auto-detected layout"** readout shows the header row + key column numbers so the user can sanity-check before importing.
- **Award status comes from the sheet's own Remarks/Status column, NOT from the awarded cost** (`pAwardText()`, mirroring `pAward()` in `db.js` which the CSV path always used). Precedence: (1) actual award date in the **future** → Not Yet Awarded (advance-date rule); (2) actual award date in the past → Awarded; (3) the **Remarks flag** ("Awarded" / "Not yet Awarded"); (4) only then infer from `awarded_cost>0`. **This was a real over-count (fixed 2026-07-23):** the monitoring sheets carry a **provisional cost on work packages that are still "Not yet Awarded"**, and the old `awarded_cost>0` inference imported them as Awarded — OPW101 reported **70 awarded against the sheet's own 64** (6 rows, ₱19.3M of provisional cost, e.g. WP 19 Vertical Equipment Consumables ₱6.8M). Same pattern on **GRP101 (50→38)** and **SLN101 (43→32)**, matching Known Issues #17. The provisional cost is still imported, but the WP stays Not Yet Awarded so it can't inflate the awarded total or the savings. The preview KPI row shows a **"Provisional cost, not awarded"** tile with that count, and `proc` is reconciled back to `Evaluated` when award is not Awarded. Preview-only keys (`_future`, `_heldByFlag`) are stripped from every row before insert (the strip is now `_`-prefix-wide, not just `_future`).
- **"Awarded, no cost" preview tile:** a WP flagged Awarded in the sheet but with **no awarded cost** imports as `award_status='Awarded'`, yet the dashboard KPIs exclude it — they require `award_status==='Awarded' && total_awarded>0` so the awarded count/value/budget reconcile (Known Issues #17). That is why **OPW101 imports 64 awarded but the Overview reads 62**: WP 29 (Supply of Gravel Bedding) and WP 30 (Chemical Treatment and Soil Poisoning) carry `awarded_cost = 0`. The importer preview now shows an **"Awarded, no cost"** KPI tile and logs the offending WP Nos., so the gap is visible before importing. Fix is at source — enter the awarded cost (in the sheet or via the app) and the dashboard count matches.
- **Deploy-order safety for the BCB columns (2026-07-23):** `budget_bcb0..2` only exist after `MIGRATION_bcb_baselines.sql`. Until then Supabase rejects the whole insert with *"Could not find the 'budget_bcb0' column … in the schema cache"* — and because **Replace deletes the project's WPs first**, MSJ101 was left EMPTY (deleted 32, inserted 0). Three guards now: (1) `db.js` `_isMissingBcbCol`/`_stripBcb` retry every `submitWP`/`updateWP`/`updateWPDirect` without the columns, exactly like the audit-trail guard; (2) the importer runs **`probeColumns()` BEFORE the delete** — one throwaway row is inserted and immediately removed, and if it is rejected the import either drops the optional columns or **aborts with nothing changed**; (3) the per-batch insert still strips the columns and retries. A genuinely unknown column is NOT swallowed — it aborts. When the columns are absent the budget still imports via `approved_budget_bcb`, so only the per-baseline split is lost.
- **BCB baselines from the sheet (2026-07-23):** the importer records which baseline the **main budget column** represents — `detectLayout` reads a digit out of that column's own header (`/bcb ?\(?([0-2])\)?/`), so **QHL706's `BUDGET 1 - BCB1 (VAT EX)` maps to `budget_bcb1`** while every other file's `BUDGET (BCB) (NET)` / `BUDGET (VAT EX)` maps to `budget_bcb0`. **DANGER — the `BCB (0)` / `BCB (1)` columns in these sheets are QUANTITIES, not budgets:** they sit in the `AVE. QTY | UoM` block and their totals are nothing like the budget (CCM302 ₱2.6M vs ₱885.9M; LCR102 ₱175K vs ₱406.7M; SLT101 ₱21.5M vs ₱4.24B). A separate per-baseline money column is therefore accepted ONLY when its header is exactly `BCB (n)`, its header stack has no `qty|quantity|average|per tower|uom|%`, **and its median is ≥ 1% of the budget column's median**. Across all nine current files that guard correctly finds **no** extra money-BCB column — do not relax it. **Manual override (2026-07):** an **"Import budget into"** picker (`#bcb-level`: Auto-detect / BCB0 / BCB1 / BCB2) lets the user force the main budget column into a chosen baseline instead of the header-detected one. `parse()` reads it and sets `L.col.budget_level` (which drives `bcbLv(n)` → `budget_bcb{n}`) unless left on `auto`; changing it re-runs the preview so the choice shows before import.
- **Import Excel Sheet is a first-class action** — a green button in the `wp-form.html` bulk-import banner and a **Tools → Import Excel Sheet** item in the `project.html` / `wp-form.html` sidebars (it used to be reachable only from inside the CSV-import modal). Both carry the project through as `import-monitoring.html?project=<id>`, which the importer pre-selects in its combobox.
- **Merged-header rescue for the WP Description (LCR templates, fixed 2026-07-23):** LCR102/LCR352 use `WORK PACKAGE DESCRIPTION` as a **merged PARENT header at col 7**, whose real sub-columns sit further right (col 9 = the WP name, col 10 = the long description under a `DESCRIPTION` sub-header). `findCol` returned the parent, which is **empty on every data row**, so every WP imported with the `WP-<n>` fallback name (the user's screenshot: Description read "WP-1", "WP-2"…). `detectLayout` now measures the **fill rate of the detected name column over real data rows** and, when it is under 30%, adopts the best-filled text column in the span between WP No. and the next known field (Type of Service / Type of Procurement / Proposed Vendors / Cost Code), taking the next filled column as `detailed_description`. Verified: LCR352/LCR102 go from 0% → 88%/96% description fill (and gain detailed descriptions); CCM302/SLT101 etc. keep the column they already detected (97–98% fill), so no other template is affected. **Note the trade-scan window (`tradeCols`) widens with it** — still correct, since the group headers sit at col 7 inside the span.
- **LCR field audit (2026-07-23):** with the two fixes above, every other mapped field resolves for both LCR files — only `cost_code`, `loa` and `sourcing` come back empty and `purchase_request` sparse, and those are genuinely blank in the source (cost code is written on group-header rows, not per WP; the stage columns are unused because award status comes from Remarks).
- **Awarded-cost header variants:** most sheets head the column `AWARDED COST (NET|VAT EX)`, but the **LCR templates (LCR352 / LCR102) use just `AWARDED (VAT EX)`** — the old `/awarded cost/` regex matched nothing there, so **no awarded cost was imported at all** (fixed 2026-07-23). Detection is now `/awarded cost/` → `/^awarded\s*\((vat|net)/` → `/awarded amount|contract amount/`. The middle pattern deliberately requires the parenthesised qualifier so it can **not** match a bare `AWARDED` column, which some sheets (e.g. SLT101 col U) use as an unrelated flag/quantity helper.
- **The per-project "helping sheet" UNDER-counts awarded WPs — do not treat it as ground truth.** Its `Total number of awarded Packages` is `AGGREGATE(4,…)` over a month-bucketed `SUMIFS` that sums a **counter column (col B)** gated by actual-award date. Two failure modes seen in real files: (a) a genuinely awarded row with a **blank counter cell** is skipped — **AVR102-107** reports 15 vs the true **17** (WP 44 Wiring Devices, WP 58 BI & cPVC Pipes), and **SLT101** reports 31 vs the true **35** (WP 71, 74, 75 blank counters + WP 83 which has no actual award date); (b) the hardcoded row range (`$22:$154`, `$14:$109`) truncates later rows (the AVR101 case in Known Issues #17). The importer counts every row flagged Awarded in Remarks, which is why it legitimately reports MORE than the helping sheet. SLT101 on the app basis: budget-for-awarded ₱1,938,623,384 − awarded ₱1,831,143,321 = **savings ₱107,480,063 (+5.5%)**, vs the helping sheet's ₱99,326,457 computed over only 31 WPs.
- **Three import lookouts (per user):** (1) **advance award dates** — a WP whose actual award date is in the **future** is imported as **Not Yet Awarded** with `actual_awarding_date`/`awarded_cost` cleared (the advance date is ignored until it arrives); (2) **OSM** — `osm='No'` for every WP unless its **Remarks** match `/osm/i`; (3) **charging** — every WP `charging_type='Main Contract'`. Also reconciles the proc⇄award invariant.
- **Scope-shrink guard (`checkScope`/`renderScopeWarning`/`scopeConfirmText`, 2026-07-23):** "Replace" deletes the whole project and inserts whatever the chosen sheet holds, so importing a **narrower or older working file silently shrinks the project**. This happened on **CCM302**: a workbook named with a LATER date (`2026 07 07`) held only WP 1–68 with 31 awarded, while the stored data came from `2025 08 28` with WP 1–78 and 40 awarded — the re-import dropped 10 work packages and un-awarded eleven ₱-bearing ones (Precast ₱223M, Logistics ₱47M, Ready Mix ₱39M…). After parsing, the importer now fetches the target project's stored `wp_no/award_status/total_awarded` and compares: fewer WPs, fewer awarded, stored WP Nos. **missing from the sheet**, or stored-Awarded rows arriving **Not Yet Awarded**. Any of those renders a red banner in the preview naming the affected WP Nos., and **Replace demands an extra confirmation** listing the losses before the delete. A same-scope or growing re-import shows nothing.
- **Duplicate WP No. guard (`doImport`):** the DB has a `(project_id, wp_no)` unique constraint, so a monitoring sheet with two rows sharing a WP No. (e.g. **SLN101** had two *different* Architectural door WPs both numbered **53** — "Wooden Doors - Supply" and "PVC Doors - Supply" — a source-sheet data-entry error) previously deleted the project's WPs then failed mid-batch (`duplicate key value violates unique constraint "work_packages_project_wp_unique"`), leaving a **partial import** (e.g. only 40 of 89 rows). `doImport` now scans `_parsed` for duplicate `wp_no` **before** the delete/insert and aborts with a clear alert naming the offending numbers — nothing is changed. Fix is at source: give each row a distinct WP No. in the .xlsx, then re-parse and import.
- **Numeric-overflow-safe percents/money (`pPct`/`pMoney`, 2026-07):** the `% of DP` and `Retention %` columns map to DB `numeric(5,4)` (max ±9.9999, stored as a decimal), but source sheets sometimes carry **garbage in those cells** — **LCR352 WP 45** had `% of DP = 60` and `Retention % = 17,258,238` (an amount mis-entered as the percent). The old raw `Math.round(dpPct*10000)/10000` stored those verbatim → Postgres **`numeric field overflow`** aborted batch 3, and because Replace deletes first, LCR352 was left with only 40 of 49 rows. `pPct(v)` now mirrors the CSV rule (**`>1 ⇒ /100`**, so `60 → 0.6`) **and clamps** — anything still `|v|>9.9999` is dropped to `null` (garbage), so it can't overflow. `pMoney(v)` guards money columns the same way (drops `|v|>1e15`). Applied to `dp_percent`/`retention_percent` and every money field (`approved_budget_bcb`, `awarded_cost`, `additionals`, `budget_bcb0..2`, `dp_amount`, `retention_amount`) in `rowToWP`. Fix the source cell to keep the real value; the importer just refuses to abort on it.
- **Preview before write:** parses → shows KPI tiles (count, awarded, advance-held, OSM=Yes, **No-trade count**, total budget) + a per-WP table, THEN a **Replace** button deletes ALL of the target project's WPs and batch-inserts (20/batch) with `review_status='approved'`. Strong confirm first. `parse()` throws a clear error if the WP-No or Budget header can't be found (wrong sheet).
- Dates handled via a robust `pDate` (JS Date from `cellDates:true`, Excel serial fallback, `YYYY-MM-DD`/`MM/DD/YYYY` strings) using **local** components (no UTC off-by-one). Generated columns are never inserted (the detected map has no `total_awarded`/`variance`/`awarding_lead_time`).
- Linked from the `project.html` CSV-import modal ("open the Excel importer →").

## CSV Import (Work Packages)

`downloadCSVTemplate()` generates `WPM_Import_Template.csv`. Both `project.html` and `wp-form.html` share the same 54-column template. The two sample rows are clearly marked **EXAMPLE ROW - edit or delete before importing** in the Description and are self-consistent valid samples (one Not-Yet-Awarded minimal row; one Awarded row with cost+vendor+matching PO/JO and no future dates) so they pass the import safeguards; officers edit or delete them freely. `importWPsFromCSV()` calls `WPDb.submitWP()` per row (throws on error), then `WPDb.approveWP()` if auto-approve role.

**Import is de-duplicating (upsert by WP No.)** â€” re-importing a CSV that already contains existing WPs plus a few new ones will **UPDATE the matching rows in place and only INSERT the genuinely new ones**, so no duplicate work packages are created. Both import paths (`project.html` and `wp-form.html` `importWPsFromCSV()`) first load the project's existing WPs, build a map keyed by **normalized WP No.** (`String(wp_no).trim().replace(/\.0+$/,'').toLowerCase()` â€” so `"5"`â†”`"5.0"` match), then per row: if the key exists â†’ `WPDb.updateWPDirect(existingId, wpData)` (keeps approved status), else `WPDb.submitWP()` (+`approveWP` for auto-approve roles). The map is updated after each insert so repeated WP Nos. within the same CSV also don't duplicate. Status message reports `N new, M updated (no duplicates)`.

**`WPCsv` shared module (`db.js`, `window.WPCsv`)** â€” both pages now route import through it:
- **`WPCsv.parse(text)`** â€” RFC-4180 tokenizer. Respects quoted fields containing embedded newlines (e.g. a header like `"BUDGET (BCB)\n(Net)"`). The old `text.split('\n')` broke on these â†’ counted 705 "rows" for an 87-row file. Returns array-of-row-arrays, drops blank rows, strips BOM.
- **`WPCsv.isHeaderMode(table)`** â€” true when the header row contains a recognizable WP No. column. When true, `toWPData` maps **by normalized header name** (not position), so the existing **Work Package Monitoring** layout imports directly â€” no need to reorder columns into the native template. `HMAP` aliases cover both the native template headers and the WP-Monitoring headers; `norm()` = lowercase + strip non-alphanumerics.
- **`WPCsv.toWPData(table, pid)`** â€” header-name mapping; falls back to position-based (native 54-col / legacy <40-col) when no header match. Handles: **Excel serial-number dates** (`pDate` converts e.g. `46050` â†’ `2026-01-28` via the 1899-12-30 epoch; range 20000â€“80000); **procurement_status** from the explicit `Procurement Status` column, else the right-most marked stage column; `pPct` treats values >1 as percentages (Ã·100) and â‰¤1 as already-decimal. Arbitrary `type_of_works` and combined trades (e.g. "Electrical & Auxiliary") pass through unchanged â€” dashboards group by exact trade string.
- **Award status comes from the Remarks/AWARDED text, NOT the stage**: `award_status = pAward(AWARDED col) || pAward(REMARKS col) || (awarded_cost>0 ? Awarded : Not Yet Awarded)`. In the WP-Monitoring files the award flag lives in **Column P â€“ Remarks** ("Not yet Awarded" / "Awarded"). Procurement stage columns are NOT a reliable award signal (a WP can be flagged stage=Mob/Del yet still "Not yet Awarded"), so the old "Contract/Mob-Del â‡’ Awarded" inference was **removed**. Award Status (under Budget & Contract) is the single source of truth for whether a WP is awarded; dashboard KPIs read `award_status`.
- **`WPCsv.dataRowCount(table)`** â€” counts rows with a WP No. for the "Ready to import N" message.

> **Status option sets (updated 2026-07)** — the three WP-form status dropdowns are:
> - **Procurement Status** (`f-proc-status`): **Not Started / Sourced / Solicited / Evaluated / Awarded** (renamed from Sourcing/Solicitation/Evaluation & Negotiation).
> - **Submittal Status** (`f-submittal`): **Not Required / Submitted / Approved w/ Comments / Approved** (added "Approved w/ Comments").
> - **Delivery Status** (`f-delivery-status`): **(blank "— Not started —") / N/A / DP Paid / Accomplished Detailed Drawings / Manufactured/Produced / Delivered** (replaced Not Awarded/DP Processing/Detailed Drawings/Production/Installed; **N/A added 2026-07** for WPs with no delivery phase). Delivery has a blank default so a not-yet-awarded WP isn't forced to a delivery state. Mirrored in `project.html` `ST_DELIV_OPTS` and `review.html` `XL_DELIV`.
>
> These same sets are mirrored in: the **Status Tracker** (`project.html` `ST_PROC_OPTS`/`ST_DELIV_OPTS`) and its badge colour maps (`procStatusBadge`/`submittalBadge`/`deliveryBadge`), the **CSV import** `STAGES` map + delivery defaults in `db.js` (sourcing→Sourced, rfq/bidopen→Solicited, bidclosed→Evaluated, loa/contract/mobdel→Awarded; delivery default now blank), the **Excel importer** (`import-monitoring.html` stage→proc derivation), and both CSV-template example rows. **Award Status is DERIVED from Procurement Status** (`onProcStatusChange()`): `proc==='Awarded'` → Award Status = `Awarded`, any earlier stage → `Not Yet Awarded`. The `f-award-status` select is **disabled** (auto-set). Edit mode uses `fillSelectLegacy()` to append any stored value not in the current list as a temporary "(legacy)" option, so **old data (Sourcing/Solicitation/DP Processing/Installed/etc.) still displays** — no migration is strictly required; run a one-off `UPDATE` in Supabase only if you want stored values realigned to the new labels. When you change any of these sets, update ALL the mirrors above.

**Importing the WP-Monitoring `.xlsx` files** â€” they are NOT in importable shape (hierarchical: trade in Column A, works-subgroup rows, WP leaf rows; cost/award/schedule columns spread across cols Aâ€“EB; dates as real datetimes). A one-off Python extractor (openpyxl) flattens each monitoring sheet into the adopted CSV format: trade from **Column A** (with "MEPF" split into Mechanical/Electrical&Auxiliary/Plumbing/Fire by keyword), Works from the nearest sub-header row, award from **Remarks (Col P)**, budgets/dates/vendors by header-name lookup. Generated CSVs (87/87/80/84/104 WPs for SLN101/SLT101/GRP101/UPTM/OPW101) live next to the source files as `EPC. PMO. Import WP. <PID>. 2026 06 11.csv` and import directly via the dashboard. OPW101 uses a different sheet (`OPW WP Monitoring`) and VAT-EX cost headers â€” the extractor detects both by header name.

The native template (`downloadCSVTemplate`) remains 54-column and imports via header mode (its headers all map through `HMAP`). Legacy files (<40 cols, no header match) still parse via position fallback.

**54-column mapping (position-based) â€” WP No. at col 4 is required (row skipped if blank):**

| # | Header | DB Column | Notes |
|---|---|---|---|
| **Identity & Classification** |
| 0 | Cost Code No. | `cost_code` | |
| 1 | Trade | `trade` | |
| 2 | Works | `works` | |
| 3 | Type of Works | `type_of_works` | |
| 4 | WP No. | `wp_no` | **Required** |
| 5 | Work Package Description | `description` | |
| 6 | Detailed Description | `detailed_description` | |
| 7 | Scope of Work | `scope` | |
| 8 | Zone | `zone` | |
| 9 | Type of Service | `type_of_service` | |
| 10 | Type of Procurement | `type_of_procurement` | |
| 11 | Type of Contract | `type_of_contract` | |
| 12 | Charging Type | `charging_type` | |
| 13 | Contract Package No. | `contract_package_no` | |
| 14 | CO Description | `co_description` | |
| 15 | Proposed Vendors | `proposed_vendors` | |
| 16 | No. of PO/JO | `po_jo_count` | |
| 17 | PO/JO Numbers | `po_jo_numbers` | |
| **Approval Matrix** |
| 18 | Responsible Team | `responsible_team` | |
| 19 | Approver | `approver` | |
| 20 | Support Team | `support_team` | |
| **Insurance Bonds** |
| 21 | Surety Bond (Yes/No) | `surety_bond` | |
| 22 | Performance Bond (Yes/No) | `performance_bond` | |
| 23 | Warranty Bond (Yes/No) | `warranty_bond` | |
| **Submittals** |
| 24 | Requires Submittal Approval (Yes/No) | `requires_approval` | stored as boolean |
| 25 | Submittal Document Type | `submittal_document_type` | |
| 26 | Submittals Approver Name | `approver_name` | |
| 27 | Date of Approval (MM/DD/YYYY) | `approval_date` | |
| 28 | Submittal Status | `submittal_type` | e.g. Not Required / Submitted / Approved |
| **Schedule** |
| 29 | Lead Time (Days) | `lead_time` | NOT `awarding_lead_time` (generated) |
| 30 | Planned Award Date (MM/DD/YYYY) | `awarding_date` | |
| 31 | Actual Award Date (MM/DD/YYYY) | `actual_awarding_date` | |
| 32 | Target Delivery Date (MM/DD/YYYY) | `target_delivery` | |
| 33 | Actual Delivery Date (MM/DD/YYYY) | `actual_delivery` | |
| 34 | Target Installation Date (MM/DD/YYYY) | `target_installation` | |
| 35 | Target Completion Date (MM/DD/YYYY) | `target_completion` | |
| **Budget & Contract** |
| 36 | Procurement Budget BCB (PHP) | `approved_budget_bcb` | |
| 37 | Contract Amount Awarded (PHP) | `awarded_cost` | NOT `total_awarded` (generated) |
| 38 | Award Status | `award_status` | |
| 39 | Vendor/Contractor | `contractor` | |
| **Payment Terms** |
| 40 | Payment Terms (Days) | `payment_terms_days` | |
| 41 | Down Payment % | `dp_percent` | enter as e.g. 20 â†’ stored as 0.20 |
| 42 | DP Terms | `dp_terms` | |
| 43 | DP Amount (PHP) | `dp_amount` | |
| 44 | Date of DP Release (MM/DD/YYYY) | `dp_release_date` | |
| 45 | Payment Notes | `dp_notes` | |
| 46 | Retention % | `retention_percent` | enter as e.g. 10 â†’ stored as 0.10 |
| 47 | Retention Amount (PHP) | `retention_amount` | |
| 48 | Retention Period | `retention_period` | |
| **Status** |
| 49 | Procurement Status | `procurement_status` | default: Not Started |
| 50 | Awarding Status | `awarding_status` | |
| 51 | Delivery Status | `delivery_status` | default: Not Awarded |
| 52 | Remarks | `remarks` | |
| 53 | Purchase Request | `purchase_request` | |

Dates accept MM/DD/YYYY or YYYY-MM-DD. `dp_percent` and `retention_percent` are entered as plain percentages (e.g. 20) and divided by 100 before storing.

---

## Claims & Change Orders (HIDDEN â€” not yet active)

To re-enable: remove `style="display:none"` from sidebar section + tab button in `project.html`; Claims tab button in `index.html`; restore Claims/CO cards in template picker modals.

`claim-form.html`: `?section=change-order` for CO mode, `?project=ID` pre-selects project, `?id=UUID` for edit. Claims and COs share the `claims` table distinguished by `claim_type`.

---

## CSS / Styling

**Design system**: `--mw-red: #EE3124`, `--mw-black: #231F20`, `--mw-dark: #282C28`. Font: Montserrat via `<link>` (not `@import`). Icons: Tabler Icons v2.44 (`ti ti-*`).

**Breakpoints**: â‰¥1024px desktop sidebar (240px) | â‰¤1024px tablet (220px) | â‰¤767px mobile slide-in drawer | â‰¤399px single-column.

**View tabs** (`.view-tabs` / `.view-tab`): defined globally in `dashboard.css` â€” do NOT redefine inline. On mobile: `position:sticky; top:52px`, horizontally scrollable.

**Mobile topbar** (52px): `.topbar` must have `overflow:visible` â€” `hidden` clips the user profile dropdown.

**Critical â€” sticky tabs + overflow**: Never set `overflow-x:hidden` or `overflow-x:clip` on `.main`, `.content`, `html`, or `body` on pages with sticky tabs â€” kills `position:sticky` in Safari iOS. Clamp overflow at element level:
```css
canvas { max-width:100% !important; }
.panel, .grid-2 > *, .grid-3 > * { min-width:0; }
.data-table, .budget-table { display:block; overflow-x:auto; }
```
Standalone pages (login, register, pending, forgot-password) without sticky tabs can use `overflow-x:hidden` safely.

**Logo**: styled globally in `dashboard.css` â€” do NOT add inline `.sidebar-logo img` CSS to individual HTML files.

**PWA / Add-to-Home-Screen icon**: `manifest.webmanifest` (repo root) + `assets/icons/` (generated from `../procurement app-white-logo.png` via Pillow â€” **white** Megawide mark + "PROCUREMENT APP" text on a full-bleed **red `#EE2E24`** square; the source tile has transparent corners so the canvas is filled with the sampled tile-red to avoid black corners on iOS). Icons: `apple-touch-icon.png` (180, iOS), `icon-192/512.png` (`purpose:any`), `icon-192/512-maskable.png` (18% padding for Android adaptive safe-zone), `favicon-32.png`. **Every** `*.html` `<head>` carries the same block (`rel=manifest`, `apple-touch-icon`, favicon, `apple-mobile-web-app-*` + `mobile-web-app-capable` metas, title "Procurement") â€” inserted idempotently (skip if `apple-touch-icon` already present). All paths are relative (pages + assets are at the `/prc-app/` root). To regenerate icons: re-run the Pillow snippet (sample tile-red bg, center, pad 4% regular / 14% maskable).

**One visual language (2026-07-23).** A second, Tailwind-ish blue palette (`#EFF6FF` / `#BFDBFE` / `#1e40af` / `#2563EB` / `#dbeafe`) ran through the WP form, importer, claim form and info panels alongside the Megawide red/black — **75 occurrences**. All are now mapped onto brand-derived tints: info surface → `#FDECEA` (`--mw-red-light`), chip surface → `#FBD9D5`, border → `#F5B8B3`, ink → `#C42127` (`--mw-red-dark`), action fill → `--mw-red`. The matching **dark-mode** panel surfaces moved from blue-grey (`#1B2533`/`#2A3F5F`) to red-tinted (`#33211F`/`#5A2E2A`) so both themes tell the same story. Contrast verified: 5.11:1 (light panel), 7.91:1 (dark panel), 6.23:1 (chip — its ink was darkened to `#9B1B1B` because `#C42127` landed at 4.45:1, just under AA). `onboarding.html`'s role-tier accents keep their own hues deliberately — they encode admin/contributor/viewer, not brand.

**Root font-size respects the browser preference (2026-07-23).** `html { font-size: 14px }` hard-overrode a user's chosen default, silently shrinking the UI for anyone who had set a larger one. It is now **`87.5%`** — identical at the 16px default, but it scales with the preference — and `body` pins `font-size:14px` so nothing shifted. **Font-sizes are now in `rem`** (2026-07-23): all **1,022 `font-size:Npx`** across `dashboard.css`, the 8 app pages and `db.js`/`ui.js`/`charts.js` were converted to rem at **1rem = 14px** (px ÷ 14, since the root is 87.5% of the 16px default). Pixel-identical at the default; text now scales with the user's browser font-size preference. **Only font-size was converted** — structural px (widths, positioning, borders, radii, shadows, and every JS-coupled dimension: grid sticky-column offsets, colgroup widths, chart canvas sizes) stay px on purpose. Chart.js numeric `font:{size:N}` configs are canvas, not CSS, and were untouched. Standalone pages with their own root (login/register/importer/onboarding) keep px. When you author a NEW font-size, use rem (`px/14`) on app pages.

**Topbar project count (2026-07-23).** `index.html`'s subtitle counted archived projects while the filter beside it said "N of M active" — the two disagreed (16 vs 15). It now reads `N active project(s)` and appends ` (+K archived)` only when archived projects exist.

**Errors use `AppNotify`, never `alert()` (2026-07-23).** The app raised **28 blocking `alert()`s**, 7 of which dumped a raw Postgres message at the user. `window.AppNotify` (end of `ui.js`) renders a dismissible toast top-right: `.error(msg,{title,detail})` (stays until dismissed, `role="alert"`), `.warn/.success/.info` (auto-dismiss). **`AppNotify.fromError(err, headline)`** maps common DB failures to plain language (duplicate key → "That value already exists", `42501`/RLS → "You don't have permission", missing column → "a migration is pending", network/timeout) and tucks the raw text behind a **"Show details"** disclosure so nothing is lost for bug reports. Styles `.ntf*` in `dashboard.css`. **Don't add new `alert()` calls** — all five app pages are now at zero. `confirm()` is retained for destructive actions (delete/replace), which genuinely need to block.

**Type scale (2026-07-23).** All **50 uses of `font-size:9px`** (uppercase micro-labels — the least legible combination) were bumped to 10px across the app. 9px should not reappear; 10px is the floor.

**Extracted components (2026-07-23).** Six repeated inline-style patterns became classes in `dashboard.css`, removing **94 inline declarations**: **`.seg-btn`/`.seg-btn.active`** (the Monthly|Quarterly, Budget|# WPs, Chart|Table toggles — 32 buttons), `.field-label`, `.sec-label`, `.hint-sm`, `.input-std`, `.empty-cell`. **The `.seg-btn` extraction also replaced ~15 JS setters that rewrote `el.style.cssText` on every toggle** with a shared `_segBtn(el, isActive)` that swaps a class and sets `aria-pressed`. That was the root of the "reserialised inline style escapes the dark-mode attribute selector" bug class (Known Issues, dark-mode section) — an inactive toggle now correctly follows dark mode (`--surface`) instead of staying hard-coded `#fff`. **Prefer these classes over new inline styles.**

**Status is never colour-alone (2026-07-23).** Award/procurement/submittal/delivery states were distinguished only by red vs green — invisible to ~8% of men and to a greyscale PDF print. **`window.statusLabel(text)` / `statusGlyph(text)` in `db.js`** prefix a glyph: **✔** done (Awarded/Delivered/Approved) · **◐** in progress (Sourced/Solicited/Evaluated/Submitted/Manufactured/DP Paid) · **○** open (Not Yet Awarded/Not Started/Not Required) · **⚠** attention (Overdue/Due-Not-Awarded/Rejected) · **⏱** pending. The glyph is wrapped in `aria-hidden` so screen readers read only the word. Wired into `procStatusBadge`/`submittalBadge`/`deliveryBadge`/`_stAwardBadge` (project.html) and every `award_status` cell + detail panel across `index.html`/`project.html`/`review.html`. The **battery "Due Not Awarded" segment is also striped** (`repeating-linear-gradient`) so it differs from Awarded by texture, and the legend carries the glyphs. **When you add a status badge, run its text through `statusLabel()`.**

**Tooltips — `data-tip`, not bare `title` (2026-07-23).** `title` never appears on touch, isn't keyboard reachable and is announced inconsistently. The `Tooltip` IIFE at the end of `ui.js` shows a real popover for any element with **`data-tip`** on click/tap, or Enter/Space when focused; Esc, blur, outside-click, scroll and resize all dismiss it. `kpiLabelAttrs()` now emits `data-tip` + `title` (desktop hover fallback) + `aria-label` + `tabindex=0` + `class="has-tip"`, and `.has-tip` gets a dotted underline so users can SEE that help exists. Styles: `.app-tip`, `.has-tip` in `dashboard.css`.

**Skeleton loading (2026-07-23).** The Supabase free tier cold-starts for 5–30s, during which every page showed only a spinner and read as broken. `#loading` on `index.html`/`project.html` now renders a skeleton of the real layout (4 KPI cards → chart block → table lines) and `review.html` a table-shaped one, each with `role="status" aria-live="polite" aria-busy="true"` and the spinner reduced to a small inline marker beside the text. Classes `.skel`, `.skel-card`, `.skel-line`, `.skel-grid`, `.skel-label` in `dashboard.css`; the shimmer is disabled under `prefers-reduced-motion`.

**Accessibility baseline (2026-07-23 UI audit)****Accessibility baseline (2026-07-23 UI audit)** — added at the end of `dashboard.css` under "ACCESSIBILITY & POLISH PASS":
- **Keyboard focus.** The app had **54 `outline:none` declarations and zero focus styles** — completely unusable by keyboard. A `:focus-visible` ring (2px `--mw-red`, offset 2px) now applies to every interactive element; it needs **`!important`** to beat the inline `outline:none`s (verified: an element with inline `outline:none` computes `solid` once the rule applies). The **dark sidebar gets a white ring** instead (red on `--mw-black` is too low-contrast). `:focus-visible` means mouse users see no change.
- **Skip link** (`.skip-link`, first tab stop on all 5 app pages) jumps past the sidebar to `#main-content-region`, which carries `role="main"`.
- **Contrast.** `--text-hint` was `#9B9999` = **2.83:1 (fails AA)**; now `#6E6C6C` = 5.22:1. The inline greys `#aaa` (106 uses), `#bbb` (41) and `#999` (25) are remapped to the same value **scoped to `.content`** — the dark sidebar is deliberately excluded so its greys stay light. Dark mode maps them to `#9D9B9B` (6.03:1 on `--surface`). `#888` (221 uses) is 3.54:1 — acceptable for the 10-11px bold labels it's mostly used on, left alone to avoid a sweeping visual change.
- **`prefers-reduced-motion`** disables animations/transitions (none existed before).
- **Touch targets**: `min-height:32px` on `.action-btn`/`.nav-item`/`.tab`/`.view-tab` and the icon buttons, applied only under `(hover:none) and (pointer:coarse)` so desktop tables keep their density.
- **Consistent `:disabled`** styling (opacity .5 + `not-allowed`) — previously ~20 disabled states had no shared treatment.
- `aria-label` on the icon-only topbar buttons (refresh / export PDF / export Excel) and `aria-hidden` on their decorative icons; `.sr-only` helper available for future icon-only controls.

**iOS pinch zoom****iOS pinch zoom**: handled in `ui.js` via `touchmove` + `gesturestart` listeners. `user-scalable=no` viewport is ignored by Safari iOS 10+.

**iOS PWA pull-to-refresh**: not available in standalone mode. A `.btn-refresh` button (Tabler `ti-refresh` icon, same style as `.btn-menu`) is added to `.topbar-right` on both `index.html` and `project.html`, left of the Export PDF button. On `project.html` it calls `loadData()` in-place; on `index.html` it busts `wpm_wps_idx_*` sessionStorage cache then reloads. Shows a CSS spin animation while loading; disabled during refresh to prevent double-taps. Style defined in `dashboard.css` (`.btn-refresh`, `.spin`, `@keyframes spin`).

---

## Script Loading (Dashboard Pages)

Scripts at **bottom of `<body>`** in this order â€” do NOT move to `<head>` without `defer`:
```html
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.min.js"></script>
<script src="assets/js/auth.js"></script>
<script src="assets/js/db.js"></script>
<script src="assets/js/ui.js"></script>
<!-- index.html + project.html also load Chart.js + charts.js -->
<script>/* inline init */</script>
```

Public pages (login, register, pending, forgot-password) load UMD bundle inline and call `window.supabase.createClient()` directly â€” they do NOT load `auth.js`/`db.js`/`ui.js`.

Resource hints in `<head>`: `preconnect` for fonts.googleapis.com, fonts.gstatic.com, cdn.jsdelivr.net, cdnjs.cloudflare.com; `dns-prefetch` for Supabase URL; `preload as="script"` for all body scripts.

**Cache-busting (`?v=` query param)**: ALL five core asset includes (`auth.js`, `db.js`, `ui.js`, `charts.js`, `dashboard.css`) carry a shared `?v=YYYYMMDD<letter>` param in `index.html` and `project.html` â€” on both the `<link rel=preload>` and the `<script>`/`<link rel=stylesheet>` tags. GitHub Pages + browser caching can serve a **stale** asset for up to ~10 min after a push (symptom: a JS/CSS fix is confirmed live via `curl` but the user still sees old behavior â€” e.g. dark-mode chart bars still dark, or a `db.js` rank-table fix not applying, because the browser cached the previous file). **When you change ANY of those five files, bump the single `?v=` value in BOTH `index.html` and `project.html`** (use one replace for the old `?v=` string + ensure any newly-versioned file matches) so browsers refetch immediately. Current version: `20260803i`. (`onboarding.js` + `coachmarks.js` now share this SAME version string too, not an independent one — the earlier note about a separate `20260730a` was stale; verify with a repo-wide grep before assuming otherwise.) **The shared `auth.js`/`db.js`/`ui.js`/`dashboard.css` includes now ALSO carry `?v=` on the other app pages (`admin.html`, `review.html`, `wp-form.html`, `my-wps.html`, `claim-form.html`, `project-selector.html`)** — previously they were unversioned and served stale `db.js` for ~10 min after a deploy (a real bug: e.g. admin.html not getting a new `db.js` write-path). When you bump the version, update it on ALL these pages too (a repo-wide `sed 's/?v=OLD/?v=NEW/g'` over the `*.html` set is simplest). `charts.js` is still only on index/project.

---

## Onboarding / Guided Tour

Three deliverables share the same copy, organised by **capability tier** (not the 6 raw roles):
- **admin** = `super_admin`, `admin` · **contributor** = `specialist`, `manager`, `user` · **viewer** = `viewer`.
- `tierOf(role)` in `onboarding.js` does the mapping; content is personalised to the logged-in user's tier.

1. **In-app modal** (`assets/js/onboarding.js`, `window.Onboarding`) â€” a carousel of ~7 slides (Welcome → Two views → Reading the dashboard → Finding WPs → **your tier** → capability matrix → Tips). Self-contained: injects its own `#ob-css` styles (uses the `--surface`/`--text-*`/`--mw-*` CSS vars so it follows dark mode), builds its own DOM, keyboard nav (←/→/Esc), backdrop-click + **×** to close, progress dots. Viewer variant drops all cost content.
   - **Auto-open once per user:** `Onboarding.maybeAutoOpen(userId, role)` opens the tour ~650 ms after load unless `localStorage['wpm_onboarded_<userId>']` is set. The **"Don't show on login"** checkbox (checked by default) + finishing on the last slide set that flag.
   - **Re-open any time:** a **?** `.btn-guide` button is injected into `.topbar-right` (before `#user-bar`, idempotent) on every page that loads the script; `onclick → Onboarding.open()` reads `window.__wpmRole` at open time. A **Restart tour** item in the profile dropdown (`renderUserBar` in `db.js`, above Sign out) calls `Onboarding.open()` when available, else navigates to `onboarding.html` (fallback for pages that don't load onboarding.js). The **login page** also links to `onboarding.html` ("View the Training Guide", opens new tab) for pre-login training access.

**Coach-mark tour (`assets/js/coachmarks.js`, `window.CoachTour`):** an interactive spotlight tour that dims the screen and highlights REAL UI elements with a tooltip card (Back/Next/Done, progress, Esc/arrow keys, auto-skips missing/hidden elements). **The card is PINNED to a fixed corner (bottom-right), NOT anchored beside the moving spotlight** (2026-07-30) — the buttons stayed in one place so the user doesn't have to chase a travelling **Next** between steps; `reposition()` keeps the horizontal position constant and only flips the card vertically to the top when the spotlight would otherwise cover the bottom-right slot. Self-contained (injects `#ct-css`, dark-mode via CSS vars). `CoachTour.configure({key,steps})` where a step is `{sel,title,body,before?,pad?}` (`before()` can switch tabs to reveal the target); `start(force)`, `maybeAutoStart()` (once per user via `localStorage['wpm_<key>_<uid>']`, ~800ms delay), `available()`. **index.html** (`key:coach_portfolio`) and **project.html** (`key:coach_project`) build role-aware steps in their `requireLogin` callback (viewer/archived-only steps skipped) and call `maybeAutoStart()` — this REPLACED the slide-deck auto-open (`Onboarding.maybeAutoOpen`). The slide deck stays reachable via the topbar **?** button; the profile-menu **Restart tour** now prefers `CoachTour.start(true)` (falls back to `Onboarding.open()` / `onboarding.html`). Versioned independently: `?v=20260730a`. **`wp-form.html` also has a CoachTour** (`key:coach_wpform`, 13 steps: project → WP No. → description → **Trade→Works→Type cascade** → OSM → Approval Matrix → Submittals → Schedule → Budget → Procurement Status (drives Award) → Award Status → Charging → Save) configured at the end of its `requireLogin` callback + `maybeAutoStart()`; re-openable via a topbar **?** `#btn-form-guide` button. wp-form loads `coachmarks.js` (steps auto-skip fields hidden for the role).

**Patch notice — TEMPORARY, 2026-08-03, delete after 2026-08-07 (`assets/js/patch-notice.js`, `window.PatchNotice`):** a one-off "What's New" modal for the Jul 27–Aug 3 update (headline: the **Not to be Awarded** flag; also: WP List multi-column pin, sidebar-collapsed-by-default/project-switcher redesign). Contributor-tier only (`specialist`/`manager`/`user` — never admin/super_admin or viewer/viewer_budget), gated by a hard-coded `EXPIRES = new Date('2026-08-08T00:00:00')` that both `maybeAutoOpen()` and `open()` check — nothing can render it past that instant regardless of localStorage state. Auto-opens once per user 1.5s after login (deliberately later than onboarding.js's 650ms / coachmarks.js's 800ms, so it doesn't stack with either on a brand-new contributor's first login), remembered via `localStorage['wpm_patch_202608_seen_<uid>']`. Same self-contained pattern as `onboarding.js` (own injected CSS, `--surface`/`--text-*`/`--mw-red` vars, dark-mode automatic). Wired into `index.html` + `project.html` only (the two pages contributors actively work in) — not `admin.html`/`review.html`/`wp-form.html`. Versioned independently: `?v=20260803a`, NOT part of the 5-file shared bump. **Cleanup: once past 2026-08-07, delete `assets/js/patch-notice.js` and its `<link rel=preload>`/`<script>`/`maybeAutoOpen(...)` call on both pages** — the date gate makes it inert but it's dead weight after that.

**New-user aids (in-context help):** (1) **KPI tooltips** — `window.KPI_HELP` + `window.kpiLabelAttrs(lbl)` in `db.js` add a hover `title` to every Cost-Overview metric label (`card()` helpers in `index.html`/`project.html`). (2) **WP-form field tooltips** — small `ⓘ` info spans (native `title`) on Budget (BCB), Awarded Cost, No. of PO/JO and Charging. (3) **WP List empty state** — `_wpEmptyState()` in `project.html`: a brand-new project (no WPs) shows an **Add Work Package / Import from CSV** CTA (hidden for viewers/archived); a filtered-to-empty list shows a "no matches" hint. Used by `buildTable()` for both the table body and mobile cards.
   - **Wiring:** `index.html` + `project.html` each load `<script src="assets/js/onboarding.js?v=…">` (after ui.js/db.js) and call `Onboarding.maybeAutoOpen(profile.id, profile.role)` inside their `AppAuth.requireLogin` callback. To add to another page: include the script and call `maybeAutoOpen` (or rely on the injected **?** button for manual open). `onboarding.js` is versioned independently (`?v=20260708a`), NOT part of the 5-file shared `?v=` bump.
2. **Standalone deck** (`onboarding.html`, repo root) â€” 13-slide training deck, self-contained (Montserrat + Tabler via CDN, same as the app), keyboard/dot nav, `F` fullscreen, `P`/print CSS (one slide per page). Covers all three tiers in full (for group training). Not linked from the app.
3. **PowerPoint** (`WPM_Dashboard_Onboarding.pptx`, repo root) â€” editable pptxgenjs-generated version mirroring the deck (13 slides, Calibri, circle-motif, no icon-font dependency). Regenerate from `scratchpad/gen.js` if content changes.

**First-run checklist (`window.GettingStarted` in `onboarding.js`):** a small dismissible **"Getting started (n/4)"** card rendered into `#gs-checklist` (top of the Overview on both `index.html` and `project.html`). Four steps: **Pick a project → Add a WP → Set its status → Award it.** Progress persists per user in `localStorage['wpm_gs_<uid>']` (JSON of done-step keys); dismissed flag `wpm_gs_x_<uid>`. `GettingStarted.markStep(key)` sets a step + re-renders any visible card; `render(containerId,{projectId})`; `dismiss()`. **Steps are marked from real actions:** `pickProject` — project.html load (non-viewer, non-archived/demo); `addWP` — `saveQuickWP` (project.html) and `submitForm` new-WP (wp-form.html, which writes the same localStorage key directly via `_gsMark()` since it doesn't load onboarding.js); `setStatus` — `updateWpStatus` (Status Tracker); `awardIt` — `updateWpStatus` proc→Awarded, or a WP saved with `award_status='Awarded'`. Hidden for viewers and on the demo/archived (read-only) projects. When all 4 are done it shows a "You're all set" state until dismissed.

**Status Tracker "NEW" hint** (`project.html`): a one-time red **NEW** pill on the tracker title + a dismissible callout (`#st-new-badge`/`#st-new-callout`), shown until seen (per-user `localStorage['wpm_st_seen_<uid>']`). `_stMaybeShowNew()` (called from `renderStatusTracker`) reveals it; `dismissStNew()` (the "Got it" button) or the first actual status edit (`updateWpStatus`) retires it. Skipped for viewers and the demo.

**Role-aware landing** (`login.html` `showProjectPicker`): **viewers skip the picker and go straight to `index.html`** (Portfolio charts — they're read-only, the overview is their main value). Contributors with exactly one project still auto-open it; everyone else keeps the picker.

**Read-only DEMO sandbox project:** a project with id **`DEMO`** that everyone can open read-only to explore realistic sample data. Seeded by **`SEED_demo_project.sql`** (repo root — creates the DEMO project + 10 sample WPs across trades: 6 awarded incl. one over-budget "loss", 4 backlog incl. overdue/current/future; run in Supabase SQL Editor, idempotent). **RLS REQUIREMENT:** non-admin roles only see WPs for their *assigned* projects, so the demo needs the **`wp_select_demo`** RLS policy (in `supabase-schema.sql` + `MIGRATION_rls_hardening.sql`) or it appears empty for regular users/viewers — client-side `canAccessProject('DEMO')=true` cannot override server-side RLS. **Wiring:** `auth.js` `canAccessProject` returns `true` for `'DEMO'` (open to all roles); `login.html` surfaces DEMO as its own picker item for everyone (pulled out of the normal project list to avoid duplicates); `index.html` **excludes `DEMO` from the portfolio** `permitted` list so its sample data never skews consolidated KPIs (it's opened directly via `project.html?id=DEMO`); the **project.html sidebar project list excludes DEMO** (it stays reachable only via the login picker); `project.html` treats `id==='DEMO'` as **read-only for everyone** (`window.__demo`, sets `window.__archived=true` to reuse all read-only guards, shows a green "Demo Project — explore freely" banner instead of the archive one, and hides the archive button).

**Admin "Remove" user (COMPLETE delete):** the old soft "Revoke" (set `status='rejected'`) is now **"Remove"** — `admin.html` `revokeUser` calls `WPDb.deleteUser(id)` (strong confirm first). This now purges **BOTH** the `public.users` profile row **AND the `auth.users` login record** (Planning-App parity — previously only the profile was deleted, leaving an orphaned auth account that still occupied the person's email so they could never re-register). The auth schema isn't writable by the anon/authenticated client (no service-role key in a static front-end), so `deleteUser` calls the **`public.admin_delete_user(target_id uuid)`** `SECURITY DEFINER` RPC (**`MIGRATION_admin_delete_user.sql`**, run once in Supabase SQL Editor — owned by `postgres` which owns the auth schema). The function re-checks the caller is `admin`/`super_admin`, refuses self-deletion, then deletes both rows; `execute` is granted to `authenticated` only (revoked from `public`/`anon`). **The Supabase advisor flagging this as a SECURITY DEFINER function callable by `authenticated` is an ACCEPTED exception** (documented via `COMMENT ON FUNCTION` in the migration): it MUST be definer (writes `auth.users`, which the client can't), MUST be `authenticated`-executable and in the `public` schema (or PostgREST couldn't expose it for the admin UI), and it **self-authorizes** — `auth.uid()` returns the caller's id even inside a definer function, so a non-admin is rejected with `42501` before any row is touched (no escalation/enumeration). Don't switch it to `SECURITY INVOKER` (client can't write `auth.users`) or revoke EXECUTE (breaks the Remove button); dismiss the advisory. `deleteUser` **falls back to a profile-only delete** if the RPC isn't there yet (detects `PGRST202`/"schema cache" — safe to deploy before the migration runs). The user loses access immediately (no profile → `AppAuth.requireLogin` signs them out) and the freed email can register again. The admin KPI/badge label "Revoked" → **"Rejected"** (only denied *registrations* remain in `status='rejected'` now).

**Per-WP audit trail — "Last Change" log (2026-07-23):** now that the dashboard is cascaded to all procurement officers, every work package records **who last changed it and when**. Columns `updated_at` / `updated_by` (auth uid, no FK) / `updated_by_name` (name snapshot, survives user deletion) on `work_packages` — run **`MIGRATION_wp_audit_trail.sql`** once (adds the columns, backfills `updated_at` from `created_at`, and recreates `wp_view_public` to expose the three non-cost fields to viewers). **Stamped centrally in `db.js`**: `_auditStamp()` reads `window.__profile` and is spread into `submitWP` / `updateWP` / `updateWPDirect`, so ALL app write paths (WP form, quick-add, grid save, Status Tracker, CSV import) record it automatically; `unmap()` strips any stale read-back of the three fields first. The **Excel importer** (`import-monitoring.html`) stamps its inserted rows from the signed-in user (`_auditUser`). **Deploy-order-safe:** if the columns don't exist yet, every write path detects the `42703`/"column does not exist" error and **retries without the audit fields** (db.js `_isMissingAuditCol`/`_stripAudit`; the importer strips them from all remaining rows and retries the batch), so the code is safe to ship before the migration runs. **Displayed** in the WP detail slide-in panel (`openWPDetail` in both `index.html` and `project.html`) as a **"Last Change"** block (Updated By + Updated On, only shown when present). Read paths use `select('*')` so the fields flow through `mapWP` untouched.

**User-management approval (`admin.html`) — two fixes (2026-07-23):** (1) **Modal confirm buttons stayed disabled after the first success.** `confirmApprove`, `confirmEditUser` and `confirmRoleChange` set `btn.disabled=true` at the start but only re-enabled it in their `catch` — on success they closed the (reused, not recreated) modal without re-enabling, so `#btnConfirmApprove` / `#crm-save-btn` were permanently disabled and every subsequent approve/edit/role-change did nothing until a page refresh recreated the buttons (the "approve requires a refresh every time" bug). Fixed by resetting `btn.textContent` **and `btn.disabled=false`** in `openApproveModal`, `openProjectModal` and `openRoleModal` (i.e. every time a modal opens). If you add another reused modal with an async confirm handler, reset its button state on open, not just in the error path. (2) **Bulk approve didn't exist.** The bulk bar's `applyBulkActions` only wrote `role`/`projects` via `updateUser`, never `status:'approved'`, so selecting pending users and clicking Apply never approved them. Added `window.bulkApprove()` + a green **✓ Approve (N)** button in `#bulk-bar` (shown by `renderUsers` only when the selection contains pending users) that approves all selected pending users at once — keeps each user's existing projects, applies the chosen bulk role/project if set (role defaults to `user`), and (for `admin` callers) sets `assigned_admin` to self so they stay visible after reload. `applyBulkActions` now ALSO flips a still-pending user to `approved` when a bulk assign touches them. Both reload via `loadData()`.

**Actionable errors** (`wp-form.html` `save()`): `showErr(msg, fieldId)` now **scrolls to and focuses the offending field**, and each message says what to do (e.g. "This WP is marked Awarded — enter the Awarded Cost (Net), or set Award Status back to 'Not Yet Awarded'").

> `.claude/launch.json` (a `python -m http.server 8765` config) exists only to preview static pages locally â€” not a deploy dependency.

---

## Known Issues / Gotchas

1. **Generated columns**: `total_awarded`, `awarding_lead_time`, `variance` â€” never INSERT into them. Use `awarded_cost` and `lead_time`. `unmap()` strips all three automatically.
2. **Sticky tabs + overflow**: `overflow:hidden/clip` on any ancestor of `.view-tabs` breaks `position:sticky` in Safari. See CSS section.
3. **READ-ONLY badge**: never set `display:inline-flex` as inline style on `.topbar-badge-readonly` â€” mobile media query can't override it.
4. **N+1 query**: never use `Promise.all(projects.map(p => WPDb.getApprovedWPs(p.id)))` â€” use `getAllApprovedWPs()` or `getApprovedWPsForProjects(ids)`.
5. **Role caching**: `window.__wpmRole` set once at login. Role/project changes require the user to log out and back in.
6. **Chart.js leaks**: always `chartInstance.destroy()` before re-rendering.
7. **Trade name consistency â€” 10 canonical trades only**: `normTrade(t)` in `index.html` and `project.html` (identical) collapses EVERY trade string into one of exactly 10: General Requirements, Site Works, Structural Works, Architectural Works, Mechanical Works, **Electrical and Auxiliary Works**, Plumbing Works, Fire Protection Works, Allied Services, Site Development Works. **Electrical Works and Auxiliary Works are combined into a single bucket named "Electrical and Auxiliary Works"** (per user request â€” the combined label keeps Auxiliary visible in the trade name): `electrical works` â†’ Electrical and Auxiliary Works; `auxiliary works` â†’ Electrical and Auxiliary Works; the combined `electrical and auxiliary works` / `electrical & auxiliary works` â†’ Electrical and Auxiliary Works. Other variants: OPW101 granular arch sub-trades â†’ Architectural (incl. **`Landscapes and Amenities` / `Laminates and Plastic Finishes`** — a `/landscap|amenit/` keyword rule sits **before** the architectural rule so Landscapes doesn't remain its own non-canonical trade); **`Structural Materials and Consumables` / `Structural Steel` â†’ Structural** (via `/structural/`); Structural Labor And Services â†’ Structural; Housekeeping & Sanitation / Drawing / Security Services â†’ General Requirements; Other Allied Services â†’ Allied Services. **NON-PO/JO items are NOT trades:** OPW101's monitoring sheet has a `NON-PO/JO` section (design/consultancy, bonds, permits, insurances, project details) below the last WP — those rows have **no WP No.** so they were never imported as WPs, and the Excel importer now **`break`s the parse loop at the `NON-PO/JO` header** (`/non[\s\-]?(po|jo)/i`) so nothing beneath it can ever be imported even if a future sheet stray-numbers a row there. Keyword-regex fallback: both `/electr/` AND `/auxiliary/` â†’ Electrical and Auxiliary Works. Applied at data load time. `IDX_TRADE_ORDER`/`TRADE_ORDER` list the same 10 (drives chart colour order; the combined label sits in the Electrical slot, "Auxiliary Works" removed). Keep the explicit map + keyword fallback in sync across both files. **`project.html`'s INITIAL data-load path skipped `normTrade` (fixed 2026-07-30):** `_rawWPs` was built with `trade: w.trade` (raw) on first load — only `__wpmReloadFn` (used after quick-add saves) applied `normTrade`. Raw variants like `General Requirement` (singular) or `Other Allied Services` never matched `TRADE_ORDER`'s canonical strings, so `TRADE_ORDER.indexOf` returned -1 → those groups fell back to sort index 999 and landed at the end of the WP List (alphabetically among themselves) instead of their intended TRADE_ORDER position. Both `_rawWPs` assignments now normalize identically.
8. **Supabase cold start**: free tier pauses after 7 days â†’ 5â€“30s delay (and a long-paused project can be deleted). Mitigated by a **GitHub Action keep-alive** (`.github/workflows/keep-alive.yml`, every 3 days — needs repo secrets `SUPABASE_URL` + `SUPABASE_ANON_KEY`) plus an optional external UptimeRobot ping.
9. **Dead-code cleanup (2026-07)**: the stale **root-level duplicate** scripts/styles (`auth.js`, `db.js`, `ui.js`, `charts.js`, `dashboard.css`, `dashboard-mobile-fixes.css`, `auth-guard.js`, `firebase-auth-shim.js`, `supabase-config.js`) and the unused **`assets/js/firebase-config.js`** (stale Firebase backend) were **deleted** — no page referenced them. The **duplicate `saveProject` in db.js** was also removed (one definition remains). Canonical assets live in `assets/js/` + `assets/css/`.
10. **dp_percent**: stored as decimal (0.20 = 20%). Form input accepts percentage (user types "20"), divided by 100 before storing; edit mode multiplies by 100 to display.
11. **Sticky columns require `table-layout:fixed` + colgroup**: Both `index.html` (`renderWPMonTable`) and `project.html` (`buildTable`) use `<table style="table-layout:fixed">` + a `<colgroup>` rebuilt on every render with explicit pixel widths matching the column defs. Without this, the browser auto-sizes columns narrower than the hardcoded sticky `left` offsets â€” causing sticky cells to physically overlap adjacent columns. Always keep colgroup in sync: `cg.innerHTML = cols.map(c => \`<col style="width:${c.w}px;min-width:${c.w}px">\`).join('')`.
12. **Sticky column chain must be contiguous**: All sticky columns must be consecutive from the left with no non-sticky column in between. A non-sticky column between two sticky ones causes the right sticky column to get a wrong `left` offset. In `index.html` overview: project(0)â†’cost_code(90)â†’wp_no(180); description NOT sticky. In `project.html` overview: cost_code(0)â†’wp_no(90)â†’works(180); description NOT sticky. In non-overview tabs (both pages): description is made sticky immediately after wp_no. In the **All tab** only `wp_no` and `description` are frozen â€” description is placed right after wp_no in the column order (before works) so the chain is unbroken.
13. **Period chart mode/button mismatch**: `_idxSchPeriodMode` (Schedule), `_idxDashPeriodMode` (Dashboard), `_idxBudPeriodMode` (Budget), `_idxBlPeriodMode` (Backlog) in `index.html` must match the button that is styled active in HTML. Schedule also has `_idxSchDataMode` (`'count'` default = # WPs; `'budget'` = BCB/Awarded). `_renderIdxSchPeriodChart()` branches on both. Mismatch = chart renders wrong on first load.
    - **4-mode data toggle collapsed into two 2-way toggles (2026-07-31):** the Overview/Dashboard period chart (`project.html` + `index.html`) and the Backlog period chart (`project.html`) used to show 4 flat buttons — `# WPs / Budget / Budget % / # WPs %` — for what is really two independent choices: **metric** (WP count vs Budget ₱) and **view** (raw # vs % of total). Per user request they're now **`WP | Budget` then `# | %`**, two segmented-button pairs separated by the same `|` divider used for Monthly/Quarterly. The underlying combined mode string (`'count'|'budget'|'countpct'|'budgetpct'`) is unchanged — `_dashDataMode`/`_blDataMode`/`_idxDashDataMode` and every render function that branches on them (`renderDashPeriod`, `_blRenderPeriod`, `_idxDashRenderPeriod`) were **not touched**. Only how that string gets SET changed: `_dashMetric`/`_dashView` (project Overview), `_blMetric`/`_blView` (project Backlog), `_idxDashMetric`/`_idxDashView` (portfolio Overview) are the two toggle states, combined by a small `_xComboMode()` helper (`wp+num→count`, `wp+pct→countpct`, `budget+num→budget`, `budget+pct→budgetpct`) and applied via the existing `setXDataMode(mode)` setter. **Scoped to exactly the 3 panels that had all 4 modes** — the Portfolio Budget tab (`bud-btn-*`, Budget/# WPs only, no % modes) and Schedule tab (`sch-btn-*`, # WPs/Budget/WP % — 3 buttons, no Budget %) and the WP-List Filtered Analysis (`wpmon-btn-*`/`pwa-btn-*`, Budget/# WPs only) were left as-is; they never had the full 4-mode pattern so there was nothing to collapse. **When adding a 4th mode to one of those 3-or-fewer-button panels, apply the same WP|Budget + #|% split rather than adding a 5th flat button.**
    - **The `#|%` pair is now a SINGLE toggle button, not two (2026-07-31, same day, per user request):** `dash-btn-view`/`bl-dm-view`/`idx-dash-btn-view` replace the `-num`/`-pct` button pairs — one button whose `onclick` flips the current `_xView` state (`onclick="setDashView(_dashView==='num'?'pct':'num')"`) and whose **label IS the current state** (`#` or `%`, defaults to `#` on load matching `_dashView='num'`). `setXView(v)` no longer toggles two buttons' `active` class — it just sets `btn.textContent`. Kept permanently `class="seg-btn active"` (red) since it's a switch control, not a pick-one-of-two choice like the `WP|Budget` pair beside it.
    - **Backlog period charts pass `{hideAwarded:true}`**: the Backlog tab (both pages) feeds `budgetAwardedByPeriod`/`Monthly` only not-awarded WPs, whose Awarded/Cumulative-Awarded series are â‚±0 by definition (red line stuck flat at 0M). All four backlog call sites pass `{hideAwarded:true}` so those two series are omitted â€” only Budget bars + Cumulative Budget line render. Don't remove the opts arg. The Dashboard tab instances get ALL WPs and keep all four series.
    - **Backlog "Work Package by Period" chart (project.html) has a 4-way data-mode toggle** (`#bl-dm-count`/`#bl-dm-budget`/`#bl-dm-countpct`/`#bl-dm-budgetpct` → `setBlDataMode`, state `_blDataMode`), mirroring the Overview period chart's `# WPs / Budget / WP % / Budget %` toggle, alongside the existing Quarterly/Monthly (`_blPeriodMode`). **The JS (`_blRenderPeriod`, `setBlDataMode`) already existed fully wired** (all 4 modes) but the HTML toggle buttons were missing (2026-07-30 fix) — only Quarterly/Monthly was reachable until then.
    - **`# WPs`/`WP %` backlog modes ALSO needed `hideAwarded` (fixed 2026-07-31):** the `{hideAwarded:true}` treatment above only ever reached the two Budget-mode calls — the default `# WPs` mode (and `WP %`) called `wpCountByPeriodMonthly`/`wpCountByPeriod` with no opts at all, so the Backlog tab's default chart always drew a **"Cumulative Actual" line pinned flat at 0** (backlog = not-yet-awarded WPs only, so the actual/awarded count can never be anything but 0) plus a dashed Forecast line. Per user feedback the flat line "doesn't bring value" — `wpCountByPeriodMonthly`/`wpCountByPeriod` (and the `_periodCountPct` helper they call for `%` mode) now accept the same `opts.hideAwarded` flag as the budget charts and drop the **Actual WPs bar + Cumulative Actual line + Forecast line** entirely when set (mirrors `budgetAwardedByPeriod`'s existing `hideAwd` spread pattern). `_blRenderPeriod`'s `count`/`countpct` branches now pass `{hideAwarded:true}` alongside the existing `budget`/`budgetpct` branches — all 4 backlog modes are consistent. The Dashboard/Schedule tab call sites (full WP set, not backlog-filtered) are untouched and keep all series.
    - **`Budget %` backlog mode had the SAME gap `_periodSharePct` never received `hideAwarded` (fixed 2026-07-31):** `budgetAwardedByPeriodMonthly`/`budgetAwardedByPeriod`'s `sharePct` branch called `_periodSharePct(...)` without forwarding `opts.hideAwarded`, so the Backlog tab's Budget % chart kept drawing the always-0% **Awarded %**, **Cumulative Awarded %** and **Forecast %** series even though its two count-mode siblings had already been fixed. `_periodSharePct` now takes a `hideAwd` 8th arg and drops those three the same way `_periodCountPct` does; both call sites forward `!!(opts&&opts.hideAwarded)`.
    - **Backlog Budget % is now weighted against the WHOLE project's budget, not the backlog's own total (2026-07-31):** `_periodSharePct` previously computed `totB` by summing the `budgetData` it was given — for the Backlog chart that's ONLY the not-awarded subset, so the cumulative line always climbed to 100% regardless of whether the backlog was 5% or 60% of the project (self-normalized, not informative). `_periodSharePct` now takes a 9th arg `totalOverride` (₱M) that, when present, replaces that self-sum as the % denominator; `budgetAwardedByPeriodMonthly`/`budgetAwardedByPeriod` forward it from `opts.totalBudget`. `project.html`'s `_blRenderPeriod` (`budgetpct` branch) computes `totalBudget = Σ allWPs.approved_budget_bcb / 1e6` (the full BCB-baseline-mapped project total, all WPs regardless of award status) and passes it in, so the backlog's Budget % bars/cumulative line now read as **share of total project budget** — a small backlog reads as a small share, a large one reads as a large share. The Dashboard/Overview tab's own `sharePct` calls are untouched (no `totalBudget` passed — they already include ALL WPs, so self-sum already equals the project total). **Only `project.html`'s Backlog tab has a Budget % toggle** — `index.html`'s Backlog tab has no % mode, so no change was needed there.
    - **Backlog `# WPs %` (WP %) gets the SAME weighting fix (2026-07-31):** `_periodCountPct`'s `total` denominator was always `wps.length` — for the Backlog chart that's just the not-awarded subset, so Cumulative Planned % always climbed to 100% of the backlog itself regardless of how big the backlog is relative to the project. `wpCountByPeriodMonthly`/`wpCountByPeriod` now forward `opts.totalCount` as that denominator when present (`(opts&&opts.totalCount)||wps.length`); `project.html`'s `_blRenderPeriod` (`countpct` branch) passes `totalCount = allWPs.length` (all WPs regardless of award status), so the backlog's WP % bars/cumulative line now read as **share of the project's total WP count**, mirroring the Budget % fix above. Same scope note: only `project.html`'s Backlog tab has this toggle.
14. **Staging credential swap**: `git push origin main` (PMODepartment) and `git push dev staging:main` (fmlozano-pmo) use different GitHub accounts. After pushing to one, Windows Credential Manager caches that account and the next push to the other fails with 403. Fix: run `git credential reject` (protocol=https, host=github.com) before each cross-account push, then re-authenticate when prompted.
15. **Staging schema setup**: New Supabase staging project requires (1) run `supabase-schema.sql` in SQL Editor, (2) run all `ALTER TABLE` migrations from CLAUDE.md, (3) run GRANT statements: `GRANT SELECT,INSERT,UPDATE ON public.users TO authenticated; GRANT INSERT ON public.users TO anon; GRANT ALL ON public.users TO service_role;` (repeat for projects, work_packages, claims). Without GRANTs, all REST API calls return 403 even with valid JWT.
16. **WP List `buildTable`/`renderWPMonTable` rendering pattern**:
17. **Awarded COUNT and awarded MONEY use different bases (changed 2026-07-23).** `helpingMetrics()` counts **`award_status==='Awarded'`** (every awarded WP, cost or no cost) but sums **`awardedCost` / `budgetAwarded` only over awarded WPs with `total_awarded>0`**. Reason: a WP that has genuinely been awarded but whose cost isn't encoded yet is still awarded, and excluding it made the Overview disagree with the WP List — **CCM302 read 39 against a list of 40**, **OPW101 64 against 62**. The money stays strict because counting a costless WP's budget in `budgetAwarded` would book its whole BCB as phantom savings. Provisional costs on Not-Yet-Awarded rows are still excluded — that is decided by `award_status`, which the Excel importer now takes from the sheet's own Remarks flag, not from the presence of a cost. Consequence: `awarded count ≥ the number of WPs behind the ₱ figures`; the gap is exactly the awarded-but-no-cost rows, which the review grid highlights as required-but-empty (Awarded Cost is `reqIfAwarded`).
17. **`actualAward` / awarded count come from `helpingMetrics(wps)` in `db.js` — FULLY ROW-DERIVED** (no Helping Sheet; `HELPING_FIGURES = {}`). **Stricter awarded definition:** a WP is awarded only when **`award_status==='Awarded'` AND `total_awarded>0`** ("if it's awarded it must have an awarded cost"). Awarded count, Awarded Cost (= "Actual Award") AND Budget-for-Awarded all use that SAME set so they reconcile with each other, with the chart/forecast, and with the source Helping Sheets. `totalBudget = Σ approved_budget_bcb` (all rows). Self-maintaining for every project incl. new ones. **All six projects (AVR/GRP/OPW/SLN/SLT/UTM) were reconciled against their WP-Monitoring files** and this basis reproduces their Helping Sheets — and CORRECTS AVR101 (its Helping Sheet under-counted ₱8.9M because its date-gated `SUMIFS` over a hardcoded row range silently dropped awarded WPs with a blank actual-award date / below the last row → AVR truly runs ~₱10M OVER budget). It also rejects the opposite failure mode (GRP/SLN/SLT/OPW had **provisional costs on "Not yet Awarded" WPs** that a naive `total_awarded>0` basis over-counted — the `award_status` filter excludes them). `HELPING_FIGURES` + the override branch are retained only as an escape hatch (currently empty); prefer fixing rows. The **chart/forecast budget series** (`budgetAwardedByPeriod`/`Monthly` `isAwd` + the `_wpForecastLine` `awardedFn`) use the SAME `award_status==='Awarded' && total_awarded>0` predicate so chart ↔ KPI agree. Use `project.html?id=<PID>&diag=1` for a per-project awarded reconciliation. Consumers: `buildMetrics()`/`renderOvBattery()`/`renderStatusTable()` (project.html); `renderAll()`/`renderIdxOvBattery()`/`computeStats()` (index.html). `Cost to Complete = totalBudget − budgetAwarded`; `Variance = totalBudget − (awardedCost + CTC)`. Due-Not-Awarded / Not-Due splits remain row+date derived. **Data-quality note:** awarded WPs with a blank cost, or costs on not-yet-awarded WPs, are excluded by design — fix them at source (the diag panel lists them).
18. **`award_status` is DERIVED from `procurement_status`**: `award_status` (`Not Yet Awarded | Awarded` — **"Partially Awarded" removed**, no partial state) follows `procurement_status` (`Not Started | Sourcing | Solicitation | Evaluation & Negotiation | Awarded`): **proc `Awarded` → award `Awarded`; any earlier stage → `Not Yet Awarded`.** One-way (proc drives award). Enforced in: **WP form** `onProcStatusChange()` (award select disabled/auto); **Status Tracker** (`updateWpStatus` — the Award column is a **read-only badge** `_stAwardBadge`, and changing Procurement Status sets award, opening the `#st-award-modal` for cost+vendor when →Awarded); **CSV import** sets award from the WP-Monitoring **Remarks (Col P)** text, then **reconciles proc⇄award in `WPCsv.toWPData` (P2, 2026-07)** — `proc==='Awarded' ⇒ award='Awarded'`, else `award==='Awarded' ⇒ proc='Awarded'` — so imported rows can't land with the two disagreeing (previously a "Not yet Awarded" Remark on a proc-Awarded row left the KPIs and the form inconsistent until a re-save). Dashboard KPIs (`buildMetrics`, `computeStats`, `helpingMetrics`) still read `award_status` (now kept consistent with proc). Because awarded requires an Awarded Cost + Vendor (safeguard), setting proc→Awarded triggers that requirement.
19. **Server-side authorization = Supabase RLS (`supabase-schema.sql`), NOT the client**: RLS is enabled on `projects`/`users`/`work_packages`/`claims` with role+status+project-scope policies via the `internal.get_my_role()/get_my_status()/get_my_projects()` SECURITY DEFINER helpers. Client checks (`canAccessProject`, `body.viewer-mode` CSS) are UX only — the DB enforces WP/claims read/write scope. **WP DELETE scope (`MIGRATION_contributor_wp_delete.sql`, 2026-07):** widened from super_admin-only to the same scope as insert/update — super_admin/admin any project; specialist/manager/user their `assigned` projects; viewer denied (so contributors can run a full Replace import on their projects). **Hardened 2026-07 (`MIGRATION_rls_hardening.sql`, run once):** `users_insert` now pins `role='user' AND status='pending'` and `users_update`'s `WITH CHECK` forbids self-changing `role`/`status`/`projects` — previously a user could `PATCH` their own row (or craft the registration insert) to become `super_admin`/`approved` (privilege escalation). Also added **`wp_select_demo`** (DEMO readable by all approved users). **Viewer cost is now enforced server-side (S4, `MIGRATION_specialist_scope_and_viewer_view.sql`):** `wp_select` now excludes viewers from the base `work_packages` table entirely; viewers instead read the **`wp_view_public`** security-definer view, which re-implements the row scope (incl. DEMO) and **OMITS every money column** (`approved_budget_bcb`, `awarded_cost`, `additionals`, `total_awarded`, `variance`, `dp_percent`, `dp_amount`, `retention_percent`, `retention_amount`). The client picks the relation by role via **`db.js _wpRel()`** (non-viewers → `work_packages`; viewers → `wp_view_public`, with a memoized probe that falls back to the table if the view doesn't exist yet, so it's safe to deploy before the migration runs). **MAINTENANCE:** when you ADD a `work_packages` column, add it to `wp_view_public` too — **non-cost columns only** (never add a money column, or viewers would see it). Export PDF is also viewer-guarded (`#btn-export` hidden + `if(window.__isViewer)return`). **Supabase "security_definer_view" advisory on `wp_view_public` is an ACCEPTED exception, not a defect** — the view MUST be definer-style (owned by a privileged role, not `security_invoker`) precisely because viewers are denied SELECT on `work_packages`: it bypasses the base-table RLS but re-derives the caller's OWN scope from their JWT via `internal.get_my_*()` in its WHERE clause and omits every cost column, so it leaks nothing. `security_invoker = on` is impossible here — all logged-in users share the single `authenticated` Postgres role (roles differ only via JWT claims), and RLS cannot hide COLUMNS, so per-viewer column hiding requires a separate definer relation. `MIGRATION_wp_audit_trail.sql` recreates the view **`with (security_barrier = true)`** (stops the planner pushing a leaky outer predicate ahead of the scope conditions) and adds a `COMMENT` documenting the exception. Don't "fix" the advisory by flipping to `security_invoker` — that would re-expose cost data. Dismiss it in the Supabase advisor UI.

**Specialist edit-scope is now enforced (P1, same migration):** specialists VIEW all projects (`canAccessProject`/`getPermittedProjects` unchanged) but EDIT only assigned ones. New **`AppAuth.canEditProject(profile, pid)`** (`super_admin`/`admin` → any; others → `profile.projects`; DEMO → false) gates `wp-form.html` edit-load + new-WP save; `project.html` puts a specialist on an **unassigned** project into a read-only **"View-only"** state (`window.__canEdit`, reuses the `__archived` guards + a blue view-only banner). Server-side, the `wp_insert`/`wp_update`/`claims_insert`/`claims_update` RLS policies dropped `specialist` from the all-projects branch (SELECT still allows specialist all).
20. **Never commit real procurement data**: `data/*.json` real WP dumps were removed (they were publicly served via GitHub Pages, bypassing auth/RLS) and `data/` is git-ignored. **Git history was scrubbed 2026-07** (`git filter-repo --path data/ --invert-paths`, force-pushed to `origin`) — the data is gone from all commits and the live Pages/raw `main` URLs 404. Residual: old commit SHAs may stay cached on GitHub until GC (open a Support ticket to purge), the `dev`/`old-origin` remotes still need scrubbing-or-deleting, and the data was public for months so treat it as already exposed. The legacy `assets/js/data.js` loader was deleted (unused).
21. **Performance / dead-code (medium audit batch, 2026-07)**: (a) **Debounced search** — `window.dbnc(key, fn, ms=180)` in `index.html` + `project.html` wraps the heavy per-keystroke handlers (`wp-mon-search`/`bl-search`/`proj-table-search` on index; `f-search` listener + `bl-search` + backlog min/max on project) so the WP table + analysis panel don't rebuild on every keystroke. (b) **`review.html` quick-add modal removed** — it was unwired dead code that could have created an Awarded WP with no cost/vendor (no safeguard). (c) **SW1 (leaner `select('*')`) deliberately NOT done**: the WP detail panel reads the cached row (`window._wpById`), and bulk views need `scope` (Works tab), `proposed_vendors`/`contractor` (vendor filter) — so trimming columns would break those for little gain. The right fix for very large portfolios is server-side pagination, not column-trimming; the current `sessionStorage` quota fallback (refetch on miss) is graceful, not a bug.
22. **Asset weights / infra (low audit batch, 2026-07)**: the brand logos `assets/img/logo-white.png` (330KBâ†’28KB) and `megawide-logo.png` (202KBâ†’24KB) were **8000Ã—4000 px** originals resized to 900Ã—450 (optimized PNG, transparency preserved) â€” they display â‰¤380px, so no visible change. When replacing a logo, downscale to â‰¤~900px wide first. `WPM_Dashboard_Onboarding.pptx` is intentionally kept in the repo (wanted training deliverable, not sensitive; GitHub Pages serves the whole repo so "moving" it would mean untracking). **Supabase-dashboard-only items (not code):** raise the Auth password policy (min length/complexity) and configure custom SMTP (Resend/Brevo) so password-reset emails aren't throttled by the ~3/hr free-tier cap.
23. **Stored-XSS remediation + CSP (security audit, 2026-07)**: user-entered WP fields (`description`, `remarks`, `proposed_vendors`, `contractor`, `wp_no`…) and **self-registered `name`/`email`** are attacker-controllable (a contributor can PATCH a WP via the REST API; a pending registrant sets their own name) and were being interpolated **raw into `innerHTML`** across every render path — a payload like `<img src=x onerror=…>` stored in a field ran in an **admin's** session when they opened the WP List / User Management, exfiltrating the Supabase JWT from `localStorage` (`sb-<ref>-auth-token`) → account takeover, bypassing all RLS. **Fix:** a single **`window.esc(v)`** helper in `db.js` (escapes `& < > " '`, returns `''` for null so `|| '—'` fallbacks still work) is now applied at **all** user-text sinks in `index.html`, `project.html`, `admin.html`, `review.html`, `my-wps.html`, and the Top-5 `buildRankTable` in `db.js`. **When adding ANY new `${...}` interpolation of a user-entered field into `innerHTML`, wrap it in `esc(...)`.** **Follow-up audit (2026-07-21) closed sinks the first pass MISSED:** the **WP detail slide-in `row()` helper** (`index.html` + `project.html`) interpolated `${val}` RAW — so `description`/`remarks`/`proposed_vendors`/`wp_no`/`po_jo_numbers`/`cost_code`/`works`/`contract_package_no`/`co_description` rendered unescaped when anyone opened a WP's detail panel (account-takeover vector: token exfil via top-level `location=` navigation, which `connect-src`/`img-src` do NOT block). Fixed by giving `row(label,val,isHtml)` a 3rd flag — `${isHtml?val:esc(val)}` — and passing `isHtml=true` only for the two pre-built-HTML calls (Award-status badge, Variance span, whose inner user value is itself `esc`'d). Also escaped: `admin.html` Performance-tab user rows (`d.user.name||d.user.email`), the `db.js` approval-matrix picker label (`u.name||u.email`), the `db.js` avatar name (title attr + dropdown), the `db.js` recent-project name, and assorted project-name/id sinks in `index.html`/`admin.html`/`review.html`. **If you add a `row()`-style helper that returns HTML, escape its text arg by default and opt specific call sites OUT — never the reverse.** Attribute values already escaped for `"` only are safe (can't break a quoted attr without a `"`). Trade option-text is left unescaped **because `normTrade()` normalizes it to one of 10 canonical safe strings at load** — don't rely on that for other fields. Two special cases: (a) **inline-handler JS-arg vectors** can't be safely double-encoded (HTML-attr decode happens before JS parse, so `&#39;`→`'` breaks out) — `admin.html` `openApproveModal` was refactored to take **only `userId`** and look up name/email from `allUsers` (rendered via `.textContent`, inherently safe); the WP-delete button's `safeNo` now strips `['"\\<>]` so a crafted `wp_no` can't break the `onclick`. (b) `esc` is a `window.` property (like the intent for globals), NOT a top-level `const` (those don't attach to `window` — the `Charts`/`Fmt` gotcha). **Defense-in-depth CSP:** every `*.html` `<head>` now carries a `Content-Security-Policy` meta (inserted after `<meta charset>`). `script-src`/`style-src` keep `'unsafe-inline'` (the app is built on inline handlers + init scripts, so CSP can't stop the inline-handler XSS — escaping does), so the real value is **`connect-src 'self' https://<supabase> wss://<supabase>`** (blocks a stolen-token beacon to an attacker host) + `img-src 'self' data: blob:` + `object-src 'none'`. Allowed script/style/font origins: `cdn.jsdelivr.net`, `cdnjs.cloudflare.com`, `fonts.googleapis.com`, `fonts.gstatic.com`. **`form-action 'self'` added to all 15 pages' CSP (2026-07-21)** as defense-in-depth — blocks a form-based token-exfil POST to an attacker host even if a future XSS sink is missed (complements `connect-src`/`img-src`, which don't govern form submissions). (`navigate-to` was intentionally NOT added — near-zero browser support; the real mitigation remains escaping every sink.) **CSP GOTCHA (broke icons on first deploy):** the **Tabler icons webfont loads from `cdn.jsdelivr.net`** as a *stylesheet* (`@tabler/icons-webfont/tabler-icons.min.css`) that then pulls `.woff2` font files from the same host — so `cdn.jsdelivr.net` MUST be in **`style-src` AND `font-src`**, not just `script-src`. The first CSP shipped with jsdelivr only in `script-src`, which blocked the Tabler CSS (→ all `ti ti-*` icons vanished across every page). `connect-src` governs `fetch`/XHR/websockets ONLY (NOT `@font-face` loads — those are `font-src`), so don't test font reachability with `fetch()`. Verify icon fonts via the **preview network panel** (look for the tabler CSS/woff2 requests **not** `[FAILED]`) or `document.fonts`. Current working set: `script-src` jsdelivr+cdnjs; `style-src` fonts.googleapis+jsdelivr+cdnjs; `font-src` gstatic+jsdelivr+cdnjs+data. **Verified via local preview:** Supabase (REST+realtime), Chart.js (cdnjs), datalabels (jsdelivr), Google Fonts, and Tabler icons all load with zero CSP violations. If you add a new external origin, update the CSP in every page (the Supabase URL is hardcoded to **prod**). **Housekeeping (same pass):** removed the dead root-level `logo-white.png` (unreferenced duplicate of `assets/img/logo-white.png`), untracked `gen_import.py` (`git rm --cached` — it was committed despite being in `.gitignore`), and deleted stray local artifacts (`{`, `{data/`, `(profile.projects`).
18. **Dashboard stale data / bfcache**: `project.html` has a `pageshow` handler that calls `__wpmReloadFn()` when restored from bfcache. `index.html` has a `pageshow` handler that busts the `wpm_wps_idx_*` sessionStorage cache and reloads. `wp-form.html` and `review.html` bust that cache on save/approve/reject. Never revert these handlers. Use `_stickyLeft = {}` dict (never mutate col objects with `c._left`). Build thead via `innerHTML` with `white-space:normal;overflow:hidden` on `<th>` â€” NOT `nowrap`, which overflows adjacent cells. Build tbody via `innerHTML` string concat with a `renderCell(key, wp)` switch. Trade group header: split into `<td colspan=nSticky>` (sticky, `left:0`) + `<td colspan=rest>` (non-sticky, WP count right-aligned) â€” a full-colspan cell has nothing to stick against horizontally.
24. **`.data-table td` has NO `overflow:hidden` by default** (`dashboard.css`, only `white-space:nowrap`) — with `table-layout:fixed`, the column width IS respected for layout but nowrap content that's WIDER than its column visually spills into the next column instead of clipping (fixed 2026-07-31: `project.html`'s WP List Award Status badge — `_stAwardBadge`'s "○ Not Yet Awarded" text at the old `w:'110px'` column overlapped the adjacent Delivery Status dropdown). Fix was two-part: widened the `award_status` COLS entry to `150px` (badge text + padding actually fits), and added `overflow:hidden` on that `<td>` as a safety net for any longer legacy/custom status string. **When adding a badge/pill (not a plain text cell) to a fixed-layout table column, size the column to the badge's actual rendered width (glyph + text + badge padding + td padding) and add `overflow:hidden` on the `<td>` — don't rely on the column `w` alone.**
25. **Vestigial empty elements still cost layout space** — `project.html` had a leftover `<div class="metrics-grid" id="metrics-grid"></div>` right above `#view-tabs`, from before the old Dashboard tab was merged into Overview (see the Dashboard-tab-removed entries above). JS explicitly kept it empty ("Keep legacy metrics-grid empty" — `.innerHTML=''` every render) rather than removing it, but `.metrics-grid`'s CSS `margin-bottom:20px` still applied to the empty element, showing as a blank gap between the topbar and the view tabs that the Portfolio Overview (`index.html`) didn't have. **`index.html` has the SAME element for the same historical reason but had already neutralized it with `style="display:none"`** (comment: "Hidden metrics-grid kept for JS compatibility") — `project.html`'s copy was missing that. Fixed 2026-07-31 by removing the div and its dead JS clear-block entirely from `project.html` (nothing else referenced it there, unlike `index.html`'s copy). **When a DOM node is kept "empty on purpose," either give it `display:none` or delete it — an empty block-level element with margin/padding still reserves visual space.**
26. **WP Status battery % label — fixed white text + outline instead of conditional dark/white (2026-07-31, both pages)** — `batteryCard()`'s `%` label sits at the battery's vertical center, which can land on the green (Awarded), red-striped (Due Not Awarded), or light-gray (Not Due Yet) fill depending on the split; the old logic picked white text when `pctAwd>=50` (label on green) else near-black `#231F20` (label on red/gray). Correct in isolation, but the **% Awarded by WP** and **% Awarded by Value** cards usually have different award rates, so their labels frequently landed on different fills and rendered in different colors side by side — reads as an inconsistency bug even though each card was individually contrast-correct. Fixed by dropping the conditional and always using **white text with a solid dark outline** (4-directional 1px `text-shadow` + a soft drop shadow, not just the old single soft blur) — reads clearly on green, red-stripe, AND the light-gray fill, so both cards always match. Applies to `project.html`'s `renderOvBattery`/`batteryCard` and `index.html`'s matching `batteryCard` (Portfolio Overview) — keep both in sync if this is touched again.

27. **Battery % label font + card misalignment (2026-08-03, both pages)** — the `%` digit's `text-shadow` used 4 separate hard-edged directional offsets, which rendered jagged/fuzzy at some zoom levels; replaced with a single soft glow stack (`0 0 3px / 0 0 6px / 0 1px 2px`, all `rgba(0,0,0,...)`) plus explicit `font-family:'Montserrat',sans-serif` and `-webkit-font-smoothing:antialiased`, so the digit reads crisp instead of outlined. Separately, the two battery cards' TITLE (`% Awarded by WP` / `% Awarded by Value`) wraps to a second line whenever the sidebar is expanded (narrower content column) — since the title's height was fixed-margin only, a 2-line title pushed that card's whole battery graphic lower than its 1-line sibling, so the two bars (and their `%`/line1/line2 rows below) went visibly out of alignment side-by-side. Fixed by giving the title a `min-height:2.4em;display:flex;align-items:flex-end` (bottom-aligned within a reserved 2-line slot) so both cards' battery boxes start at the same Y position regardless of whether either title wraps. Applies to both `batteryCard()` copies — keep in sync.

28. **"Not to be Awarded" flag — a WP that will NEVER get a real cost should count as SAVINGS, not be excluded (2026-08-03, decoupled from Award Status same day).** Known Issue #17's rule (a WP's money only counts when `award_status==='Awarded' && total_awarded>0`) was built to stop WPs whose cost is merely *pending data entry* from booking phantom savings. But some WPs are marked "Awarded" in the source Remarks with **no cost by design** — Ops decided they'll never go through a formal award at all (handled outside PRC: petty cash, direct arrangement, or removed from scope; found via a free-text note in the source monitoring sheet, e.g. "NOT TO BE AWARD PER OPS" / "NOT TO BE AWARDED"). Excluding those from BOTH sides of the ledger (old behavior) understates real savings — their budget genuinely will never be spent, so it should count as realized savings at an effective ₱0 cost. Example (CSF101): WP 10 (Transport Truck/Earthmoving, ₱3.25M budget) and WP 13 (Falsework, ₱0.64M budget) are flagged this way — Savings/Loss reads +₱27.80M with the flag vs +₱23.92M without (the ₱3.89M difference is exactly their combined budget).
    - **New column**: `work_packages.not_to_be_awarded boolean DEFAULT false` (`MIGRATION_not_to_be_awarded.sql`).
    - **Deliberately does NOT require `award_status==='Awarded'`.** The first cut did require it (so the flag only "activated" once Procurement Status was set to Awarded), but that reads as a direct contradiction in the WP form — "Award Status: Awarded" sitting right next to "Not to be Awarded: Yes". Fixed same day: the flag is now fully self-sufficient. A user can leave Procurement Status at whatever honestly reflects reality (e.g. "Not Started") and just check the box — no need to lie about the status to get the savings treatment.
    - **`window.isResolved(w)`** (db.js) — `award_status==='Awarded' || not_to_be_awarded` — "is this WP done, not sitting in the active backlog." **`window.isMoneyAwarded(w)`** — `not_to_be_awarded || (award_status==='Awarded' && total_awarded>0)`. **`window.effectiveAwardedCost(w)`** — `0` when `not_to_be_awarded`, else `total_awarded||0`. **Use these everywhere the app decides a WP's resolved/backlog state or its money — never re-inline `award_status==='Awarded'` (or its negation) or `total_awarded>0`,** or a flagged WP disagrees between tabs/charts again (the exact bug #17 was written to prevent, now generalized). Consumers, swept 2026-08-03 across `db.js`, `charts.js`, `index.html`, `project.html`, `admin.html`, `review.html`: `helpingMetrics()`'s awarded COUNT and MONEY bases (the shared basis for `buildMetrics()`/`computeStats()` — every Cost-Overview KPI card on both dashboards); the WP-status **batteries** (`renderOvBattery`/`renderIdxOvBattery`) and status-bars/status-table Due-Not-Awarded / Not-Due splits; the **Backlog** tables and their period-chart filters on both pages; `charts.js`'s `budgetAwardedByPeriod(Monthly)` `isAwd`/forecast, `wpStatusDonut(Value)`, `wpByTrade`, `awardRateByTrade`, and the WP-count period charts' Forecast predicate (`_actAwDate(w)||not_to_be_awarded`, additive — doesn't touch any other WP's forecast inclusion); Top-5 `withBoth`/`variance()` (already flowed through `isMoneyAwarded`, so this "just worked" once decoupled); the Budget-tab per-trade table and WP-List "Total to be Awarded" tiles (both pages); `admin.html`'s Performance-tab awarded/overdue counts; the PDF export's awarded count.
    - **WP-count battery/donut = Yes, ₱-value battery = savings, not spend.** By design the two lenses differ: the **WP-count** side (`helpingMetrics().awarded`, `wpStatusDonut`) counts a flagged WP as Awarded (green) — it's resolved. The **₱-value** side (`hm.awardedCost`, the battery's `awd`) stays real-dollars-spent only — a flagged WP contributes `effectiveAwardedCost=0` there, so its budget never inflates "Awarded ₱"; that budget's savings nature is captured by the Savings/Loss KPI card instead, not by the battery bucket. (`wpStatusDonutValue` is the one exception — it already summed *budget*, not real cost, for its "Awarded" bucket even before this feature, so it naturally treats a flagged WP's budget the same as any other awarded WP's, with no special-casing needed.)
    - **Status badges** (`_stAwardBadge` in `project.html`, the WP-detail panels in both pages, the WP List `case 'status'` cell in `index.html`, review.html's Cards view) show a distinct **"Not to be Awarded"** label (neutral gray, not red/green) instead of the literal (and now potentially inaccurate) `award_status` text, whenever the flag is set. **Not yet done** (minor, cosmetic, low-traffic corners): the mobile card-view badges on both pages (`_sc`/`_bc` helpers take a bare status string, not the WP), the Schedule tab's per-WP expandable row (`index.html`), and `admin.html`'s Performance-tab per-row status color — these still show the literal award_status color there. None of these affect any number, only a label/color in a rarely-viewed spot.
    - **UI:** a **"Not to be Awarded"** checkbox in `wp-form.html`'s Budget & Contract section (`#f-not-to-be-awarded`, `onNotToBeAwardedChange()`) disables + clears Awarded Cost/Vendor while checked and waives their `reqIfAwarded` requirement in `save()`. Same column added to the `review.html` grid (`XL_COLS`, `g:'budget'`, `type:'bool'`) — `_xlRequired()` waives `reqIfAwarded` the same way. Both reuse existing generic `bool`-column machinery — no new editor code needed.
    - **When to check it**: only for a WP that will *permanently* never have a cost (Ops removed it from the award pipeline). A WP whose cost simply hasn't been entered yet should stay unflagged — it'll correctly show as excluded (not as savings) until a real Awarded Cost lands, per #17.

29. **`review.html` grid: bool-column cell editor showed a spurious raw `true`/`false` option (pre-existing bug, surfaced 2026-08-03 by the new Not to be Awarded column).** The cell editor's "keep legacy/off-list values" logic (`_xlBeginEdit`, ~line 1277) unshifts `String(cur)` as an extra `<option>` whenever the current value isn't already in the dropdown's option list — correct for `select`-type columns (a stored value the current option set dropped), but wrong for `type:'bool'` columns: their option list is the DISPLAY strings `['','Yes','No']`, while `cur` is the raw JS boolean `true`/`false`, so `String(cur)` (`"true"`/`"false"`) never matches and gets unshifted as a bogus 4th option every time — any bool cell with a real value showed `false`/`true`, `— none —`, `Yes`, `No` instead of just the three. This silently affected `requires_approval` too (rarely re-edited directly, so unnoticed) — it wasn't introduced by the new column, just made obvious because every row of a brand-new bool column defaults to `false`. **Fixed by excluding `col.type==='bool'` from that unshift** — a bool column's 3-state set (`blank`/`Yes`/`No`) is always complete, there's no such thing as an off-list boolean. If you add another `type:'bool'` column, this is already handled; don't reintroduce a per-column bypass.

30. **Mobile UI pass (2026-08-03): topbar title clash, dead expand buttons, review.html default view, onboarding-modal footer.**
    - **Topbar title was crushed to 1 character on phones (`index.html` + `project.html`).** Root cause was two-fold: (a) the `<div>` wrapping `.page-title`/`.page-sub` had no `min-width:0`, so as a flex item its automatic min-size was its full un-truncated content width — `.topbar-left`'s `overflow:hidden` (mobile only) then hard-clipped it instead of letting `text-overflow:ellipsis` engage; (b) even after fixing that, `.topbar-right` (`flex-shrink:0`, never shrinks) was crowded with up to 6 icon buttons (refresh, Export Excel, Export PDF, guide "?", theme toggle, avatar) — on a 375px phone their combined footprint left almost nothing for the title regardless of the ellipsis fix. Fixed both: the title wrapper now has `min-width:0;flex:1 1 auto` (project.html's wrapper also needed to stay overflow:visible, not hidden, since it hosts the `#hdr-switcher` popover — only `min-width:0` was added there); the actual truncating text moved to a dedicated `.proj-switcher-text` span (`overflow:hidden;text-overflow:ellipsis;white-space:nowrap;min-width:0`) since `.page-title`'s own ellipsis CSS doesn't apply cleanly once project.html repurposes it as an `inline-flex` row with a caret child. **And** `#btn-export`, `#btn-export-xlsx`, `#btn-guide` are now `display:none` below 768px (dashboard.css) — see next bullet for where they went. Verified via a throwaway static harness (real `dashboard.css`/`db.js`, deleted after) at 375px: topbar-right dropped from an estimated ~350px to a measured 118px, and the title box went from ~0px usable width to 153px (rendering "AVR101 — Avida R…" instead of "A").
    - **Export Excel / Export PDF / the "?" guide button moved into the profile (avatar) dropdown on mobile only**, per user request, instead of just hiding them outright. `renderUserBar()` (db.js) now builds an `exportItems` block by checking for `#btn-export-xlsx`/`#btn-export` in the DOM (so it adapts per page with no per-page code — project.html gets both, index.html gets PDF only) and injects "Export Excel"/"Export PDF" `<a class="user-menu-mobile-only">` rows that just call `.click()` on the real (now hidden) button — reusing its exact handler, including its own `window.__hideBudget` viewer guard, so nothing needed duplicating. The builder ALSO checks `window.__hideBudget` itself before adding anything, so a `viewer` never sees an export entry that would silently no-op (both pages set `__isViewer`/`__hideBudget` before calling `renderUserBar`, so the flag is always ready in time). `.user-menu-mobile-only{display:none!important}` at `≥768px` hides these dropdown rows on tablet/desktop, where the topbar buttons are the primary path — **the `!important` is required**: the rows carry an inline `display:flex` (matching the other dropdown rows' inline-styled layout) which would otherwise always beat a plain media-query rule regardless of viewport (caught by testing the real `db.js` output, not just a hand-copied snippet). The standalone `.btn-guide` topbar button is simply hidden on mobile (no dropdown replacement needed) since the dropdown's existing "Restart tour" link already opens the same tour.
    - **Chart "fullscreen" expand button did nothing on mobile.** `initExpandableCharts()` (ui.js) used only the real Fullscreen API — iOS Safari has no `Element.requestFullscreen` at all (video-only), and some Android/PWA-standalone browsers silently reject the request or never fire `fullscreenchange`, so the button looked broken. Added a CSS-simulated fallback: `.panel-fake-fullscreen` (dashboard.css — `position:fixed;inset:0;z-index:500`, mirrors `.panel:fullscreen`'s styling) is applied whenever `requestFullscreen`/`webkitRequestFullscreen` don't exist at all, whenever the request's promise rejects, or (safety net) whenever fullscreen still hasn't actually engaged ~350ms after asking. That 350ms fallback timer is cancelled the moment a real `fullscreenchange` for the panel does land (`fakeFallbackTimer`, cleared in `syncState()`), so a native fullscreen that's merely slow to engage can't end up ALSO stuck in the fake mode. Esc exits fake mode via a dedicated `keydown` listener (real fullscreen already had this via the browser). `document.body.style.overflow` is toggled to `hidden` while faked, matching the existing mobile-drawer pattern.
    - **Top 5 KPI card expand caret was a dead click target on mobile.** `initTop5Accordion()` (ui.js) has always no-op'd below 768px (mobile already stacks each card full-width, so there's nothing to expand) — but the `.t5-caret` icon was still rendered with a hover-affordance, looking clickable while doing nothing. `.top5-row .t5-caret{display:none}` + `cursor:default` on the title now hide it below 768px (dashboard.css) — no JS change needed since `initTop5Accordion` already gated on the same breakpoint.
    - **Chart expand button clipped past the panel's right edge on mobile, then two follow-ups the same day.** Round 1: the panels with a Monthly/Quarterly/WP/Budget/# toggle row (the period-chart panels on both dashboards) already filled their `.panel-title` row edge-to-edge on a phone; that row's toggle-button group is `flex-shrink:0` (never shrinks) and had no wrap room to spare, so appending the expand icon as a normal flex child rendered up to ~11px past the panel's right edge (confirmed via a throwaway harness loading the real `dashboard.css`: 374px button-right vs. a 363px panel-right). First fix let the title row wrap — no longer clipped, but a user screenshot showed the icon then landing on its own orphaned line with nothing around it, reading as randomly placed. **Round 2 (current design):** the button is no longer a flex child of `.panel-title` at all — `initExpandableCharts()` (ui.js) anchors it via `position:absolute;top:18px;right:18px` to the PANEL itself (`panel.style.position='relative'` set if not already positioned), independent of however crowded or wrapped the title row gets. The title row still gets `flexWrap='wrap'` (so a `flex-shrink:0` toggle group drops to its own row 2, getting the FULL row width there, instead of fighting the corner icon for room) plus 28px of reserved `padding-right` on row 1 (so the title's own shrinking text-line never runs under the icon — padding alone doesn't stop a `flex-shrink:0` group from overflowing into it, which is why the wrap is still needed too). Verified geometrically at 320/375/1280px with the real CSS: zero clipping, zero overlap with either the title text or the toggle group, ~3-4px clean gaps on all sides.
    - **`review.html` filter bar: the project badge and "Trade" label landed next to each other, orphaned from the Trade `<select>` (follow-up, same day)** — in per-project context the row was `[Project group (hidden)] [GPR101 badge] [Trade label] [Trade select] [search] [view switch]`, all as separate flex siblings with one shared `flex-wrap`. On a phone the wrap broke the row wherever space ran out — landing "GPR101" and the bare "Trade" text label together on one line while the actual dropdown wrapped alone to the next, reading as two mismatched badges. Fixed by grouping each label with its own control as a single flex-wrap unit (`.rv-filter-group`, mirroring the Project group's existing pattern), plus a `≤520px` layout that stacks the badge / Trade group / search / view-switch into four full-width rows instead of an unpredictable wrap. Verified with a harness: badge on its own row, "Trade" label+select together on the next row, both confirmed unchanged at desktop width.
    - **`review.html` opened into the Grid (Excel) view on mobile** — a full spreadsheet is unusable on a phone screen; the desktop default makes sense there but not on mobile. The `requireLogin` callback now calls `setReviewView('cards')` once, right after the initial `loadAll()`, when `matchMedia('(max-width:767px)')` matches at load time. Desktop/tablet are untouched (still default to Grid); a user can still switch views manually on either.
    - **Onboarding "Dashboard Guide" modal footer wrapped badly on mobile** (`onboarding.js`) — `.ob-foot` crammed 4 items (progress dots, the "Don't show on login" checkbox, Back, Next) onto one `nowrap` row; the checkbox label had no `white-space:nowrap` so it wrapped to 2 lines and visually collided with the buttons. The existing `@media(max-width:520px)` block now also sets `.ob-foot{flex-wrap:wrap;row-gap:10px}` with explicit `order` on each child — dots (own centered row) → checkbox (own centered row, `white-space:nowrap`) → Back/Next (`flex:1` each, split the row 50/50) — plus a smaller `.ob-overlay` padding and `.ob-x` close-button size so the modal itself uses more of a small screen's width.

31. **`project.html` sidebar project-list names were hard-clipped instead of ellipsis-truncated (2026-08-03).** `renderSidebarProjects()`'s per-project `<a class="nav-item">` is a flex row (icon + text `<span>`), but the `<span>` had `overflow:hidden;text-overflow:ellipsis;white-space:nowrap` with no `min-width:0` — a flex item's default `min-width` is `auto`, not `0`, so it refuses to shrink below its content's natural width regardless of `overflow`/`text-overflow`. The text just ran past the sidebar's edge and got hard-clipped by `.sidebar{overflow-x:hidden}` (no "…"), worse on longer project names and the ~6px scrollbar eating into the already-narrow 240px sidebar. **Fixed by adding `flex:1;min-width:0`** to the span (matches the working pattern already used elsewhere, e.g. index.html's period-chart title span). `index.html`/`admin.html`'s own sidebar project lists don't have this bug — they truncate by slicing the string in JS (`.slice(0,12)`/`.slice(0,14)`) rather than relying on CSS ellipsis, so they were never exposed to this. If you add another CSS-ellipsis text span inside a flex `.nav-item`, it needs `min-width:0` explicitly — `flex-shrink:1` alone is not enough.
    - **`admin.html`'s sidebar had a DIFFERENT bug (same investigation, fixed same day):** `renderSidebarProjects()` there set `navProj.style.maxHeight='240px'; navProj.style.overflowY='auto'` on the project list — a second scrollbar nested inside `.sidebar`'s own `overflow-y:auto`, the exact "two scrollbars side by side" anti-pattern the sidebar-scrolling rule elsewhere in this doc warns against (`project.html`/`index.html` were already fixed for it; `admin.html`'s own copy of this feature had drifted). Removed the inline max-height/overflow so the list grows naturally in the single sidebar scroller, and made `#sidebar-proj-search-wrap` `position:sticky;top:0` (background `var(--mw-black)`) so the search box stays reachable while scrolling, matching `project.html`'s pattern.

---

## Workflow Rules

- **After every prompt:** Update relevant sections of this CLAUDE.md, then commit and push all modified files (including CLAUDE.md) to **production only** (`git push origin main`). **NEVER push to staging** (`git push dev staging:main`) â€” user has explicitly stopped staging pushes.
- **Cross-account push 403:** Windows Credential Manager caches one GitHub account at a time. Before switching accounts, run `git credential reject` (protocol=https, host=github.com) then re-authenticate when prompted. See Known Issues #14.

---

## Deployment

```bash
git add <files>
git commit -m "description"
# Push to production (PMODepartment/prc-app)
git push origin main
# Push to staging (fmlozano-pmo/prc-app-dev) â€” clear credentials first if last push was to prod
git push dev staging:main
```

GitHub Pages auto-deploys on push to main (~1â€“2 min).

**Pages build can silently FAIL to trigger (2026-07):** a real incident — a fix was pushed to `origin/main` (confirmed via `raw.githubusercontent.com`) but GitHub Pages never created a deployment for it, so the live `pmodepartment.github.io` site kept serving the OLD file for 30+ min regardless of hard-refresh. Symptom: the fix is in the raw source but NOT on the live URL, and the served `Last-Modified` header is hours stale. **Diagnosis:** `curl -s "https://api.github.com/repos/PMODepartment/prc-app/deployments?per_page=1"` — if the newest `github-pages` deployment `sha` isn't your commit, the build didn't fire. **Fix:** push an **empty commit** to nudge it: `git commit --allow-empty -m "chore: trigger Pages rebuild" && git push origin main`, then poll the live URL until the new code appears. (Separately, once a build DOES deploy, the Fastly CDN caches HTML `max-age=600` and ignores query strings, so `?x=…` can't bust it — wait out the 10-min TTL. `import-monitoring.html` has no `?v=` cache-buster, so browsers also need a hard refresh.)

---

## Development Notes

- Pure vanilla JS â€” no npm, no bundler, no TypeScript
- Supabase via UMD bundle (`supabase.min.js`) â€” `window.supabase.createClient()` called in `auth.js`
- Always use `AppAuth.requireLogin()` / `AppAuth.requireAdmin()` as page entry point
- `WPDb.mapWP()` normalizes aliases: `budget_bcb` â†” `approved_budget_bcb`, `contract_amount_php` â†” `total_awarded`
- `Fmt.money(v)` **auto-scales**: `â‚±16.52B` for â‰¥1e9, else `â‚±X.XXM` (so billions aren't shown as an ambiguous `â‚±16521.42M`); `Fmt.moneyShort(v)` â†’ `â‚±16.52B`/`â‚±2.40M`/full <1M (KPI headlines); `Fmt.moneyFull(v)` â†’ `â‚±16,521,423,165` (tooltips); `Fmt.date(d)` â†’ `May 29, 2025` (global date format is **Mmm dd, yyyy**; old mm/dd/yyyy formatters switched)`
- **Chart number format**: `charts.js` `_mAbbr(vM)` (datalabels) and `_axM(vM)` (axis ticks) take a value **already in millions** and render B/M (`16.5B` / `350M`). All chart datalabels, axis callbacks, and the per-trade donut centre labels route through these â€” never re-introduce a bare `(.../1e6).toFixed(1)+'M'`. **`_mAbbr` ALWAYS appends `M`/`B`, so only use it for money charts.** `wpAgingBuckets` (Work Package by Aging Bucket) plots **WP COUNTS**, not money — its datalabel uses `String(Math.round(v))` (plain integer). Previously it wrongly used `_mAbbr`, so a count of 3 rendered as `3M`. Any count-based chart's datalabels must stay plain integers.
- **wpStatusDonut omits zero-count categories** — it builds the donut from only the present buckets (Awarded / Due but Not Awarded / Not Due), so a backlog (not-awarded only) set shows just the not-awarded slices, not an empty "Awarded" legend entry.
- **Trade palette**: the Works-tab stacked bar + Budget/Awarded/WP-count donuts use a shared 20-colour distinct palette (`#EE3124,#2D9B6F,#2563EB,#D97706,#7C3AED,#0EA5E9,#DB2777,#65A30D,#EA580C,#0D9488,#9333EA,#CA8A04,#DC2626,#16A34A,#4F46E5,#F59E0B,#BE185D,#0891B2,#84CC16,#92400E`) â€” replaced the old 10-colour set that had multiple near-greys (projects like OPW101 have ~19 trades). Defined as `COLORS` in the three charts.js trade funcs and inline for the index.html WP-count donut (`c-idx-wk-count`).
- **Top 5 Savings/Overbudget** (both pages) count **only `award_status==='Awarded'`** WPs (`withBoth` filter) â€” a Not Yet / Partially Awarded WP would otherwise show phantom savings while still in progress. Top 5 by Contract Value still uses any `total_awarded>0`.
- **Top 5 rows are clickable** â†’ open the WP detail slide-in panel (`openWPDetail`). `buildRankTable` adds `onclick="openWPDetail(id)"` + `cursor:pointer` when the rank item carries an `id`; callers pass `id:w.id` in the mapped items. Lets users jump straight from a Top-5 row to full WP details + Edit/Delete instead of hunting in the WP List / Review tabs.
- **Table header alignment**: numeric table headers must match their cells' alignment (right for â‚± columns, center for counts/%) â€” left-aligned headers over right-aligned values read as "different columns". Fixed on the Budget Summary by Trade `<thead>` (`#idx-bud-table`).
- Charts: Chart.js v4.4.1 via cdnjs + `chartjs-plugin-datalabels@2.2.0` via jsdelivr; functions in `assets/js/charts.js`
- **Excel export** (`exportProjectExcel` in `project.html`, 2026-07-23): an **Export Excel** button (`#btn-export-xlsx`, `ti-file-type-xls`, beside Export PDF) generates a real `.xlsx` of the project's work packages. **SheetJS is lazy-loaded on first click** (`_loadSheetJS()` injects `cdn.jsdelivr.net/npm/xlsx@0.18.5/dist/xlsx.full.min.js` — same version as the importer; allow-listed in `script-src`) so it doesn't weigh down the dashboard. Columns are the dashboard's `COLS` (all of them), rows are the full BCB-baseline-mapped `allWPs` sorted by `TRADE_ORDER` then numeric WP No.; `_xlsxCellVal(w,key)` mirrors `renderCell`'s derived values — money/counts stay **numeric**, dates as `YYYY-MM-DD`, percents ×100 (`dp_percent`/`retention_percent`), `variance` = `bud−awd`, `pct_wt` = `savings ÷ _projTotalBCB`. Header row frozen; file name `<PID>_WorkPackages_<YYYYMMDD>.xlsx`. **Viewer-guarded** exactly like the PDF button (`body.viewer-mode #btn-export-xlsx{display:none}` + `if(window.__isViewer)return` in both the handler and `exportProjectExcel`) so viewers can't export cost data.
- **PDF export** (`exportPDF` in `db.js`): jsPDF landscape A4, margin 14mm, usable width 269mm. Column widths sum to ~255mm (multi-project) / ~247mm (single). `tableWidth: avail` prevents overflow. Font 6.5pt. Use plain ASCII `'-'` for nulls and plain `M` suffix for monetary values â€” jsPDF Helvetica does not render `â‚±` or `â€”` (em-dash) correctly. Multi-project adds `Project` col (16mm); single-project widens `Description` to 48mm.
- **Planned-vs-Actual line styling + Forecast line**: across the cumulative Planned-vs-Actual WP-count charts (`wpCountByPeriodMonthly`, `wpCountByPeriod`, `wpByPeriodQuarterly`) and the `sCurve`, **only the Forecast line is dashed** â€” Cumulative Planned and Cumulative Actual are now **solid** (removed their old `borderDash`). Each adds a **`Forecast`** dataset (same red `#EE3124`, `borderDash:[6,4]`) continuing the Actual line by projecting the **not-yet-awarded** WPs toward 100%. **Forecast award/completion month per WP** (`_wpForecastLine` for count charts; inline `fdate` in `sCurve`): already-Awarded â†’ excluded (Actual); not-awarded with **planned `awarding_date` still in the future** â†’ its **planned award month** (on-schedule); not-awarded **overdue** (planned date < today) or no planned date â†’ the **next period** after today (backlog assumed cleared next period). Anchored to the current Actual total (null before today's period) so the dashed line joins the solid Actual line and climbs to total WPs. **`_wpForecastLine(keys,keyOf,wps,cumActual,valFn,awardedFn)`** takes an optional `valFn` â€” default `()=>1` (counts) for the WP-count charts; the **₱ `budgetAwardedByPeriod(Monthly)` charts pass `w=>(w.approved_budget_bcb||0)/1e6`** so the Forecast = actual Cumulative Awarded (cost) so far + the **budget** of the remaining not-awarded WPs. **`awardedFn` (6th arg) is the predicate for "already counted in the Actual/Awarded cumulative line" and MUST match the chart's actual series, else the WP is double-counted (its cost in the anchor + its budget added again) and the dashed Forecast renders ABOVE the Cumulative Budget instead of below it by the savings.** Budget charts pass `w=>(w.total_awarded||0)>0` (their awarded series is `isAwd=total_awarded>0`); count charts pass `w=>!!_actAwDate(w)` (their actual series counts by actual award date); default is `award_status==='Awarded'`. This was the "forecast over budget despite ₱X savings" bug — caused by cost-bearing WPs flagged "Not yet Awarded" (`total_awarded>0` but `award_status!=='Awarded'`, common in the imports per #17) being both in the anchor and re-added at full budget. **The two forecast modes behave DIFFERENTLY on purpose (per user):** the **# WPs** forecast is a *normal S-curve* converging to the planned total (every WP = 1, no savings); the **₱ budget** forecast intentionally **ends at projected spend** â€” below the total Cumulative Budget by the savings already realised on awarded WPs (it does NOT force-converge to total budget). Do NOT re-add the `_fcBudgetValFn` scaling that made budget meet total BCB â€” that was explicitly reverted. The $ charts add the Forecast dataset only when **not** `hideAwarded` (so the Backlog instances â€” which have no Awarded series â€” skip it). **`_extendForecastAxis(set,wps,mode,hideAwd)`** adds the **current + next period** to the axis when a backlog exists, so projects whose WP dates are all in the past still give the dashed Forecast (and the awarded tail) somewhere to land â€” without it the forecast collapsed to a single invisible anchor point (the per-project Overview `c-dash-period` bug). **Cumulative Actual / Cumulative Awarded both stop at today** (`cumADisp`, `spanGaps:false`) on the count charts AND the $ charts â€” the solid red line ends at the current month/quarter and the dashed Forecast continues from there (no flat run into future budget periods).
- **Legend line keys**: `_lineLegendStyle(cfg)` runs inside `make()` for every chart â€” if the config has any line dataset it sets `legend.labels.usePointStyle=true` and gives line datasets `pointStyle:'line'` (renders a line key, not the default hollow box) while bar datasets get `pointStyle:'rect'` (stays a box). It also overrides `legend.labels.generateLabels` to copy each dataset's `borderDash` onto the legend item's `lineDash`, so the **Forecast key renders as a DASHED line** (not solid) in the legend. Pure bar/donut charts are untouched. Applies to the S-curve and all combo (bar+line) period charts.
- Data labels: plugin registered globally with `display:false` default; each chart opts in via `plugins.datalabels`. Helpers: `_dlBar(fmtFn, axis)` (outside-end), `_dlStacked(fmtFn)` (center, white), `_dlDonut(fmtFn, minPct)` (center, white, skip <5%), **`_dlTidy(fmtFn, [labels], frac)`** (declutters dense period charts: labels only the named series e.g. `['Budget (BCB)']`, **horizontal `rotation:0`** (was vertical `-90` — switched to horizontal for readability per user request), skips bars below `frac`Ã—series-max, default 6%). `budgetAwardedByPeriod`/`budgetAwardedByPeriodMonthly` take `opts.tidyLabels` â†’ use `_dlTidy`; the index.html **WP-List Filtered Analysis**, **Dashboard**, **Budget** (`_idxBudRenderPeriod`), and **Schedule** (`_idxSchRenderPeriod`) "Procurement Budget (BCB) and Awarded by Period" budget-mode call sites all pass `{tidyLabels:true}`. `_dlTidy(fmtFn, seriesColors, frac)` now takes a **`seriesColors` map** (`{'Budget (BCB)':'#231F20','Awarded':'#EE3124'}`) so **both** the Budget AND Awarded bars are labelled in **distinct colours** (Budget dark, Awarded red) â€” each series skips values below `frac`Ã—its-OWN-max; labels are now **horizontal** (`rotation:0`) for easier reading. Dark mode brightens via `darkMap` (`#231F20/#282C28â†’#DCDBDB`, `#EE3124â†’#FF7A6E`, `#2D9B6Fâ†’#5FD3A3`) (both had clashing overlapping labels with two series Ã— many months). Mobile already hides datalabels (`_mob()` â†’ `display:false`), so tidy only affects desktop. Dense charts (awardingLeadTime, budgetVsContract, varianceTrend, budgetByPeriodPerTrade, scheduleTimeline) keep labels off. Mobile: font 7px (vs 9px desktop) via `_mob()` check.
- **Dark mode**: Toggle button in topbar (`renderUserBar` in `db.js`). `AppTheme` IIFE in `ui.js` â€” `init(userId)`, `toggle(userId)`, `apply(dark)`. Preference stored in `localStorage`: `wpm_theme_{userId}` (per-user) + `wpm_theme_last` (fast anti-flash). Anti-flash IIFE at top of `ui.js` applies `dark-mode` class to `body` and `html` before render. `auth.js` calls `AppTheme.init(userId)` after profile load. Charts updated via `Charts.updateTheme(dark)` in `charts.js`.
  - **CSS pitfall â€” class rules vs attribute selectors**: inline `<style>` blocks in `project.html`/`index.html`/`admin.html` define `.budget-table`, `.ov-donut-card`, `.ov-stats-card`, `.works-kpi`, `.sched-wp`, `.kpi-card`, `.section-card`, `.user-table`, `.perf-table`, `.tab-btn` etc. with hardcoded light colors â€” these are CSS **class rules**, so they override the cascade and are NOT caught by inline-style attribute selectors (`[style*="color:#231F20"]`). Each needs an explicit `body.dark-mode .classname` override in `dashboard.css` (Tables + admin sections).
  - **Inline backgrounds covered by attribute selectors**: `#fff`â†’surface; `#f5f5f5/#f9f9f9/#f0f0f0/#fafafa/#f8f8f8/#e0e0e0/#ddd`â†’surface-2. `#fafafa`=total rows/trade headers, `#f8f8f8`=sticky WP headers/sub-tab container, `#e0e0e0`/`#ddd`=collapse/expand +/âˆ’ toggle badges (Works/Schedule/WP-List).
  - **Reserialised-style gotcha**: when JS sets ANY `element.style.*`, the browser reserialises the whole inline style, turning authored `#fff` into `rgb(255, 255, 255)` â€” which **escapes** the `[style*="background:#fff"]` selectors (e.g. `#proj-filter-btn` after `toggleProjFilter()` touches its style â†’ stayed bright white in dark mode). Fix: added `[style*="background: rgb(255, 255, 255)"]` variants + an explicit `body.dark-mode #proj-filter-btn` rule. For JS-mutated elements prefer an ID/class dark rule over relying on inline-color attribute selectors. **Schedule Summary by Project (`index.html`)**: `toggleSchProj`/`toggleSchTrade` mutate `style.display` on `#f0f0f0`/`#fafafa` rows â†’ reserialised to `rgb(240,240,240)`/`rgb(250,250,250)`, so they reverted to light after collapse+expand. Fixed by adding `rgb(240/250/248/245/249/224, â€¦)` variants to the surface-2 selector list in `dashboard.css`.
  - **`color-scheme: dark`**: set on `body.dark-mode` in `dashboard.css` so native form controls (`<select>` option popups, date pickers, scrollbars) render dark across all dashboard pages.
  - **`wp-form.html` dark mode**: form fields already use `var(--surface-2)`/`var(--text-primary)`; added an inline `body.dark-mode` block fixing the `:focus{background:#fff}` flash, the readonly `#f-type` field, `.form-section-title`, the `.ms-*` multiselect dropdown/pills, `.status-info`/`.alert-*`/`.rejection-box`, the `.bulk-import-card` banner, and the two `.modal-surface` modals (Template Picker, CSV Import).
  - **Buttons**: `body.dark-mode button:not([style*="color"])` gives a light default to buttons relying on the UA black `ButtonText` (e.g. Collapse/Expand toggles whose bg was darkened). Buttons with inline color or a 2-class rule still win.
  - **Charts (`charts.js`)**: `_themeConfig(cfg,dark)` remaps near-black brand hues (`#282C28/#231F20/#2B2C2B` â†’ `#9B9999`, a visible MID gray kept 65 levels below the `#DCDBDB` "Future"/"Not Due" buckets â€” at `#B8B7B7` the two grays were nearly identical in the Status donut; translucent bar variant `rgba(40,44,40,0.55)` â†’ `rgba(155,153,153,0.92)`) on **donut segments, bar fills (Budget(BCB)/Planned/Total WP bars â€” else invisible on dark), and combo-chart cumulative lines**, adds 2px surface arc separators to donuts, and flips `_dlBar` value-label color. Originals cached on the dataset (`_origBg`/`_origBorder`) so light mode restores exactly; idempotent.
    - **CRITICAL Chart.js v4 gotcha**: `_themeConfig` operates on the **config BEFORE `new Chart()`** (in `make()`), NOT on a live chart. Chart.js v4 caches bar element options, so mutating `dataset.backgroundColor` + `update()` on an existing chart re-resolves **line strokes but NOT bar fills** â€” bars stay their original near-black and look "stuck dark" even though `dataset.backgroundColor` reads correct. Therefore `make()` themes the config pre-construction, and `updateTheme()` (toggle) **destroys + re-creates** every chart (`new Chart(canvas, rethemedCfg)`) rather than mutate+update. Never revert to a post-creation `chart.update()` approach for bar colors.
  - **Mobile WP List cards (`.wpc*`)**: built in JS (`#proj-wp-cards`/`#wp-mon-cards`) but styled by `.wpc`/`.wpc-desc`/`.wpc-kv`/`.wpc-actions` etc. class rules (identical in both files). Dark overrides in the "Mobile WP List cards" section of `dashboard.css` (`.wpc` bgâ†’surface, textâ†’primary/secondary/hint, action buttonsâ†’surface-2).
  - **Top 5 rank tables**: do NOT add a blanket `body.dark-mode .rank-side { color }` rule â€” it kills the inline Savings(green `#2D9B6F`)/Overbudget(red `#EE3124`) accent colors. Let the inline-color attribute selectors recolor only neutral cells; accents are left untouched.
  - **Public-page dark mode (self-contained)**: `login.html`, `register.html`, `pending.html`, `forgot-password.html` don't load `ui.js`/`dashboard.css`, so each has its OWN inline dark mode (identical pattern): inline anti-flash `<script>` in `<head>` adds `dark-mode` to `<html>` from `wpm_theme_last` (or system pref if unset); CSS rules prefixed `html.dark-mode` (set early enough to avoid a white flash on `.form-wrap`/`.card`); a fixed top-right `.login-theme-toggle` (sun/moon inline SVG) calls `toggleLoginTheme()` which persists `wpm_theme_last`. Shares the `wpm_theme_last` key with the dashboard so the theme carries across the whole auth flow â†’ dashboard. Gotcha: `forgot-password.html`'s logo has an inline `filter:invert(1) brightness(0)` (black logo for the white card) â€” dark mode overrides it with `html.dark-mode .logo-wrap img{filter:none!important}` so the white logo shows. These are inline (no `?v=` needed; HTML revalidates via ETag).




















